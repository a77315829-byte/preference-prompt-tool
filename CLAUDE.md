# 선호 기반 프롬프트 자동 생성 도구

## 프로젝트 목표

사용자가 결과물 두 개 중 마음에 드는 쪽을 8~10회 고르면, 그 선택 이력에서
사용자의 암묵적 선호를 추정하고, 이를 평가 함수로 변환해 자동 프롬프트
최적화기(GEPA)에 전달한다. 최종 산출물은 그 사용자 전용 시스템 프롬프트
텍스트 한 덩어리다.

핵심 전환: 기존 개인화 수단은 "당신이 원하는 것을 서술하세요"에서 출발한다.
본 도구는 "이 둘 중 나은 쪽을 고르세요"에서 출발한다.

**이 프로젝트가 하지 않는 것**
- 언어 모델을 학습하거나 파인튜닝하지 않는다. 상용 API를 부품으로 호출만 한다.
- "범용적으로 좋은 프롬프트"를 목표로 하지 않는다. 개인 적합도가 목표다.
- 산출물이 모든 사람에게 잘 맞으면 개인화에 실패한 것이다.

## 팀 및 제약

- 3인, 3개월, 타 프로젝트 병행 (1인당 주 10시간 가정)
- 실제 개발 가능 기간 6~8주로 간주하고 범위를 잡는다
- 완성도의 기준은 "동작하는 도구"가 아니라 "측정된 결과"다

---

## 절대 규칙

1. **`engine/` 안의 코드에 도메인 특정 단어가 등장해서는 안 된다.**
   "요약", "문장 수", "추출성" 같은 단어는 금지. 축 이름, 축 값, 프롬프트
   문구, 검사 함수 지정은 전부 `domains/*.yaml`에서 읽는다. 엔진은 "축이 N개
   있고 각 축에 값이 M개 있다"까지만 안다.
   → 이 규칙이 확장성 주장의 실체다. 어기면 도메인 추가 시 엔진을 다시 짜야 한다.

2. **API 호출 결과는 반드시 캐싱한다.** 같은 (원문, 축조합) 쌍은 두 번
   호출하지 않는다. 실험을 수백 회 돌리므로 캐싱 없이는 비용이 폭발한다.

3. **새 라이브러리를 추가하기 전에 먼저 물어본다.** 의존성을 늘리지 않는다.

4. **API 키를 코드에 하드코딩하지 않는다.** `.env`에서만 읽는다.
   `.env`는 절대 커밋하지 않는다.

5. **한 번에 파일 하나 또는 함수 하나 단위로 작업한다.** 전체를 한꺼번에
   생성하지 않는다.

6. **평가 지표는 가능한 한 코드로 직접 잰다.** LLM 판정은 최후 수단.
   싸고 빠르고 재현 가능한 쪽을 우선한다.

---

## 스택

```
Python 3.12   (3.13/3.14 금지 — 패키지 호환성 문제)
├─ dspy         최적화 프레임워크
├─ gepa         프롬프트 최적화기
├─ litellm      여러 모델 API 통합 인터페이스
├─ streamlit    UI (로컬 실행)
├─ pandas       실험 집계
├─ matplotlib   그래프
├─ pyyaml       도메인 정의 로드
├─ python-dotenv
└─ pytest       검사 함수 테스트
```

- DB는 SQLite 또는 JSON 파일. 서버형 DB를 쓰지 않는다.
- 배포는 프로젝트 후반의 선택 항목. 초기에는 전부 로컬.
- 후보 생성은 저렴한 소형 모델, GEPA 성찰 모델만 강한 모델을 쓴다.

---

## 디렉토리 구조

```
preference-prompt-tool/
├─ CLAUDE.md
├─ .env                        (커밋 금지)
├─ .gitignore
├─ requirements.txt
├─ domains/
│   └─ summarization.yaml      축 정의 · 프롬프트 문구 · 검사함수 이름
├─ engine/                     ← 도메인을 모르는 코어 (★ 팀의 핵심 기여)
│   ├─ domain_loader.py        YAML 로드 및 검증
│   ├─ generator.py            축조합 → 프롬프트 조립 → API 호출 → 캐싱
│   ├─ selector.py           ★ 다음에 보여줄 쌍 선택 알고리즘
│   ├─ estimator.py          ★ 선택 이력 → 축별 선호 + 확신도 추정
│   └─ metric_builder.py     ★ 선호 → GEPA용 평가 함수 조립
├─ checks/
│   └─ summarization.py        축별 검사 함수 (도메인 의존)
├─ optimize/
│   └─ run_gepa.py             GEPA 연동
├─ experiments/
│   ├─ persona.py              MACSum 속성조합 기반 선택 대행
│   ├─ run_all.py              실험 실행 및 집계
│   └─ plots.py                수렴 곡선 등 시각화
├─ app.py                      Streamlit UI (마지막에 작성)
├─ data/
│   └─ macsum/                 (커밋 금지)
└─ cache/                      (커밋 금지)
```

★ 표시가 라이브러리로 대체 불가능한 부분이다. 여기에 시간을 쓴다.

---

## 도메인: 문서 요약 (MACSum 기반)

### 데이터셋

MACSum — 혼합 속성 제어 요약을 위한 사람 주석 데이터셋.
출처: https://github.com/psunlpgroup/MACSum
원문은 CNN/DailyMail(뉴스)과 QMSum(대화)에서 수집.

**이 데이터셋을 쓰는 이유**: 축 목록이 이미 학술적으로 정의되어 있고,
같은 원문에 대해 서로 다른 속성 조합으로 사람이 작성한 요약이 들어 있다.
따라서 **속성 조합 하나 = 페르소나 하나**로 쓸 수 있다.

**9주차에 수정**: 원래는 "그 조합의 사람 요약 = 답안지"로 삼아
정답 요약과의 단어 겹침으로 페르소나의 선택을 대행했는데, 실험을
돌려보니 `extractiveness=fully`(원문 그대로 발췌) 후보가 원문 고유명사를
그대로 가져와 정답 요약과 우연히 어휘가 더 겹쳐버려 엉뚱한 축으로
수렴하는 문제가 있었다 (내용 겹침과 문체 겹침을 구분 못 함). 정답
요약 텍스트 대신, 6주차에 검증해둔 `checks/summarization.py`를 페르소나의
실제 축값에 대고 두 후보에 돌려 점수가 더 높은 쪽을 고르는 방식
(`experiments/persona.py`)으로 바꿨다. 수정 전엔 5문서 평균 완전복원율이
0~8%였는데, 수정 후 순차/불확실도 알고리즘 모두 80%로 올라갔다.

### 사용할 축 (3개, 6주차에 4개→3개로 축소)

| 축 | 값 | 검사 방식 |
|---|---|---|
| length | short / normal / long | 코드 (문장 수, MACSum 실측 분포로 보정) |
| extractiveness | normal / high / fully | 코드 (원문과의 연속 2단어(bigram) 겹침 비율) |
| topic | 키워드 지정 | 코드 (키워드 포함 여부) |

`speaker` 축은 대화 전용이므로 제외한다 (후속 연구들의 관례).

`specificity`(숫자·고유명사 밀도) 축은 6주차에 범위에서 제외했다.
정규식 휴리스틱은 물론 spaCy NER로 측정해도 normal(0.217)과 high(0.228)
사이에 실질적 차이가 없었다 (val split 실측). MACSum의 specificity
라벨은 표면적 수치·고유명사 밀도가 아닌 다른 성질을 반영하는 것으로
보이고, 100% topic 축과 동반 출현해 독립 축으로 보기도 어렵다. 코드로
재현 가능하게 잴 수 없는 축은 넣지 않는다 (절대 규칙 6).

**8주차에 발견한 중대 버그(수정 완료)**: `task_description`이 한국어라
출력 언어가 축조합에 따라 들쭉날쭉했다 (extractiveness=fully일 땐 원문을
그대로 베껴 영어로, 그 외엔 한국어로). MACSum 원문·정답 요약이 전부
영어라서 lexical_overlap 체크, keyword_presence 체크, persona.choose
전부가 언어 불일치로 왜곡된 값을 내고 있었다. task_description에 "요약은
반드시 영어로 작성하라"를 명시해 고쳤다. 같은 맥락에서 `generator.py`의
캐시 키도 (도메인명, 축조합)이 아니라 조립된 프롬프트 텍스트 자체로
바꿨다 - 안 그러면 YAML 문구를 고쳐도 캐시가 무효화되지 않는다.
`experiments/persona.py`의 유사도 지표도 자카드 대신 Dice(F1)로 교체했다
(3주차에 발견한 길이 편향 수정). 이 세 수정을 다 적용한 뒤에야 실제 API
호출로 도는 선택 루프 전체가 숨긴 페르소나 선호를 정확히 복원하는 걸
확인했다 (test_closed_loop.py).

---

## GEPA 연동 방식

GEPA에 넘기는 것은 세 가지다.

| GEPA 인자 | 우리 쪽 대응 |
|---|---|
| `seed_candidate` | 추정된 축 선호로 조립한 초기 시스템 프롬프트 |
| `trainset` / `valset` | MACSum 원문 수십 개 |
| `metric` | **`engine/metric_builder.py`가 조립한 평가 함수** ← 우리 기여 |

```python
result = gepa.optimize(
    seed_candidate={"system_prompt": seed},
    trainset=train, valset=val,
    task_lm="<저렴한 모델>",        # 요약 생성
    reflection_lm="<강한 모델>",     # 성찰
    max_metric_calls=150,           # 비용 상한
)
optimized = result.best_candidate["system_prompt"]
```

**중요한 설계 포인트**: GEPA는 점수뿐 아니라 자연어 피드백도 받는다.
평가 함수가 축별 위반 내역을 텍스트로 반환하면 GEPA가 이를 성찰에 사용해
적은 시도로 좋은 프롬프트를 찾는다. 우리는 축을 명시적으로 갖고 있으므로
이것이 가능하다. 범용 최적화 도구는 못 하는 부분이며, 프로젝트의 기여로
명시한다.

```python
# metric 반환 형태
return 0.6, "길이 위반: 6문장(목표 3문장 이하). 원문 수치 3개 중 1개 누락."
```

비용 관리를 위해 초기에는 `auto="light"` 수준으로 시작한다.

---

## 개발 순서

| 단계 | 주차 | 내용 |
|---|---|---|
| 0 | 1 | 환경 구성, MACSum 로드, 같은 원문의 속성별 요약 비교 출력 |
| 0 | 1 | 팀원 3인 각자 GEPA 튜토리얼 1회 완주 |
| 1 | 2 | `domains/summarization.yaml` 작성 |
| 2 | 3 | `generator.py` — 축조합 → 결과물 생성 + 캐싱 |
| 3 | 3 | `persona.py` — MACSum 정답 요약 기반 선택 대행 |
| 4 | 4–5 | ★ `selector.py`, `estimator.py` (무작위·순차·불확실도 3종 구현) |
| 5 | 6 | ★ `metric_builder.py` + `checks/` |
| 6 | 7 | `run_gepa.py` 연동, 엔드투엔드 1회 관통 |
| — | 8 | **중간 점검 — 범위 축소선 판단** |
| 7 | 9–10 | 주 실험 실행, 알고리즘 3종 수렴 곡선 비교 |
| 8 | 11 | Streamlit UI (얇게), 실사용자 3~5인 검증, 확장성 검증 |
| 9 | 12 | 결과 정리, 발표 준비 |

**UI는 반드시 마지막에 만든다.** 먼저 만들면 시간을 전부 잡아먹는다.

---

## 평가 계획

### 측정 지표

| 지표 | 정의 |
|---|---|
| 기준 복원율 | 숨긴 축별 선호 중 정확히 맞힌 비율 |
| 수렴 질문 수 | 복원율이 목표치에 도달하기까지의 선택 횟수 |
| 평가 함수 일치율 | 생성된 평가 함수와 페르소나 기준의 판정 일치 비율 |
| 최종 만족도 | 산출 프롬프트 결과물에 대한 페르소나 채점 |

### 비교군

| 조건 | 설명 |
|---|---|
| A | 프롬프트 없이 기본 호출 |
| B | 사용자가 커스텀 인스트럭션을 직접 작성 |
| C | 기존 상용 프롬프트 생성 도구의 출력 |
| D | 본 도구 |

두 축으로 본다 — **만족도**, 그리고 **도달까지의 사용자 노력**.
D가 B와 만족도에서 대등하되 노력이 유의하게 적으면 성공으로 본다.

### 쌍 선택 알고리즘 비교 (발표의 핵심 그래프)

| 방법 | 예상 |
|---|---|
| 무작위 | 중복 질문 다발, 수렴 느림 |
| 축 순차 질의 | 질문 수는 적으나 축 간 상호작용 미포착 |
| 불확실도 기반 | 목표 방식 |

### 확장성 검증

11주차에 더미 도메인 하나(예: 이메일 초안, 축 3개)를 YAML만 작성해
붙인다. `engine/` 코드 수정 없이 동작하면 확장성 증명 완료.

---

## 범위 축소선

**최소 달성 (반드시)**
- 도메인 1종, 축 3개
- 선택 루프 동작, 평가 함수 자동 생성
- 페르소나 기반 복원율·수렴 곡선 측정
- 비교군 A·B 대비 결과

**여유 시**
- 알고리즘 3종 비교
- 비교군 C 추가
- 실사용자 검증
- 최적화기 교체 검증 (GEPA → MIPROv2)
- 2번째 도메인 이식
- Streamlit Community Cloud 배포

---

## 주의사항

- 8주차 중간 점검에서 진척이 부족하면 축소선을 강제 적용한다.
- 배포보다 실험 결과가 우선이다. 실험이 안 끝났으면 배포는 버린다.
- 공개 배포 시 API 키 노출 주의 — 사용자가 자기 키를 넣게 하거나 호출 상한을 건다.

---

## 참고

- MACSum: Controllable Summarization with Mixed Attributes (Zhang et al., TACL 2023)
- GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning (Agrawal et al., ICLR 2026)
- DSPy 자동 프롬프트 최적화 (MIPROv2, GEPA)

---

# 갱신 (v2) — 다음 단계 계획

> 이 블록은 기존 CLAUDE.md 하단에 이어붙인 것이다.
> v1(엔드투엔드 파이프라인 + 3종 알고리즘 비교 + 확장성 검증)은 완료 상태이며,
> 아래는 그 위에 얹을 다음 단계다.

## 프로젝트 정체성 (갱신)

기존 서술에 더해, 최종 형태는 다음과 같이 정의한다.

> 사용자의 비교 선택만으로 개인 맞춤 프롬프트를 생성하는 시스템.
> 새 도메인은 **에이전트가 스스로 축을 발견하고 검증해서** 온보딩하며,
> 판정 계층은 **코드 · 소형 모델 · 대형 모델을 비용 기준으로 라우팅**한다.

기술 계보를 명시할 것:
- 반성적 프롬프트 진화 (GEPA, ICLR 2026)
- Bradley-Terry 선호 학습
- 능동 학습 기반 질의 선택
- 자동 평가 함수 생성 (LLM-as-judge 계열)
- 하이브리드 모델 라우팅 ← 신규
- 자율 축 발견 에이전트 ← 신규

---

## 작업 우선순위

| 순위 | 항목 | 예상 기간 | 목적 |
|---|---|---|---|
| 1 | 독립 채점자 도입 | 며칠 | **기존 결과의 신뢰도 확보 (최우선)** |
| 2 | 한국어 checks 이식 | 3~4일 | 언어 이식성 실증, 데모 효과 |
| 3 | 하이브리드 판정 계층 | 1~2주 | 코드로 못 재는 축 복원, 비용 곡선 |
| 4 | 도메인 온보딩 에이전트 | 3~4주 | 프로젝트를 에이전트 계열로 확장 |

**1번을 반드시 먼저 한다.** 새 기능보다 기존 결과의 방어가 우선이다.

---

## 1. 독립 채점자 (최우선)

### 문제

현재 비교군 D의 프롬프트는 `checks/`로 조립한 metric에 대해 GEPA로
최적화되고, 그 결과를 다시 같은 `checks/`로 채점한다. 자기가 최적화한
함수로 자기를 채점하는 구조이므로 **순환 논증으로 지적당할 수 있다.**
A·B는 그 최적화를 받지 않았으므로 D가 이기는 것이 부분적으로 보장되어 있다.

### 대응

최적화에 사용되지 않은 **독립 지표**를 최소 하나 추가한다.

- MACSum의 사람 작성 정답 요약과의 직접 비교, 또는
- 페르소나 속성 설명만 제시하고 LLM에게 채점시키는 방식

`experiments/compare_baselines`에 독립 지표 열을 추가하고, 기존 지표와
나란히 보고한다.

### 규칙

- **최적화에 쓴 함수로만 최종 결과를 보고하지 않는다.**
- 독립 지표에서 점수가 낮아져도 그대로 보고한다. 0.996보다 0.85가
  더 믿을 만한 결과다.

**구현 완료** (`experiments/independent_grader.py`): MACSum 자체 ROUGE는
Perl+pyrouge가 필요해 Windows에서 설치 부담이 커서, 대신 `rouge-score`
(Google의 순수 파이썬 구현, HuggingFace 등에서 표준으로 쓰임)로 ROUGE-L을
계산한다. `checks/`의 bigram Dice와는 다른 알고리즘(LCS 기반)이고, 비교
대상도 원문이 아니라 MACSum 사람 작성 정답 요약이라 최적화 루프와 완전히
분리되어 있다. `compare_baselines.py`가 `checks_score`와
`independent_score`를 나란히 보고하도록 갱신했다 - 결과는 아래 참고.

---

## 2. 한국어 이식

### 방침

**주 실험은 영어 MACSum으로 유지한다.** 한국어에는 사람 주석 답안지가
없으므로 페르소나 오라클을 세울 수 없고, 3개월 안에 데이터셋을 만들 수 없다.
한국어는 **언어 이식성 검증 및 데모 용도**로만 쓴다.

### 구성

```
domains/summarization_ko.yaml
checks/summarization_ko.py
```

`engine/`은 한 줄도 고치지 않는다. 이것이 검증 대상이다.

### 한국어에서 실제로 고쳐야 하는 것

- **어휘 겹침(extractiveness)**: 한국어는 조사가 붙어 "정부는/정부가/정부의"가
  전부 다른 토큰이 된다. 표면형 비교는 겹침을 과소평가한다.
  형태소 분석기(Kiwi 권장, Mecab 대안)로 어간만 추출해 비교한다.
- **길이(length)**: 어절 수는 영어 단어 수와 정보 밀도가 다르므로
  문장 수 기준을 우선하고, 어절 기준을 쓸 경우 임계값을 재조정한다.
- **출력 언어 고정**: 기존에 겪은 출력 언어 혼재 문제가 재발하지 않도록
  프롬프트에 출력 언어를 명시 고정하는 처리를 유지한다.

### 검증 범위 (정직하게 제한)

한국어에서는 **복원율 등 정량 수치를 산출하지 않는다.** 답안지가 없기 때문이다.

- 파이프라인 완주 여부 (기능 검증)
- 팀원 3인이 각자 8회 선택 후 산출 프롬프트의 결과물이 자기 취향에 맞는지 (정성)
- 축별 검사 함수가 한국어에서 의도대로 값을 매기는지 스팟 체크 20건

보고 시 "정량 실험은 영어, 언어 이식성은 한국어로 확인"이라고 명시한다.

**구현 완료** (`domains/summarization_ko.yaml`, `checks/summarization_ko.py`,
`test_korean_extensibility.py`): 형태소 분석기는 Kiwi(`kiwipiepy`)를 썼다.
`checks/summarization_ko.py`의 `lexical_overlap`은 명사·동사·형용사·부사·
어근 형태소만 뽑아 연속 2개 겹침 비율을 잰다 (조사·어미는 버림). 스팟
체크 결과 short+fully 조합은 원문과 형태소 겹침 94%, long+normal 조합은
40%로 명확히 갈렸다. 6회 선택 루프로 가상 선호(short+fully)도 정확히
학습했고, 출력이 한국어로 안정적으로 고정됨을 확인했다(8주차에 겪은
언어 혼재 버그 재발 없음). `engine/` 코드는 한 줄도 안 고쳤다.

---

## 3. 하이브리드 판정 계층

### 배경

현재 검사는 전부 코드다. 이 원칙 자체는 유지한다(싸고 빠르고 재현 가능).
그러나 어조·관점·격식처럼 코드로 잴 수 없는 축은 다룰 수 없고,
specificity 축을 제외한 것도 결국 이 한계 때문이었다.

### 구조

```
축 판정 요청
  ├─ 코드로 잴 수 있는 축      → checks/ 함수      (무료, 즉시)
  ├─ 애매한 축                → 소형 모델 판정     (저렴)
  └─ 소형 모델이 확신 없을 때  → 대형 모델 판정     (비쌈, 드물게)
```

### 왜 필요한가

GEPA는 metric을 수백 회 호출한다. 전부 대형 모델로 판정하면 비용이
폭발하고, 전부 코드로만 하면 다룰 수 있는 축이 제한된다.

### 측정할 것

- 판정 정확도 대비 총비용 곡선 (코드 전용 / 소형 전용 / 하이브리드 / 대형 전용)
- 라우팅 임계값에 따른 대형 모델 호출 비율
- 지연 시간

### 부수 목표

**코드로 못 재서 제외했던 축을 하이브리드 판정으로 복원한다.**
성공하면 "제외했던 축의 복원"이 그대로 성과 서술이 된다.

### 규칙

- 라우터 자체가 무거우면 절감분이 사라진다. 판정용 소형 모델은
  제약 디코딩 등으로 출력을 최소화한다.
- 오라우팅 비용은 비대칭이다(쉬운 것을 크게 보내면 비용만 낭비,
  어려운 것을 작게 보내면 판정이 틀린다). 임계값은 보수적으로 시작한다.

### 구현 완료 - 결과: 부수 목표(축 복원)는 실패, 라우팅 자체의 교훈은 확보

**구성**: `domains/summarization_hybrid.yaml`(engine/domain_loader.py에 추가한
`extends:` 상속 메커니즘으로 summarization.yaml에서 length/extractiveness/
topic을 그대로 가져오고 specificity만 추가) + `checks/summarization_hybrid.py`
(계층 2: TF-IDF+로지스틱회귀 분류기, 계층 3/4: 소형/대형 LLM - 자기보고
확신도 대신 단일 토큰 출력의 logprob을 소프트맥스한 값을 확신도로 씀).

**정답 데이터**: MACSum의 실제 specificity 라벨 5,379건(normal 4670 / high
709, `experiments/specificity_dataset.py`)으로 진짜 정확도를 쟀다 - 프록시가
아니다.

**결과 (균형정확도, 클래스 불균형 보정)**:

| 계층 | 요약문만 | 원문도 같이 줬을 때 |
|---|---|---|
| 학습형 분류기 | 0.500 (우연) | (원문 안 씀) |
| 소형 LLM | 0.525 | 0.558 |
| 대형 LLM | 0.464 (우연보다 낮음) | 0.514 |

부수 목표였던 "specificity 축 복원"은 **실패로 결론**낸다 - 원문을 같이
줘도 균형정확도가 0.56을 못 넘는다. 정규식(6주차)·spaCy NER(6주차)·학습형
분류기·소형 LLM·대형 LLM까지 다섯 가지 방법이 전부 실패했다는 것 자체가,
MACSum의 specificity 라벨이 표면적 신호로는 원문을 줘도 복원하기 어려운
성질(주석자의 주관적 판단?)을 담고 있다는 증거로 보인다. 6주차의 축 제외
결정이 사후적으로 더 강하게 뒷받침된다.

**라우팅 임계값 스윕은 독자적인 결과가 있다** (`experiments/hybrid_routing_eval.py`,
`experiments/results/hybrid_routing_curve.png`): 확신도 임계값 0.85에서
균형정확도가 최고점(0.558)을 찍고, 임계값을 1.0으로 올려 대형 모델에
전부 에스컬레이션하면 오히려 0.514로 **떨어지면서 비용은 5배**가 된다.
"더 비싼 모델이 항상 더 정확한 건 아니다"는 이 축 하나에 한정된 결과지만
방법론적으로는 재사용 가능한 교훈이다.

---

## 4. 도메인 온보딩 에이전트

### 문제

새 도메인 추가 시 `domains/*.yaml`과 `checks/*.py`를 사람이 손으로 쓴다.
어떤 축을 넣을지, 그 축이 실제로 판별력이 있는지를 사람이 판단해야 한다.

**이 어려움은 이미 실증되었다.** specificity 축을 정규식으로도 spaCy NER로도
측정해봤으나 MACSum에서 판별력이 없어 결국 제외했고, 그 판단에 상당한
시간이 들었다. 새 도메인마다 이 과정이 반복된다.

### 에이전트가 수행할 루프

1. 사용자가 제공한 원문·결과물 예시를 읽고 **갈릴 만한 축 후보를 제안**
2. 각 축에 대해 **검사 함수 코드를 작성**
3. 그 함수를 **실제로 실행**해 데이터에서 판별력이 있는지 측정
4. 판별력이 없으면 **버리거나 측정 방식을 바꿔 재시도**
5. 살아남은 축으로 `domains/*.yaml` 및 `checks/*.py` 생성

### 이것이 에이전트인 이유 (기획안·발표에 명시)

3번 결과에 따라 4번 행동이 달라지고, 반복 횟수를 사전에 정할 수 없다.
플로우차트로 미리 그릴 수 없으므로 워크플로우가 아니라 에이전트다.
또한 도구를 실제로 사용한다(코드 실행, 데이터 조회).

### 평가 설계 (정답이 존재함)

요약 도메인에서 사람이 확정한 현행 축 구성이 답안지다.
에이전트에게 MACSum을 주고 축을 찾게 했을 때:

- specificity를 스스로 판별력 없음으로 판정해 버리는가
- length·extractiveness를 찾아내는가
- 사람이 만든 검사 함수와 비교해 판정 일치율이 얼마인가

재사용 테스트 케이스: 한국어 도메인의 축을 스스로 찾아내는가.

### 리스크와 범위 제한

- 에이전트가 생성한 코드를 **실행하기 전에 검증**한다(구문, 반환 범위,
  예외 처리). 무검증 실행 금지.
- 축 후보 수와 재시도 횟수에 상한을 건다. 무한 루프 방지.
- 이 모듈은 **기존 파이프라인과 분리**한다. 에이전트가 실패해도
  v1 결과는 영향받지 않아야 한다.

---

## 추가된 절대 규칙

7. **최적화에 사용한 지표만으로 최종 성능을 보고하지 않는다.**
   독립 지표를 병기한다.

8. **한국어에서는 정량 복원율을 산출하지 않는다.** 답안지가 없기 때문이다.
   기능 검증과 정성 확인으로 범위를 제한하고, 그 사실을 보고에 명시한다.

9. **에이전트가 생성한 코드는 검증 후에만 실행한다.**

10. **신규 모듈은 기존 파이프라인과 분리한다.** 신규 기능의 실패가
    확보된 결과를 훼손해서는 안 된다.

---

## 발표 서술 방향

강조할 것:
- 실험 과정에서 발견하고 고친 함정들(페르소나 오라클 재설계로
  완전복원율 0~8% → 80%). 실제로 실험을 굴려본 팀만 쓸 수 있는 내용이다.
- 도메인·언어 양쪽에 대한 이식성 실증.
- 무엇을 가져다 쓰고 무엇을 직접 만들었는지의 명시적 구분.

피할 것:
- 산출물이 "범용적으로 더 좋은 프롬프트"라는 주장.
  목표는 개인 적합도이며, 타인에게 맞지 않는 것이 정상이다.
- 순환 논증으로 보이는 단일 지표 보고.
