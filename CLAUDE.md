# 선호 기반 프롬프트 자동 생성 도구

## 프로젝트 목표

사용자가 결과물 두 개 중 마음에 드는 쪽을 8~10회 고르면, 그 선택 이력에서
사용자의 암묵적 선호를 추정하고, 이를 평가 함수로 변환해 자동 프롬프트
최적화기(GEPA)에 전달한다. 최종 산출물은 그 사용자 전용 시스템 프롬프트
텍스트 한 덩어리다.

핵심 전환: 기존 개인화 수단은 "당신이 원하는 것을 서술하세요"에서 출발한다.
본 도구는 "이 둘 중 나은 쪽을 고르세요"에서 출발한다.

**이 프로젝트가 하지 않는 것**
- 언어 모델을 학습하거나 파인튜닝하지 않는다. 상용 API를 부품으로 호출만 한다.
- "범용적으로 좋은 프롬프트"를 목표로 하지 않는다. 개인 적합도가 목표다.
- 산출물이 모든 사람에게 잘 맞으면 개인화에 실패한 것이다.

## 팀 및 제약

- 3인, 3개월, 타 프로젝트 병행 (1인당 주 10시간 가정)
- 실제 개발 가능 기간 6~8주로 간주하고 범위를 잡는다
- 완성도의 기준은 "동작하는 도구"가 아니라 "측정된 결과"다

---

## 절대 규칙

1. **`engine/` 안의 코드에 도메인 특정 단어가 등장해서는 안 된다.**
   "요약", "문장 수", "추출성" 같은 단어는 금지. 축 이름, 축 값, 프롬프트
   문구, 검사 함수 지정은 전부 `domains/*.yaml`에서 읽는다. 엔진은 "축이 N개
   있고 각 축에 값이 M개 있다"까지만 안다.
   → 이 규칙이 확장성 주장의 실체다. 어기면 도메인 추가 시 엔진을 다시 짜야 한다.

2. **API 호출 결과는 반드시 캐싱한다.** 같은 (원문, 축조합) 쌍은 두 번
   호출하지 않는다. 실험을 수백 회 돌리므로 캐싱 없이는 비용이 폭발한다.

3. **새 라이브러리를 추가하기 전에 먼저 물어본다.** 의존성을 늘리지 않는다.

4. **API 키를 코드에 하드코딩하지 않는다.** `.env`에서만 읽는다.
   `.env`는 절대 커밋하지 않는다.

5. **한 번에 파일 하나 또는 함수 하나 단위로 작업한다.** 전체를 한꺼번에
   생성하지 않는다.

6. **평가 지표는 가능한 한 코드로 직접 잰다.** LLM 판정은 최후 수단.
   싸고 빠르고 재현 가능한 쪽을 우선한다.

---

## 스택

```
Python 3.12   (3.13/3.14 금지 — 패키지 호환성 문제)
├─ dspy         최적화 프레임워크
├─ gepa         프롬프트 최적화기
├─ litellm      여러 모델 API 통합 인터페이스
├─ streamlit    UI (로컬 실행)
├─ pandas       실험 집계
├─ matplotlib   그래프
├─ pyyaml       도메인 정의 로드
├─ python-dotenv
└─ pytest       검사 함수 테스트
```

- DB는 SQLite 또는 JSON 파일. 서버형 DB를 쓰지 않는다.
- 배포는 프로젝트 후반의 선택 항목. 초기에는 전부 로컬.
- 후보 생성은 저렴한 소형 모델, GEPA 성찰 모델만 강한 모델을 쓴다.

---

## 디렉토리 구조

```
preference-prompt-tool/
├─ CLAUDE.md
├─ .env                        (커밋 금지)
├─ .gitignore
├─ requirements.txt
├─ domains/
│   └─ summarization.yaml      축 정의 · 프롬프트 문구 · 검사함수 이름
├─ engine/                     ← 도메인을 모르는 코어 (★ 팀의 핵심 기여)
│   ├─ domain_loader.py        YAML 로드 및 검증
│   ├─ generator.py            축조합 → 프롬프트 조립 → API 호출 → 캐싱
│   ├─ selector.py           ★ 다음에 보여줄 쌍 선택 알고리즘
│   ├─ estimator.py          ★ 선택 이력 → 축별 선호 + 확신도 추정
│   └─ metric_builder.py     ★ 선호 → GEPA용 평가 함수 조립
├─ checks/
│   └─ summarization.py        축별 검사 함수 (도메인 의존)
├─ optimize/
│   └─ run_gepa.py             GEPA 연동
├─ experiments/
│   ├─ persona.py              MACSum 속성조합 기반 선택 대행
│   ├─ run_all.py              실험 실행 및 집계
│   └─ plots.py                수렴 곡선 등 시각화
├─ app.py                      Streamlit UI (마지막에 작성)
├─ data/
│   └─ macsum/                 (커밋 금지)
└─ cache/                      (커밋 금지)
```

★ 표시가 라이브러리로 대체 불가능한 부분이다. 여기에 시간을 쓴다.

---

## 도메인: 문서 요약 (MACSum 기반)

### 데이터셋

MACSum — 혼합 속성 제어 요약을 위한 사람 주석 데이터셋.
출처: https://github.com/psunlpgroup/MACSum
원문은 CNN/DailyMail(뉴스)과 QMSum(대화)에서 수집.

**이 데이터셋을 쓰는 이유**: 축 목록이 이미 학술적으로 정의되어 있고,
같은 원문에 대해 서로 다른 속성 조합으로 사람이 작성한 요약이 들어 있다.
따라서 **속성 조합 하나 = 페르소나 하나**로 쓸 수 있다.

**9주차에 수정**: 원래는 "그 조합의 사람 요약 = 답안지"로 삼아
정답 요약과의 단어 겹침으로 페르소나의 선택을 대행했는데, 실험을
돌려보니 `extractiveness=fully`(원문 그대로 발췌) 후보가 원문 고유명사를
그대로 가져와 정답 요약과 우연히 어휘가 더 겹쳐버려 엉뚱한 축으로
수렴하는 문제가 있었다 (내용 겹침과 문체 겹침을 구분 못 함). 정답
요약 텍스트 대신, 6주차에 검증해둔 `checks/summarization.py`를 페르소나의
실제 축값에 대고 두 후보에 돌려 점수가 더 높은 쪽을 고르는 방식
(`experiments/persona.py`)으로 바꿨다. 수정 전엔 5문서 평균 완전복원율이
0~8%였는데, 수정 후 순차/불확실도 알고리즘 모두 80%로 올라갔다.

### 사용할 축 (3개, 6주차에 4개→3개로 축소)

| 축 | 값 | 검사 방식 |
|---|---|---|
| length | short / normal / long | 코드 (문장 수, MACSum 실측 분포로 보정) |
| extractiveness | normal / high / fully | 코드 (원문과의 연속 2단어(bigram) 겹침 비율) |
| topic | 키워드 지정 | 코드 (키워드 포함 여부) |

`speaker` 축은 대화 전용이므로 제외한다 (후속 연구들의 관례).

`specificity`(숫자·고유명사 밀도) 축은 6주차에 범위에서 제외했다.
정규식 휴리스틱은 물론 spaCy NER로 측정해도 normal(0.217)과 high(0.228)
사이에 실질적 차이가 없었다 (val split 실측). MACSum의 specificity
라벨은 표면적 수치·고유명사 밀도가 아닌 다른 성질을 반영하는 것으로
보이고, 100% topic 축과 동반 출현해 독립 축으로 보기도 어렵다. 코드로
재현 가능하게 잴 수 없는 축은 넣지 않는다 (절대 규칙 6).

**8주차에 발견한 중대 버그(수정 완료)**: `task_description`이 한국어라
출력 언어가 축조합에 따라 들쭉날쭉했다 (extractiveness=fully일 땐 원문을
그대로 베껴 영어로, 그 외엔 한국어로). MACSum 원문·정답 요약이 전부
영어라서 lexical_overlap 체크, keyword_presence 체크, persona.choose
전부가 언어 불일치로 왜곡된 값을 내고 있었다. task_description에 "요약은
반드시 영어로 작성하라"를 명시해 고쳤다. 같은 맥락에서 `generator.py`의
캐시 키도 (도메인명, 축조합)이 아니라 조립된 프롬프트 텍스트 자체로
바꿨다 - 안 그러면 YAML 문구를 고쳐도 캐시가 무효화되지 않는다.
`experiments/persona.py`의 유사도 지표도 자카드 대신 Dice(F1)로 교체했다
(3주차에 발견한 길이 편향 수정). 이 세 수정을 다 적용한 뒤에야 실제 API
호출로 도는 선택 루프 전체가 숨긴 페르소나 선호를 복원하기 시작했다
(tests/test_closed_loop.py).

**이 문장은 2026-09-16에 실측으로 정정했다.** 원래는 "정확히 복원하는 걸
확인했다"라고 적어뒀는데, 그 근거 파일이 assert 없는 출력 스크립트라
아무도 회귀를 못 보고 있었다. 테스트로 바꾸면서 문서 3개 x 페르소나
3개(자유 키워드 축 제외) = 9케이스를 8라운드씩 실제로 돌려 쟀다.

| 지표 | 실측 |
|---|---|
| 완전복원 | 5/9 (56%) |
| 축 단위 정답 | 12/18 (67%) |

축별로 갈린다. `length=short` 선호는 문서 3개에서 3번 다 정확히 복원되고,
`length=long` 선호는 3번 중 2번 `normal`로 수렴한다. 8라운드 안에서는
인접한 값(normal/long)을 가르기 어렵다는 뜻이다. 그래서 테스트는 "모든
페르소나를 정확히 복원한다"를 박지 않고, 안정적으로 복원되는 케이스의
회귀와 어려운 케이스의 완주만 잡는다.

README 3절이 보고하는 **완전복원 80%**는 이것과 다른 실험이다
(`experiments/run_all.py`, 5문서 x 3알고리즘 x 5시드, 750행이
`experiments/results/results.csv`에 커밋돼 있다). 그 수치는 커밋된
데이터에서 다시 계산해 일치함을 확인했고, `tests/test_reported_results.py`
가 문서와 데이터가 어긋나면 실패한다.

---

## GEPA 연동 방식

GEPA에 넘기는 것은 세 가지다.

| GEPA 인자 | 우리 쪽 대응 |
|---|---|
| `seed_candidate` | 추정된 축 선호로 조립한 초기 시스템 프롬프트 |
| `trainset` / `valset` | MACSum 원문 수십 개 |
| `metric` | **`engine/metric_builder.py`가 조립한 평가 함수** ← 우리 기여 |

```python
result = gepa.optimize(
    seed_candidate={"system_prompt": seed},
    trainset=train, valset=val,
    task_lm="<저렴한 모델>",        # 요약 생성
    reflection_lm="<강한 모델>",     # 성찰
    max_metric_calls=150,           # 비용 상한
)
optimized = result.best_candidate["system_prompt"]
```

**중요한 설계 포인트**: GEPA는 점수뿐 아니라 자연어 피드백도 받는다.
평가 함수가 축별 위반 내역을 텍스트로 반환하면 GEPA가 이를 성찰에 사용해
적은 시도로 좋은 프롬프트를 찾는다. 우리는 축을 명시적으로 갖고 있으므로
이것이 가능하다. 범용 최적화 도구는 못 하는 부분이며, 프로젝트의 기여로
명시한다.

```python
# metric 반환 형태
return 0.6, "길이 위반: 6문장(목표 3문장 이하). 원문 수치 3개 중 1개 누락."
```

비용 관리를 위해 초기에는 `auto="light"` 수준으로 시작한다.

---

## 개발 순서

| 단계 | 주차 | 내용 |
|---|---|---|
| 0 | 1 | 환경 구성, MACSum 로드, 같은 원문의 속성별 요약 비교 출력 |
| 0 | 1 | 팀원 3인 각자 GEPA 튜토리얼 1회 완주 |
| 1 | 2 | `domains/summarization.yaml` 작성 |
| 2 | 3 | `generator.py` — 축조합 → 결과물 생성 + 캐싱 |
| 3 | 3 | `persona.py` — MACSum 정답 요약 기반 선택 대행 |
| 4 | 4–5 | ★ `selector.py`, `estimator.py` (무작위·순차·불확실도 3종 구현) |
| 5 | 6 | ★ `metric_builder.py` + `checks/` |
| 6 | 7 | `run_gepa.py` 연동, 엔드투엔드 1회 관통 |
| — | 8 | **중간 점검 — 범위 축소선 판단** |
| 7 | 9–10 | 주 실험 실행, 알고리즘 3종 수렴 곡선 비교 |
| 8 | 11 | Streamlit UI (얇게), 실사용자 3~5인 검증, 확장성 검증 |
| 9 | 12 | 결과 정리, 발표 준비 |

**UI는 반드시 마지막에 만든다.** 먼저 만들면 시간을 전부 잡아먹는다.

---

## 평가 계획

### 측정 지표

| 지표 | 정의 |
|---|---|
| 기준 복원율 | 숨긴 축별 선호 중 정확히 맞힌 비율 |
| 수렴 질문 수 | 복원율이 목표치에 도달하기까지의 선택 횟수 |
| 평가 함수 일치율 | 생성된 평가 함수와 페르소나 기준의 판정 일치 비율 |
| 최종 만족도 | 산출 프롬프트 결과물에 대한 페르소나 채점 |

### 비교군

| 조건 | 설명 |
|---|---|
| A | 프롬프트 없이 기본 호출 |
| B | 사용자가 커스텀 인스트럭션을 직접 작성 |
| C | 기존 상용 프롬프트 생성 도구의 출력 |
| D | 본 도구 |

두 축으로 본다 — **만족도**, 그리고 **도달까지의 사용자 노력**.
D가 B와 만족도에서 대등하되 노력이 유의하게 적으면 성공으로 본다.

### 쌍 선택 알고리즘 비교 (발표의 핵심 그래프)

| 방법 | 예상 |
|---|---|
| 무작위 | 중복 질문 다발, 수렴 느림 |
| 축 순차 질의 | 질문 수는 적으나 축 간 상호작용 미포착 |
| 불확실도 기반 | 목표 방식 |

### 확장성 검증

11주차에 더미 도메인 하나(예: 이메일 초안, 축 3개)를 YAML만 작성해
붙인다. `engine/` 코드 수정 없이 동작하면 확장성 증명 완료.

---

## 범위 축소선

**최소 달성 (반드시)**
- 도메인 1종, 축 3개
- 선택 루프 동작, 평가 함수 자동 생성
- 페르소나 기반 복원율·수렴 곡선 측정
- 비교군 A·B 대비 결과

**여유 시**
- 알고리즘 3종 비교
- 비교군 C 추가
- 실사용자 검증
- 최적화기 교체 검증 (GEPA → MIPROv2)
- 2번째 도메인 이식
- Streamlit Community Cloud 배포

---

## 주의사항

- 8주차 중간 점검에서 진척이 부족하면 축소선을 강제 적용한다.
- 배포보다 실험 결과가 우선이다. 실험이 안 끝났으면 배포는 버린다.
- 공개 배포 시 API 키 노출 주의 — 사용자가 자기 키를 넣게 하거나 호출 상한을 건다.

---

## 참고

- MACSum: Controllable Summarization with Mixed Attributes (Zhang et al., TACL 2023)
- GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning (Agrawal et al., ICLR 2026)
- DSPy 자동 프롬프트 최적화 (MIPROv2, GEPA)

---

# 갱신 (v2) — 다음 단계 계획

> 이 블록은 기존 CLAUDE.md 하단에 이어붙인 것이다.
> v1(엔드투엔드 파이프라인 + 3종 알고리즘 비교 + 확장성 검증)은 완료 상태이며,
> 아래는 그 위에 얹을 다음 단계다.

## 프로젝트 정체성 (갱신)

기존 서술에 더해, 최종 형태는 다음과 같이 정의한다.

> 사용자의 비교 선택만으로 개인 맞춤 프롬프트를 생성하는 시스템.
> 새 도메인은 **에이전트가 스스로 축을 발견하고 검증해서** 온보딩하며,
> 판정 계층은 **코드 · 소형 모델 · 대형 모델을 비용 기준으로 라우팅**한다.

기술 계보를 명시할 것:
- 반성적 프롬프트 진화 (GEPA, ICLR 2026)
- Bradley-Terry 선호 학습
- 능동 학습 기반 질의 선택
- 자동 평가 함수 생성 (LLM-as-judge 계열)
- 하이브리드 모델 라우팅 ← 신규
- 자율 축 발견 에이전트 ← 신규

---

## 작업 우선순위

| 순위 | 항목 | 예상 기간 | 목적 |
|---|---|---|---|
| 1 | 독립 채점자 도입 | 며칠 | **기존 결과의 신뢰도 확보 (최우선)** |
| 2 | 한국어 checks 이식 | 3~4일 | 언어 이식성 실증, 데모 효과 |
| 3 | 하이브리드 판정 계층 | 1~2주 | 코드로 못 재는 축 복원, 비용 곡선 |
| 4 | 도메인 온보딩 에이전트 | 3~4주 | 프로젝트를 에이전트 계열로 확장 |

**1번을 반드시 먼저 한다.** 새 기능보다 기존 결과의 방어가 우선이다.

---

## 1. 독립 채점자 (최우선)

### 문제

현재 비교군 D의 프롬프트는 `checks/`로 조립한 metric에 대해 GEPA로
최적화되고, 그 결과를 다시 같은 `checks/`로 채점한다. 자기가 최적화한
함수로 자기를 채점하는 구조이므로 **순환 논증으로 지적당할 수 있다.**
A·B는 그 최적화를 받지 않았으므로 D가 이기는 것이 부분적으로 보장되어 있다.

### 대응

최적화에 사용되지 않은 **독립 지표**를 최소 하나 추가한다.

- MACSum의 사람 작성 정답 요약과의 직접 비교, 또는
- 페르소나 속성 설명만 제시하고 LLM에게 채점시키는 방식

`experiments/compare_baselines`에 독립 지표 열을 추가하고, 기존 지표와
나란히 보고한다.

### 규칙

- **최적화에 쓴 함수로만 최종 결과를 보고하지 않는다.**
- 독립 지표에서 점수가 낮아져도 그대로 보고한다. 0.996보다 0.85가
  더 믿을 만한 결과다.

**구현 완료** (`experiments/independent_grader.py`): MACSum 자체 ROUGE는
Perl+pyrouge가 필요해 Windows에서 설치 부담이 커서, 대신 `rouge-score`
(Google의 순수 파이썬 구현, HuggingFace 등에서 표준으로 쓰임)로 ROUGE-L을
계산한다. `checks/`의 bigram Dice와는 다른 알고리즘(LCS 기반)이고, 비교
대상도 원문이 아니라 MACSum 사람 작성 정답 요약이라 최적화 루프와 완전히
분리되어 있다. `compare_baselines.py`가 `checks_score`와
`independent_score`를 나란히 보고하도록 갱신했다 - 결과는 아래 참고.

---

## 2. 한국어 이식

### 방침

**주 실험은 영어 MACSum으로 유지한다.** 한국어에는 사람 주석 답안지가
없으므로 페르소나 오라클을 세울 수 없고, 3개월 안에 데이터셋을 만들 수 없다.
한국어는 **언어 이식성 검증 및 데모 용도**로만 쓴다.

### 구성

```
domains/summarization_ko.yaml
checks/summarization_ko.py
```

`engine/`은 한 줄도 고치지 않는다. 이것이 검증 대상이다.

### 한국어에서 실제로 고쳐야 하는 것

- **어휘 겹침(extractiveness)**: 한국어는 조사가 붙어 "정부는/정부가/정부의"가
  전부 다른 토큰이 된다. 표면형 비교는 겹침을 과소평가한다.
  형태소 분석기(Kiwi 권장, Mecab 대안)로 어간만 추출해 비교한다.
- **길이(length)**: 어절 수는 영어 단어 수와 정보 밀도가 다르므로
  문장 수 기준을 우선하고, 어절 기준을 쓸 경우 임계값을 재조정한다.
- **출력 언어 고정**: 기존에 겪은 출력 언어 혼재 문제가 재발하지 않도록
  프롬프트에 출력 언어를 명시 고정하는 처리를 유지한다.

### 검증 범위 (정직하게 제한)

한국어에서는 **복원율 등 정량 수치를 산출하지 않는다.** 답안지가 없기 때문이다.

- 파이프라인 완주 여부 (기능 검증)
- 팀원 3인이 각자 8회 선택 후 산출 프롬프트의 결과물이 자기 취향에 맞는지 (정성)
- 축별 검사 함수가 한국어에서 의도대로 값을 매기는지 스팟 체크 20건

보고 시 "정량 실험은 영어, 언어 이식성은 한국어로 확인"이라고 명시한다.

**구현 완료** (`domains/summarization_ko.yaml`, `checks/summarization_ko.py`,
`tests/test_korean_extensibility.py`): 형태소 분석기는 Kiwi(`kiwipiepy`)를 썼다.
`checks/summarization_ko.py`의 `lexical_overlap`은 명사·동사·형용사·부사·
어근 형태소만 뽑아 연속 2개 겹침 비율을 잰다 (조사·어미는 버림). 스팟
체크 결과 short+fully 조합은 원문과 형태소 겹침 94%, long+normal 조합은
40%로 명확히 갈렸다. 6회 선택 루프로 가상 선호(short+fully)도 정확히
학습했고, 출력이 한국어로 안정적으로 고정됨을 확인했다(8주차에 겪은
언어 혼재 버그 재발 없음). `engine/` 코드는 한 줄도 안 고쳤다.

**앱 연결 (배포 대응 포함)**: 한국어 도메인이 v2 당시엔 실험·테스트에만
쓰이고 `app.py`에는 안 붙어 있었는데, 웹 UI에 "문서 요약 (한국어)"
카테고리로 연결했다(영어는 "문서 요약 (영어)"로 유지). 붙이면서 두 가지가
걸렸다.

1. `engine/demo_generator.py`가 `demos.<도메인명>`을 이름으로 찾으므로
   데모 모드가 ValueError를 낸다 → `demos/summarization_ko.py` 추가.
   영어 데모는 extractiveness=normal에서 키워드를 나열하는데 한국어로는
   요약으로 안 읽혀서, 서술어를 명사형으로 바꾸는 개조식 압축으로 만들었다
   (발표했다 → 발표). 변환 목록에 없는 서술어는 문장을 그대로 둬서 비문이
   안 생긴다.
2. `checks/summarization_ko.py`가 `kiwipiepy`를 모듈 최상단에서 import하고
   `Kiwi()`를 즉시 생성한다. 그런데 `kiwipiepy_model`은 88MB이고 소스
   tarball로만 배포돼서, 배포용 requirements.txt에 넣으면 Streamlit
   Community Cloud 무료 티어에서 빌드 실패나 OOM 위험이 있다 - **제출
   링크가 죽으면 실격**이라 감수할 수 없는 리스크로 판단했다. Kiwi를
   지연 로드로 바꾸고, 사전이 필요 없는 **문자 4-gram 겹침** fallback을
   뒀다.

   처음엔 어절 끝 조사를 규칙으로 떼는 방식을 썼는데 실패했다 -
   "전문가"의 "가"를 주격 조사로 오인해 중간 대역 겹침이 0.59 → 0.47로
   내려가 임계값 0.5를 잘못 넘었다. 한국어는 조사가 어절 뒤에 붙으므로
   문자 n-gram에는 그 문제가 없다. n은 실측으로 골랐다 (원문 3종 x 발췌
   문장수 4단계 x 어절 누락률 6단계 = 72케이스, 임계값 판정 일치율):
   n=2 57%, n=3 67%, **n=4 97%**, n=5 82%, n=6 74%. n=4는 평균 오차
   0.035, r=0.977, Kiwi 값으로의 회귀 기울기 1.07/절편 -0.07로 거의
   항등이라 보정 상수 없이 같은 YAML 임계값을 공유한다.

   **보고 수치는 전부 Kiwi 기준으로 유지**한다. 실험 환경이 조용히
   fallback으로 내려앉는 사고를 막으려고 `tests/test_korean_checks_backends.py`
   가 kiwipiepy가 깔린 환경에서는 kiwi 백엔드가 쓰이는지 검사한다.

**실측 검증**: 실제 API로 3개 축조합을 돌려 전부 한글 100%, 축값도 목표
대역에 정확히 들어갔다 (short+fully 2문장/겹침 100%, long+normal
5문장/22%, normal+high 3문장/72%, topic 지정 시 키워드 포함). kiwipiepy를
제거한 상태에서 GEPA 최적화(max_metric_calls=20)도 완주했다 - 배포 경로
재현. 배포된 앱을 브라우저로 직접 몰아 데모 모드 8회 완주와 API 모드
한국어 생성(한글 98%)까지 확인했다.

---

## 3. 하이브리드 판정 계층

### 배경

현재 검사는 전부 코드다. 이 원칙 자체는 유지한다(싸고 빠르고 재현 가능).
그러나 어조·관점·격식처럼 코드로 잴 수 없는 축은 다룰 수 없고,
specificity 축을 제외한 것도 결국 이 한계 때문이었다.

### 구조

```
축 판정 요청
  ├─ 코드로 잴 수 있는 축      → checks/ 함수      (무료, 즉시)
  ├─ 애매한 축                → 소형 모델 판정     (저렴)
  └─ 소형 모델이 확신 없을 때  → 대형 모델 판정     (비쌈, 드물게)
```

### 왜 필요한가

GEPA는 metric을 수백 회 호출한다. 전부 대형 모델로 판정하면 비용이
폭발하고, 전부 코드로만 하면 다룰 수 있는 축이 제한된다.

### 측정할 것

- 판정 정확도 대비 총비용 곡선 (코드 전용 / 소형 전용 / 하이브리드 / 대형 전용)
- 라우팅 임계값에 따른 대형 모델 호출 비율
- 지연 시간

### 부수 목표

**코드로 못 재서 제외했던 축을 하이브리드 판정으로 복원한다.**
성공하면 "제외했던 축의 복원"이 그대로 성과 서술이 된다.

### 규칙

- 라우터 자체가 무거우면 절감분이 사라진다. 판정용 소형 모델은
  제약 디코딩 등으로 출력을 최소화한다.
- 오라우팅 비용은 비대칭이다(쉬운 것을 크게 보내면 비용만 낭비,
  어려운 것을 작게 보내면 판정이 틀린다). 임계값은 보수적으로 시작한다.

### 구현 완료 - 결과: 부수 목표(축 복원)는 실패, 라우팅 자체의 교훈은 확보

**구성**: `domains/summarization_hybrid.yaml`(engine/domain_loader.py에 추가한
`extends:` 상속 메커니즘으로 summarization.yaml에서 length/extractiveness/
topic을 그대로 가져오고 specificity만 추가) + `checks/summarization_hybrid.py`
(계층 2: TF-IDF+로지스틱회귀 분류기, 계층 3/4: 소형/대형 LLM - 자기보고
확신도 대신 단일 토큰 출력의 logprob을 소프트맥스한 값을 확신도로 씀).

**정답 데이터**: MACSum의 실제 specificity 라벨 5,379건(normal 4670 / high
709, `experiments/specificity_dataset.py`)으로 진짜 정확도를 쟀다 - 프록시가
아니다.

**결과 (균형정확도, 클래스 불균형 보정)**:

| 계층 | 요약문만 | 원문도 같이 줬을 때 |
|---|---|---|
| 학습형 분류기 | 0.500 (우연) | (원문 안 씀) |
| 소형 LLM | 0.525 | 0.558 |
| 대형 LLM | 0.464 (우연보다 낮음) | 0.514 |

부수 목표였던 "specificity 축 복원"은 **실패로 결론**낸다 - 원문을 같이
줘도 균형정확도가 0.56을 못 넘는다. 정규식(6주차)·spaCy NER(6주차)·학습형
분류기·소형 LLM·대형 LLM까지 다섯 가지 방법이 전부 실패했다는 것 자체가,
MACSum의 specificity 라벨이 표면적 신호로는 원문을 줘도 복원하기 어려운
성질(주석자의 주관적 판단?)을 담고 있다는 증거로 보인다. 6주차의 축 제외
결정이 사후적으로 더 강하게 뒷받침된다.

**라우팅 임계값 스윕은 독자적인 결과가 있다** (`experiments/hybrid_routing_eval.py`,
`experiments/results/hybrid_routing_curve.png`): 확신도 임계값 0.85에서
균형정확도가 최고점(0.558)을 찍고, 임계값을 1.0으로 올려 대형 모델에
전부 에스컬레이션하면 오히려 0.514로 **떨어지면서 비용은 5배**가 된다.
다만 이 곡선은 판별력이 확인되지 않은 축(우연 근방 0.46~0.56)에서 잰
것이라, 0.85라는 최적값 자체보다는 "비싼 모델이 항상 낫지는 않다"는
정성적 관찰로만 해석해야 안전하다.

**대조군으로 검증** (`experiments/hybrid_routing_eval_length.py`,
`experiments/results/hybrid_routing_curve_length.png`): 판별력이 이미
확인된 length(short/long) 축에 완전히 독립적으로 작성한 같은 구조의
스윕을 돌렸다. 결과: 분류기 단독 0.736, 소형 LLM 0.428, 대형 LLM 0.272로
**LLM에 맡길수록 단조롭게 나빠진다** - specificity 곡선의 "중간 피크"와
확연히 다른 모양이라, 그 피크가 노이즈였다는 걸 뒷받침한다. 원인도
확인했다: MACSum이 "short"로 라벨링한 2문장짜리를 대형 LLM은 확신도
1.000으로 "long"이라 판정한다 - 기준점 없이 물으면 LLM이 이 데이터셋
고유의 라벨링 관례 대신 자기 내부 기준을 쓴다. **제로샷 LLM 판정이
싼 학습형 분류기보다 못할 수 있다**는 것도 이 대조 실험에서 나온
독자적인 결론이다.

---

## 4. 도메인 온보딩 에이전트

### 문제

새 도메인 추가 시 `domains/*.yaml`과 `checks/*.py`를 사람이 손으로 쓴다.
어떤 축을 넣을지, 그 축이 실제로 판별력이 있는지를 사람이 판단해야 한다.

**이 어려움은 이미 실증되었다.** specificity 축을 정규식으로도 spaCy NER로도
측정해봤으나 MACSum에서 판별력이 없어 결국 제외했고, 그 판단에 상당한
시간이 들었다. 새 도메인마다 이 과정이 반복된다.

### 에이전트가 수행할 루프

1. 사용자가 제공한 원문·결과물 예시를 읽고 **갈릴 만한 축 후보를 제안**
2. 각 축에 대해 **검사 함수 코드를 작성**
3. 그 함수를 **실제로 실행**해 데이터에서 판별력이 있는지 측정
4. 판별력이 없으면 **버리거나 측정 방식을 바꿔 재시도**
5. 살아남은 축으로 `domains/*.yaml` 및 `checks/*.py` 생성

### 이것이 에이전트인 이유 (기획안·발표에 명시)

3번 결과에 따라 4번 행동이 달라지고, 반복 횟수를 사전에 정할 수 없다.
플로우차트로 미리 그릴 수 없으므로 워크플로우가 아니라 에이전트다.
또한 도구를 실제로 사용한다(코드 실행, 데이터 조회).

### 평가 설계 (정답이 존재함)

요약 도메인에서 사람이 확정한 현행 축 구성이 답안지다.
에이전트에게 MACSum을 주고 축을 찾게 했을 때:

- specificity를 스스로 판별력 없음으로 판정해 버리는가
- length·extractiveness를 찾아내는가
- 사람이 만든 검사 함수와 비교해 판정 일치율이 얼마인가

재사용 테스트 케이스: 한국어 도메인의 축을 스스로 찾아내는가.

### 리스크와 범위 제한

- 에이전트가 생성한 코드를 **실행하기 전에 검증**한다(구문, 반환 범위,
  예외 처리). 무검증 실행 금지.
- 축 후보 수와 재시도 횟수에 상한을 건다. 무한 루프 방지.
- 이 모듈은 **기존 파이프라인과 분리**한다. 에이전트가 실패해도
  v1 결과는 영향받지 않아야 한다.

### 구현 완료 - MVP 수준. 안전한 축 발견 루프는 성공, 사람 축 재발견은 실패 (정량 확인)

**구성** (`agents/` - 기존 `engine/domains/checks`와 완전히 분리된 새 패키지):

- `agents/code_validator.py`: AST 정적 검증 (1차 방어선). import 문 전면
  금지, 던더(`__x__`) 이름·속성 패턴 전체 차단 - 개별 위험 이름을
  나열하는 방식 대신 구조적으로 막아서 `().__class__.__base__.__subclasses__()`
  류의 샌드박스 탈출 경로까지 잡는다. 시그니처(`measure(text, source)`)도 검사.
- `agents/sandbox.py` + `agents/_measure_runner.py`: 프로세스 격리 + 타임아웃
  (2차 방어선 - 실제 방어는 여기서 일어난다). 정상 코드, os 접근, 무한루프,
  `__builtins__` 유출, 클래스 계층 우회, `getattr` 우회 6가지 케이스로
  실제 검증함 - 처음엔 `__builtins__`를 bare name으로 접근하는 우회를
  놓쳤다가 던더 패턴 차단으로 막았다.
- `agents/domain_onboarding.py`: 루프 본체. `propose_axes`(LLM) ->
  `write_measure_code`(LLM) -> `measure_discriminative_power`(코드, 샌드박스
  경유 - corpus 전체에 measure()를 돌려 (p90-p10)/(|mean|+eps)로 판별력을
  잼, 라벨 없이도 되는 지표) -> 임계값 미달 시 실패 사유를 담아 재시도
  (최대 2회) -> `emit_domain`(살아남은 축으로 domains/checks 파일 생성,
  항상 `_agent` 접미사라 기존 파일과 안 겹침).
- `agents/evaluate_domain_onboarding.py`: MACSum 문서를 라벨 없이(원문 +
  서로 다른 결과물 여러 개만) 줘서 축을 발견시키고, 사람이 정한 답안
  (length/extractiveness)과 비교. 문자열 완전일치와 LLM 기반 의미 매칭
  둘 다로 비교한다.

**결과**: 6개 문서(문서당 결과물 5~7개)로 돌렸더니 conciseness, formality,
sentence_complexity, focus_on_entities 4개 축을 제안했고 전부 판별력
기준을 통과해 살아남았다 - specificity류의 판별 불가능한 축을 제안하지도,
채택하지도 않았다. 다만 **length/extractiveness와 문자 그대로도, LLM
의미 판정으로도 일치하지 않았다** - "conciseness"조차 "length"와 완전히
같은 개념은 아니라고 판정됐다(더 엄격한 기준: 간결함은 밀도 문제고 길이는
순수 분량 문제). extractiveness류(원문 대비 그대로 베끼는 정도)는 아예
제안되지도 않았다 - 에이전트가 축을 비교할 때 결과물끼리만 견주고 원문과의
관계는 덜 고려하는 경향으로 보인다.

**"다른 이름의 같은 축"인지 확인 (`agents/correlate_with_human_axes.py`)**:
이름이 달라도 측정값이 사람 축과 강하게 상관되면 기능적으로 같은
축이라고 볼 수 있다는 지적을 받고, 에이전트의 measure_* 함수와 사람이
만든 `checks/summarization.py`의 원시 측정값(문장 수, bigram 겹침)을
같은 텍스트 36개에 돌려 피어슨 상관을 쟀다.

topic은 열거형이 아니라 자유 키워드 축이라 연속값 상관 비교 설계 자체가
성립하지 않아 length/extractiveness 2개만 비교한다.

| 사람 축 \ 에이전트 축 | conciseness | formality | sentence_complexity | focus_on_entities |
|---|---|---|---|---|
| length (문장 수) | 0.121 | -0.453 | 0.062 | -0.088 |
| extractiveness (bigram 겹침) | 0.012 | 0.113 | -0.112 | 0.261 |

2×4=8개 조합 전부, "같은 축"으로 볼 임계값(\|r\| ≥ 0.7)에 크게 못
미친다 (최댓값 0.453). length와
가장 가까운 것도 직관적으로 예상했던 conciseness가 아니라 formality였고
그마저 약한 음의 상관이다 - **명명이 다른 것이 아니라 정말 다른 축을
찾은 것으로 정량 확인됐다.**

**결론**: 에이전트는 "그럴듯하고 실제로 판별력 있는 축을 스스로 찾아
검증하는" 핵심 루프는 실제로 작동한다(안전하게, 반복적으로, 실패시
스스로 재시도/포기하면서 - 이건 사람이 만든 것과 독립적인 성과다). 다만
사람이 고른 축과 같은 것을 재발견하지는 못했다 - 상관분석까지 마친
이상 "다른 이름의 성공"으로 포장하지 않고 "다른 것을 찾았다"로 정직하게
보고한다. 발표에서는 "사람의 답안을 그대로 재현했다"가 아니라 "라벨
없이도 실제로 판별력 있는 새로운 축을 안전하게 찾아내고 나쁜 축은
스스로 걸러낸다"로 서술해야 정직하다. 한국어 도메인 재사용 테스트,
3-4주 분량으로 예정됐던 정교화(값 개수 자동 결정, 축 간 중복 제거,
반복 자기개선)는 시간 제약상 범위 밖으로 남겨둔다.

---

## 추가된 절대 규칙

7. **최적화에 사용한 지표만으로 최종 성능을 보고하지 않는다.**
   독립 지표를 병기한다.

8. **한국어에서는 정량 복원율을 산출하지 않는다.** 답안지가 없기 때문이다.
   기능 검증과 정성 확인으로 범위를 제한하고, 그 사실을 보고에 명시한다.

9. **에이전트가 생성한 코드는 검증 후에만 실행한다.**

10. **신규 모듈은 기존 파이프라인과 분리한다.** 신규 기능의 실패가
    확보된 결과를 훼손해서는 안 된다.

11. **개별 케이스의 작은 차이를 결론으로 쓰지 않는다.** 실측으로
    확인했다 - 저자별 홀드아웃 15개에서 "앵커가 해로웠다"던 3명 중 2명이
    홀드아웃 45개에서 부호가 뒤집혔고, 가설의 출발점이었던 관찰도
    홀드아웃 25개에서만 나타났다. 케이스 단위 주장을 하려면 표본을
    키우고, 아니면 평균만 보고한다.

---

## 보너스: GEPA 피드백 풍부도 어블레이션 (구현 완료)

`experiments/feedback_richness_ablation.py`. metric_builder.py가 만드는
축별 자연어 피드백("길이 위반: 6문장...")이 GEPA 성찰에 실제로 도움이
되는지 검증. 점수 계산 로직은 완전히 동일하게 두고 GEPA에게 보이는
피드백 텍스트만 rich(축별 위반 내역) vs terse(점수만, `f"Score: {score:.2f}"`)
로 바꿔 같은 호출 예산(40회) 안에서 도달하는 val 점수를 비교했다.

1차 시도는 시드 프롬프트를 `build_seed_prompt()`(estimator의 추정 선호로
조립)로 썼는데, 그러면 시드가 이미 metric에 최적화돼 있어 val 점수가
처음부터 1.0으로 포화되고 두 조건 다 개선할 여지가 없었다 - 무의미한
비교였다. 시드를 일부러 못 맞는 것("Summarize the following text.")으로
바꿔 재시도.

**결과**: rich 0.98 vs terse 0.44 (같은 40회 예산). rich는 8회 만에
0.36→0.96으로 뛰고 그 이후 그 언저리를 유지하는데, terse는 40회 내내
0.44 근처에서 못 벗어난다 (`experiments/results/feedback_richness_ablation.png`).
"우리는 축을 명시적으로 갖고 있어서 구조화된 피드백을 줄 수 있고, 범용
최적화 도구는 못 하는 부분"이라는 기여 주장에 대한 직접적인 실증이다.

---

## 보너스 2: 세 번째 과제 도메인 - 고객 리뷰 작성 (구현 완료)

팀 분담이 요약(이 프로젝트)/코딩(팀원)으로 갈렸는데, 완전히 다른 세
번째 과제 카테고리를 하나 더 추가했다. 후보를 몇 개 검토했다:

- **GYAFC**(격식체 변환 벤치마크) - 원저작권(Yahoo Webscope) 신청이
  필요해 당일 구현 불가로 기각.
- **PASTEL**(같은 문장을 성별·나이·정치성향별로 다르게 쓴 병렬 데이터) -
  데이터 접근은 자유로우나, 축 자체가 인구통계 속성이라 "개인화 축"으로
  쓰기엔 부적절해 기각.
- **MACSum macdial**(대화/회의록 요약) - 이미 로컬에 있어 즉시 가능했지만,
  여전히 "요약" 계열이라 팀원 논의 후 기각.
- **Yelp Review Full**(HuggingFace, `Yelp/yelp_review_full`) - 실사람이
  쓴 리뷰 65만 건 + 1~5점 별점, 게이팅 없음, `datasets` 라이브러리 없이
  datasets-server의 rows API(순수 HTTP+JSON)로 바로 받을 수 있어 **채택**.

**구성**: `domains/review.yaml` + `checks/review.py`, 축 3개(length,
sentiment, topic). sentiment 축은 어휘 사전 기반 점수를 쓰는데, 실측
없이 만들면 specificity 꼴 날 수 있어서 먼저 검증했다
(`experiments/review_dataset.py`로 3,000건 캐싱): 별점 파생 라벨 기준
negative 평균 -0.0022, neutral 0.019, positive 0.0392로 **단조
증가하는 실신호**를 확인한 뒤에야 도메인을 만들었다. 임계값(negative
max 0.007, neutral 0.007~0.02, positive min 0.02)도 중앙값 사이 중점을
써서 실측 분포에서 그대로 뽑았다.

**검증** (`tests/test_review_domain.py`): 실제 API로 6회 선택 루프를
돌려 선호(short+positive)를 정확히 학습했고, 각 후보를 자기 목표 라벨로
채점하면 정확히 1.0/0.0이 나오는 것도 확인했다. `engine/` 코드는
무수정이다 - 세 번째 도메인까지 축적되면서 확장성 주장이 더 단단해진다.

---

## 보너스 3: 사용자에게 보여줄 확신도 - 시도했으나 되돌림 (음성 결과)

결과 화면이 추정된 선호를 단정적으로 보여주는데 근거가 얼마나 강한지는
안 알려준다는 점을 자가진단에서 지적하고, `estimator.confidence()`를
화면에 띄우려 했다. **되돌렸다.** 수치가 사용자에게 보여줄 만큼 보정돼
있지 않다.

완벽히 일관된 가상 사용자(숨긴 축값에 더 가까운 쪽을 항상 고름)로
전 축조합을 돌려 쟀다.

| 도메인 (축 값 개수) | 8회 | 12회 | 16회 | 24회 | 완전복원 |
|---|---|---|---|---|---|
| summarization_ko (3값) | 0.06 | 0.06 | 0.06 | 0.06 | 9/9 |
| coding (2값) | 0.18 | 0.28 | 0.37 | 0.49 | 8/8 |

3값 축에서는 비교를 24회까지 늘려도 0.06에 머무는데 선호는 9/9 전부
정확히 복원한다. 맞힌 사용자에게 "확신도 6%"라고 적으면 오히려 오인시킨다.

대안으로 "이 축이 갈린 비교 중 추정 결과와 같은 쪽을 고른 비율"도 쟀는데
이것도 실패했다 - 3값 축에서는 선호값이 아닌 두 값끼리 붙는 비교가 섞여서
일관된 사용자 52% vs 무작위 클릭 55%로 구분이 안 됐다 (2값인 coding은
100% vs 83%로 갈리긴 한다).

`confidence()`는 metric_builder의 축 가중치처럼 **축끼리 상대 비교하는
원래 용도로는 계속 쓴다** - 절대값이 작아도 상대 순서는 쓸 수 있다.
사용자에게 보여줄 보정된 확신도는 Bradley-Terry 사후분포나 홀드아웃
일관성 검사가 필요한 별도 작업이라 범위 밖으로 남긴다.

교훈은 specificity 축과 같은 계열이다 - **내부 계산에 쓰이던 수치가
사용자에게 보여줄 만큼 의미 있는지는 따로 재봐야 한다.**

---

## 보너스 4: 배포 앱 실사용 대응 (구현 완료)

제출 전 자가진단에서 나온 항목들을 처리했다. 실험 결과에는 영향이 없고
전부 배포된 앱과 저장소 쪽이다.

**후보 동시 생성** (`engine/generator.py`의 `generate_all`): 한 라운드에
후보 두 개를 순차로 호출하던 것을 동시 호출로 바꿨다. 서로 의존이 없는데
순차로 부르면 대기가 그대로 쌓인다. API 호출은 CPU가 아니라 네트워크
대기라 스레드로 충분하다.

| 구간 | 라운드당 | 8회 완주 |
|---|---|---|
| 배포 앱, 순차 (이전 실측) | 20.3초 | 약 160초 |
| 배포 앱, 동시 (현재 실측) | 4.2초 | 약 34초 |
| 로컬 통제 비교, 순차 | 7.8초 | 약 62초 |
| 로컬 통제 비교, 동시 | 2.9초 | 약 23초 |

배포 수치의 개선폭이 더 큰 이유는 이전 측정에 콜드스타트가 섞여 있었기
때문이다. 동시 생성만의 효과는 같은 조건에서 잰 로컬 비교의 63%로 본다
(캐시를 타지 않도록 매번 새 원문, 3회 측정).

순서 보존이 중요하다 - `as_completed`로 모으면 먼저 끝난 것이 앞에 와서
A/B가 뒤바뀌고, 사용자가 고른 것과 다른 축을 학습한다
(`tests/test_generator_parallel.py`가 이 회귀를 막는다). 캐시 쓰기도
임시 파일 + rename으로 바꿨다 - 두 스레드가 같은 키를 동시에 쓰면
반쯤 쓰인 JSON이 남는다.

**세션을 넘는 지출 상한** (`budget.py`): 기존 세션당 3회 상한은 브라우저
세션을 새로 열면 초기화돼서 공개 링크에서는 방어가 안 됐다. 앱 인스턴스가
공유하는 하루 상한(60회)을 추가했다. 확인과 차감을 한 락 안에서 한다 -
나누면 동시 방문자가 마지막 한 칸을 같이 가져간다. Streamlit을 import하지
않는 순수 모듈로 빼서 UI 없이 단위 테스트한다. 이 상한은 결제 쪽 월
지출 상한을 대체하지 않는다 - 앱이 재시작되면 카운터도 0이 된다.

**배포 감시** (`.github/workflows/smoke-live.yml`, `scripts/smoke_live.py`):
6시간마다 실제 브라우저로 배포 앱을 열어 화면이 그려지는지 보고, 실패하면
GitHub가 메일을 보낸다. 절전 방지가 목적이 아니다 - 12시간 무트래픽이면
자고 방문하면 깨어나므로 그건 감수한다. 목적은 심사 기간에 앱이 아예
안 뜨는 상태를 빨리 아는 것이다. 마커로 DOM 속성 대신 화면에 보이는
문구를 쓴다 - 예전에 입력란의 `maxlength`를 배포 확인 마커로 썼다가
Streamlit이 `max_chars`를 DOM 속성으로 찍지 않아 낭패를 봤다.

**핵심 3개 모듈 테스트**: `pytest tests/`가 초록색으로 나오는데도 16개
파일 중 4개만 수집하고 있었고, 9개 파일은 assert가 0개인 출력 스크립트였다.
별표로 표시한 selector·estimator·metric_builder 중 estimator는 테스트
파일 자체가 없었다. 세 모듈 전부 API 없이 도는 회귀 테스트를 붙였다
(16개 → 54개).

테스트 값은 가정하지 않고 실측해서 넣었다. 불확실도 방식이 순차보다
빠르지 않고 둘 다 6회차에 완전 복원한다는 것이 실제 곡선이라, "불확실도가
더 빠르다"를 테스트로 박지 않았다. 대신 둘 다 복원 후 되돌아가지 않는
것과, 무작위는 15회차에도 복원을 유지하지 못하는 것(평균 1.8/2)을 고정했다.

**실사용자 검증 착수** (`feedback.py`, `scripts/summarize_feedback.py`):
원래 계획(11주차)의 실사용자 3~5인 검증이 실행되지 않아, 모든 정량 결과가
페르소나 기반이고 "실제 사람도 좋아했다"는 근거가 없었다. 결과 화면 끝에
예/아니오 한 줄 질문을 넣어 공개 링크 방문자의 응답을 모은다.

저장 문제는 로그로 우회했다 - Streamlit Community Cloud 파일 시스템은
재시작 시 날아가고 프로젝트 규칙상 서버형 DB는 쓰지 않는다. 표준 출력에
한 줄 JSON으로 찍으면 콘솔 로그에서 읽히고 새 의존성이 0개다.

만드는 과정에서 두 번 틀렸고 둘 다 조용한 실패였다. (1) `print` 에 flush가
없어서 UI는 정상인데 로그에 아무 줄도 남지 않았다 - 모으고 있다고 착각하기
딱 좋다. (2) 윈도우에서 표준 출력을 파일로 리다이렉트하면 cp949로 써서
로그의 한글이 깨졌다. 줄을 ASCII 전용(유니코드 escape)으로 만들어 환경
의존을 없앴고, 사람이 읽을 때는 집계 스크립트가 풀어서 보여준다.

원문과 생성 결과는 기록하지 않는다. 개인 정보가 섞일 수 있고 사용자가
공유할 의도로 넣은 값이 아니다. **데모 모드 응답은 합치지 않는다** -
규칙 기반 결과에 대한 평가라서 개인화 검증 근거로는 약하다.

예선 제출(9/20) 시점에는 표본이 없으므로 "수집 중"으로만 쓰고, 본선(10/7)
발표에 실제 수치를 넣는 것이 이 설계의 의도다.

**테스트 공백 해소 (완료)**: `pytest tests/`가 초록색으로 나오면서도
16개 파일 중 4개만 수집하던 문제를 끝까지 정리했다. API·MACSum 이 필요해
남겨뒀던 9개 파일에 전부 검증을 붙였다.

- `pytest.ini` + `tests/conftest.py`: `api`·`macsum` 마커를 등록하고, 키나
  데이터가 없으면 이유를 표시하며 건너뛴다. 조용히 통과시키지 않는다.
  `pytest -m "not api"` 로 API 없이 105개를 돌릴 수 있다.
- 모델은 기존 스크립트와 같은 `gpt-4.1-mini`를 유지했다. `cache/`에 이미
  있는 응답을 재사용해서 테스트가 빠르고 추가 비용이 거의 없다.
- `tests/test_reported_results.py`(신규): README 에 적힌 수치를 커밋된
  `experiments/results/*.csv`에서 다시 계산해 맞춘다. 리뷰 과정에서 이미
  두 번 문서와 데이터가 어긋났기 때문에(%p 혼용, 표본 5개 평균만으로
  우위 주장) 코드로 막는다.
- 정리 중 `test_generator.py`·`test_persona.py`에 6주차에 제외한
  `specificity` 축이 축조합에 남아 있는 것을 발견해 걷어냈다. MACSum
  원본 데이터에는 그 축이 그대로 있으므로, 페르소나를 찾을 때는 딕셔너리
  정확 비교 대신 필요한 축만 본다.

최종: 테스트 121개(API 포함) / 105개(API 없이). 시작 시점은 16개였다.
실제 API 호출과 MACSum 데이터가 필요해서, 키 없는 환경에서 건너뛰는
표시를 붙이는 작업이 따로 필요하다.

---

## 보너스 5: 전문가 프롬프트 추출 - 부분 성공 (진행 중)

"자기 전문 자료를 넣으면 그 사람처럼 쓰는 프롬프트를 뽑을 수 있지
않을까"라는 아이디어를 구현했다. `agents/expert_onboarding.py` +
`experiments/expert_prompt_eval.py`. **규칙 10에 따라 배포 앱에는 아직
연결하지 않았다** - 제출 직전에 확보된 결과를 흔들지 않는다.

**용어를 정확히 쓴다.** 산출물은 시스템 프롬프트이고 에이전트가 아니다.
CLAUDE.md가 정의한 에이전트는 결과에 따라 다음 행동이 달라지고 도구를
쓰는 것이다. 다만 프롬프트를 *만드는 과정*은 에이전트다. 그래서 보고는
"에이전트가 전문가 프롬프트를 만든다"로 한다.

`domain_onboarding`과 다른 점: 그쪽은 같은 원문에 대한 서로 다른 결과물
여러 개를 받아 "무엇이 갈리는가"를 찾는다. 이쪽은 변이가 없는 한 사람의
(과제, 답변) 뭉치를 받아 "이 사람의 설정값"을 추정하고, 비교용 대안값은
LLM이 만든다. `to_domain()`이 추출 결과를 Domain 객체로 바꿔주므로
generator·selector·estimator는 무수정으로 돈다.

**검증 설계**: MACSum의 속성 조합 하나를 고정하면 그 조합으로 94개 문서를
요약한 "한 사람"이 생긴다. 그 사람의 답변 8개만 보여주고(라벨은 감춘다)
축을 추출해 프롬프트를 만든 뒤, **한 번도 보여주지 않은 14개 문서**에
적용해 그 사람의 실제 요약과의 ROUGE-L을 잰다. 지표는 추출 루프에 쓰이지
않으므로 순환 논증이 아니다. 조건은 셋이다 - base(과제 서술만),
expert(추정한 설정), anti(일부러 반대 설정). anti를 넣는 이유: expert가
base보다 높은 게 그냥 지시문을 더 붙인 효과일 수도 있어서, anti가 base보다
낮아야 축이 방향을 가진 것이다.

**검증 방법을 한 번 고쳐야 했다.** 처음에는 ROUGE-L 만 봤는데, 이
방법이 주장하는 것은 형식 복제이므로 주장과 지표가 어긋났다. 긴 글을
쓰는 저자에서 expert 조건이 목표 길이에 훨씬 가까웠는데도(목표 128.4단어
대비 base +52.0, expert +18.2) ROUGE-L 은 거의 같았다. 그래서 **형식
거리**(문장 수·단어 수·문장당 단어 수의 상대 오차 평균, 0이면 형식 동일)를
코드로 재는 지표로 추가해 둘을 나란히 본다.

**생성 온도를 고정하지 않아 한동안 노이즈를 읽었다.** 같은 페르소나를 두
번 돌렸을 때 한 번은 277단어 과교정, 다음은 104단어 미달이었고 base 조차
180.4 -> 170.1 로 움직였다. 추출과 생성 양쪽에 temperature=0 을 걸어야
비교가 성립한다. `engine/generator.py` 에 temperature 인자를 더했고
기본값 None 이라 앱 동작과 기존 캐시는 그대로다.

**두 번 틀리고 세 번째에 맞았다.** 중간 결론을 두 개 폐기했으므로 경과를
그대로 남긴다.

*1차 - 축 기반.* LLM 이 제안한 축을 그 사람의 값으로 설정. short 만
작동하고 long 은 오히려 나빠졌다.

*2차 - 절대 단어 수 앵커.* 코드로 잰 단어 수를 프롬프트에 박았다.
short·normal 은 좋아졌지만 long 은 여전히 안 됐고, "모델이 큰 길이
목표에 체계적으로 미달한다"는 결론을 냈다. **이 결론은 틀렸다.**
요청 단어 수를 20~320 으로 바꿔 전달 함수를 직접 재보니
(`experiments/length_transfer.py`) 모델은 요청값을 거의 정확히 따랐다
(요청/실제 비율 0.99~1.06). 실제 원인은 내 실험이었다 - 앵커는 학습
8개로 만들고 목표는 홀드아웃 14개로 쟀는데, MACSum 요약 길이가 원문
길이를 따라가고 레코드가 정렬돼 있어서 두 부분집합의 평균이 체계적으로
달랐다(long 에서 98.5 대 128.4 단어). 모델은 학습에서 잰 98단어를 요청받아
97.9단어를 냈다. 거의 완벽한 준수를 미달로 읽은 것이다.

*3차 - 압축률 앵커.* 섞어서 분할해도 차이가 남아 더 파보니 근본 문제가
나왔다. **저자의 길이는 상수가 아니다.** 같은 사람이 짧은 기사에는
40단어, 긴 기사에는 258단어를 쓴다.

| 페르소나 | 절대 단어 변동계수 | 압축률 변동계수 | 상관(원문,답변) |
|---|---|---|---|
| short | 0.584 | **0.076** | 0.995 |
| normal | 0.556 | **0.087** | 0.967 |
| long | 0.560 | **0.089** | 0.942 |

압축률이 7배 안정적이다. 그래서 학습 예시에서 **압축률만 재고**, 목표
단어 수는 새 과제의 원문 길이에 곱해 과제마다 따로 구한다.

**최종 결과 (홀드아웃 30문서 / 학습 12개, 결정적 생성, 시드 고정 분할)**

ROUGE-L 과 문서별 페어드 비교. 표준편차가 평균 차이보다 큰 경우가 있어
양측 부호검정 p 값을 함께 낸다(`sign_test_p`, 이항분포로 정확 계산).

| 페르소나 | base | 절대 앵커 | **압축률 앵커** | 평균차 | 페어드 | p값 | 판정 |
|---|---|---|---|---|---|---|---|
| short | 0.148 | 0.223 | **0.262** | +0.114 | 27/30 | <0.0001 | 유의 |
| normal | 0.201 | 0.214 | **0.262** | +0.061 | 22/30 | 0.016 | 유의 |
| long | 0.204 | 0.213 | **0.228** | +0.024 | 20/30 | 0.099 | **판정 보류** |

형식 거리 (낮을수록 이 저자에 가깝다) 와 길이 오차:

| 페르소나 | base | 절대 앵커 | **압축률 앵커** | 길이 오차(압축률) |
|---|---|---|---|---|
| short | 2.267 | 0.382 | **0.049** | +1.1 단어 |
| normal | 0.919 | 0.387 | **0.016** | −1.0 단어 |
| long | 0.449 | 0.408 | **0.073** | −3.3 단어 |

**형식 복제는 세 저자 모두에서 확실하다.** 형식 거리가 절대 앵커의
0.38~0.41 에서 0.016~0.073 으로 한 자릿수 떨어지고, 길이 오차는 수십
단어에서 3단어 이내로 들어온다.

**내용 근접(ROUGE-L)은 두 저자에서 유의하고 한 저자에서는 보류다.**
short·normal 은 부호검정 p 가 0.05 아래지만, long 은 20/30 에 p=0.099 로
방향만 양수다. 표본을 더 키우면 갈릴 수 있으나 지금 수치로는 "유의하게
낫다"고 쓰지 않는다.

*4차 - 비율을 규칙으로 적어보기. 실패.* 위 최고 조건은 과제마다 목표
단어 수를 계산해서 프롬프트에 박아 넣는다. 그런데 사용자가 받고 싶은 건
복사해서 아무 데나 붙일 수 있는 **프롬프트 한 덩어리**다. 그래서 계산
결과 대신 비율 자체를 규칙으로 적어주고 모델이 스스로 적용하는지 쟀다
(`ratio_rule_anchor`, 조건 이름 `ratio_rule`). 생성되는 문구는 이렇다.

```
Match this form: write about 5.2% as many words as the source text
(roughly 52 words for a 1000-word source), averaging about 18 words
per sentence.
```

| 페르소나 | base | 과제별 계산 | 규칙 | 과제별 승 | 규칙 승 | 규칙 p |
|---|---|---|---|---|---|---|
| short | 0.148 | **0.262** | 0.207 | 27/30 | 28/30 | <0.0001 |
| normal | 0.201 | **0.262** | 0.193 | 22/30 | 13/30 | 0.585 |
| long | 0.204 | **0.228** | 0.205 | 20/30 | 16/30 | 0.856 |

| 페르소나 | 과제별 길이오차 / 형식거리 | 규칙 길이오차 / 형식거리 |
|---|---|---|
| short | +1.1 단어 / 0.049 | +26.6 단어 / 0.777 |
| normal | −1.0 단어 / 0.016 | +40.4 단어 / 0.600 |
| long | −3.3 단어 / 0.073 | +56.5 단어 / 0.582 |

**모델은 백분율 규칙을 스스로 적용하지 못한다.** 형식 거리가 한 자릿수
나빠지고(0.016~0.073 → 0.58~0.78), 길이 오차가 3단어 이내에서 27~57단어로
벌어진다. 방향도 전부 초과다 - 1000단어 환산 예시까지 같이 줬는데도
원문 길이에 비례시키지 않는다. short 만 base 를 이기는데(28/30), 이건
비율을 지켰기 때문이 아니라 **어떤 종류든 짧게 쓰라는 신호가 있으면
그 저자에 가까워지기 때문**이다. normal·long 에서는 base 와 구분되지
않는다 (p=0.59, p=0.86).

**제품 설계에 직접 걸린다.** 산출물을 "복사해서 쓰는 정적 프롬프트 한
장"으로 내보내면 이 방법의 효과 대부분이 사라진다. 목표 단어 수는
**앱이 사용자가 넣은 원문을 보고 코드로 계산해 프롬프트에 채워 넣어야**
한다. 앱은 원문을 이미 갖고 있으므로 구현에 걸림돌은 없지만, 산출물의
형태가 "프롬프트 텍스트"가 아니라 "프롬프트 템플릿 + 채우는 코드"로
바뀐다는 뜻이다. 이건 한계로 정직하게 적는다 - 우리 도구의 값어치 중
길이 부분은 프롬프트 밖으로 들고 나가면 따라오지 않는다.

`ratio_rule_anchor` 자체는 지운 게 아니라 남겨둔다. 앱 밖으로 프롬프트를
내보내야 할 때의 차선책이고, "이 경로는 이만큼 약해진다"를 수치로 보여줄
수 있는 것 자체가 결과다.

*5차 - 왜 넓힌 어휘가 안 먹혔나. API 없이 가름.*
`experiments/form_feature_spread.py`. 넓힌 지표가 실패한 원인이 둘로
갈렸다 - (가) 지표가 저자를 못 구분한다, (나) MACSum 페르소나의 문체가
길이 말고는 균질하다. 다음 행동이 정반대(지표 교체 대 코퍼스 교체)라
그냥 둘 수 없었는데, **저자 간 차이가 저자 안 흔들림보다 큰지**만 보면
호출 없이 갈린다.

분모를 무엇으로 두느냐가 함정이었다. 처음엔 답변 하나하나의 분산으로
나눴는데 그러면 효과가 확인된 words_per_answer(34/65/97단어로 뚜렷이
갈린다)조차 0.48 로 탈락했다. 저자 안 흔들림이 크기 때문인데 -
**앵커가 쓰는 값은 답변 하나가 아니라 학습 예시 12개의 평균**이다.
분모를 평균의 분산(개별분산/12)으로 바꾸면 맞다.

| 특징 | short | normal | long | 답변기준 | 평균기준 |
|---|---|---|---|---|---|
| 압축률 | 0.053 | 0.103 | 0.152 | 13.79 | **165.51** |
| 단어/답변 | 34.3 | 65.5 | 96.8 | 0.48 | **5.76** |
| 문장/답변 | 1.9 | 3.1 | 4.3 | 0.26 | **3.07** |
| 문장당 단어 | 18.9 | 22.3 | 24.8 | 0.08 | 1.02 |
| 글머리 기호 비율 | 0.000 | 0.000 | 0.000 | 0.00 | 0.00 |
| 문단/답변 | 1.000 | 1.000 | 1.000 | 0.00 | 0.00 |
| 유보 표현/100단어 | 0.82 | 0.93 | 0.93 | 0.00 | 0.01 |
| 숫자/100단어 | 2.25 | 2.14 | 1.75 | 0.01 | 0.06 |
| 1인칭/100단어 | 0.10 | 0.32 | 0.35 | 0.02 | 0.23 |

**가설 (나)다.** 길이 계열 3개는 임계값(2.0)을 넘고 넓힌 지표는 다섯 개
전부 0.23 아래다. 그중 글머리 기호 비율과 문단 수는 **코퍼스 전체에서
값이 한 번도 변하지 않는다** - MACSum 요약은 글머리 기호가 아예 없고
전부 1문단이다. 그 둘은 어떤 앵커에 넣어도 저자를 구분할 수 없었다.

**진단이 사후적으로 맞아떨어진다.** 특징의 강도 순서(압축률 165 > 단어
5.8 > 문장 3.1 > 문장당 단어 1.0 > 넓힌 지표 ~0)가 어떤 방법이 실제로
통했는지와 일치한다. 압축률 앵커가 최고 조건이었고, 절대 단어 앵커는
작동하지만 약했고, 넓힌 어휘는 무효였다.

*6차 - 앵커의 두 절이 각각 일을 하는가.* 진단에서 문장당 단어 수가
탈락(1.02)했으므로 앵커의 문장 절이 단어 절의 반복일 수 있다고 봤다.
문장 절을 뺀 조건(`ratio_words`, `ratio_anchor(..., keys=("words_per_answer",))`)을
같은 홀드아웃에 돌렸다.

| 조건 | 문장당 단어 수 (short/normal/long) | 형식 거리 | ROUGE-L |
|---|---|---|---|
| 저자 목표 | 17.9 / 20.7 / 23.1 | - | - |
| base | 22.2 (모델 기본값) | 2.267 / 0.919 / 0.449 | 0.148 / 0.201 / 0.204 |
| ratio (두 절) | 17.5 / 20.4 / 20.5 | **0.049 / 0.016 / 0.073** | **0.262 / 0.262 / 0.228** |
| ratio_words | 13.4 / 16.2 / 17.3 | 0.187 / 0.175 / 0.196 | 0.252 / 0.250 / 0.229 |

**문장 절은 반복이 아니다.** 단어 목표만 주면 단어 수는 오히려 더 정확히
맞지만(오차 −0.4/+0.6/−0.4 대 +1.1/−1.0/−3.3) 모델이 문장을 잘게
쪼개서 문장당 단어 수가 13~17 로 내려간다 - 저자(18~23)도, 모델 기본값
(22.2)도 아닌 값이다. 형식 거리가 3~11배 나빠진다.

교훈을 정확히 적어야 한다. **"그 특징이 저자를 구분하지 못한다"와
"그 특징을 적는 것이 도움이 안 된다"는 다른 말이다.** 문장 절은 저자
정보를 나르는 게 아니라 **모델의 기본 습관을 묶는 제약**으로 일한다.
저자 신호를 나르는 쪽은 문장당 단어 비율(1.02)이 아니라 문장 개수
(3.07)였고, 진단 목록에 그 항목을 빠뜨렸던 것을 이 어블레이션이 잡았다.

*7차 - 신호가 있는 코퍼스에서 다시 재기. 판정이 뒤집힌다.*
"이 코퍼스에 신호가 없다"는 판정은 신호가 있는 곳에서 같은 지표가
살아나는 걸 보여야 완성된다. 먼저 MACSum 반대쪽(macdial, 대화·회의록
요약 2,954개)에 같은 진단을 돌렸다 - **글머리 기호 0, 문단 수 정확히 1**
로 macdoc 과 똑같았다. MACSum 으로는 길이 외 문체를 검증할 수 없다는
것이 3,000건 규모로 확정됐다.

그래서 실제 저자 코퍼스를 받았다 (`experiments/expert_corpus_se.py`).
Stack Exchange 공개 API - 키 없이 하루 300회, 표준 라이브러리만 사용,
새 의존성 0개. cooking 사이트 상위 답변자 4명, 저자당 88~99개의
(질문, 답변) 쌍. **제품이 노리는 상황 그 자체다** - 자기 분야에 답을
많이 써둔 실제 사람. HTML 본문은 `<li>` 를 "- " 로, 문단·코드 블록을
빈 줄로 옮긴다 (태그를 지우면 재려던 특징이 사라진다).

| 특징 | u4638 | u67 | u17143 | u19707 | MACSum 평균기준 | SE 평균기준 |
|---|---|---|---|---|---|---|
| 단어/답변 | 299.1 | 202.7 | 104.5 | 162.3 | 5.76 | **4.27** |
| 문장/답변 | 15.6 | 8.8 | 6.9 | 7.1 | 3.07 | **4.19** |
| 문장당 단어 | 19.4 | 24.1 | 15.7 | 23.6 | 1.02 | **3.27** |
| 문단/답변 | 5.3 | 6.1 | 1.7 | 2.9 | 0.00 | **4.14** |
| 글머리 기호 비율 | 0.049 | 0.106 | 0.000 | 0.088 | 0.00 | 0.66 |
| 유보 표현/100단어 | 0.98 | 1.08 | 1.49 | 1.28 | 0.01 | 0.34 |
| 숫자/100단어 | 0.59 | 0.69 | 0.62 | 0.68 | 0.06 | 0.01 |
| 1인칭/100단어 | 0.69 | 1.20 | 1.21 | 0.91 | 0.23 | 0.33 |

**가설 (가)로 뒤집힌다 - 부분적으로.** `paragraphs_per_answer` 가 4.14
로 통과한다(1.7문단 대 6.1문단). `words_per_sentence` 도 MACSum 에서
1.02 였던 것이 여기서는 3.27 로 살아난다 - 그 지표는 멀쩡했고 MACSum 이
그 축으로 안 갈렸던 것이다. 글머리 기호는 0.66 으로 임계값 아래지만
0.000~0.106 으로 실제로 변한다(MACSum 에서는 리터럴 0 이었다). 반면
유보 표현·숫자·1인칭 밀도는 실제 사람 4명 사이에서도 통과하지 못한다
(0.01~0.34) - 이 셋은 코퍼스 문제가 아니라 **지표 문제로 판정**한다.

**그리고 훨씬 중요한 것이 나왔다. 압축률 앵커는 요약 과제의 산물이다.**

| 코퍼스 | 상관(과제 길이, 답변 길이) | 절대 단어 변동계수 | 압축률 변동계수 |
|---|---|---|---|
| MACSum macdoc | 0.913 ~ 0.996 | 0.53 ~ 0.56 | **0.08 ~ 0.10** |
| SE cooking | **0.04 ~ 0.25** | **0.54 ~ 0.68** | 0.77 ~ 1.03 |

MACSum 에서 압축률이 절대 단어 수보다 7배 안정적이었던 이유는 요약문이
원문에서 파생되기 때문이다 - 원문이 길면 요약도 길다(상관 0.99). 실제
Q&A 에서는 그 관계가 없다(상관 0.04~0.25). 답변 길이는 **질문의 난이도**가
정하고 질문의 길이가 정하지 않는다. 그래서 압축률의 변동계수가 1.0 을
넘어 절대 단어 수(0.54~0.68)보다 오히려 불안정하다. 판별력비도 1.72 로
임계값 아래다.

즉 3차에서 얻은 "압축률로 재라"는 결론은 **요약 도메인에 한정된다.**
n=30 부호검정까지 붙인 결과가 다른 과제 유형에서는 방향이 반대다.
보고에 반드시 범위를 명시해야 하고, 방법 쪽에서는 **어느 모수화가 이
저자에게 더 안정적인지를 학습 예시에서 재서 고르는** 것이 옳다 -
상수를 박아 넣는 게 아니라. 절대 규칙 6 과 같은 방향이다.

*8차 - 두 번째 사이트로 재현.* 한 사이트에서 나온 반전은 그 사이트의
특성일 수 있다. writing 사이트 상위 답변자 4명(저자당 92~100쌍)으로
같은 진단을 돌렸다.

| 특징 | MACSum macdoc | SE cooking | SE writing |
|---|---|---|---|
| 압축률 | **165.51** | 1.72 | **0.38** |
| 단어/답변 | 5.76 | 4.27 | 2.13 |
| 문장/답변 | 3.07 | 4.19 | 2.23 |
| 문장당 단어 | 1.02 | 3.27 | 1.14 |
| 문단/답변 | 0.00 | 4.14 | 1.43 |
| 글머리 기호 비율 | 0.00 | 0.66 | 0.97 |

**압축률 탈락이 재현된다** (1.72, 0.38 - 둘 다 임계값 2.0 아래). 실제
Q&A 에서 답변 길이가 질문 길이를 따라가지 않는다는 것은 한 사이트의
특성이 아니다. 반면 절대 단어 수와 문장 수는 두 사이트 모두에서 통과한다.

문단 수는 사이트에 따라 갈린다 - cooking 4.14(통과), writing 1.43(미달,
다만 4.1~7.8문단으로 값 자체는 변한다). 글머리 기호도 0.66/0.97 로 둘 다
미달이지만 MACSum 의 리터럴 0 과는 다르다. 정직하게 적으면 **문단 수와
글머리 기호는 약한 저자 신호**이고, 유보 표현·숫자·1인칭 밀도는 실제
사람 8명 사이에서도 신호가 없어 **지표 자체를 기각**한다.

**길이 모수화를 측정으로 고르게 고쳤다** (`length_parameterization`,
`stable_length_anchor`). 학습 예시에서 절대 단어 수와 압축률의 변동계수를
각각 재서 작은 쪽을 쓴다. 상수를 박지 않는다. 두 코퍼스를 정확히 갈라낸다 -
MACSum 페르소나 3명 전원 ratio(압축률 CV 0.07~0.17 대 절대 0.55~0.60),
SE 저자 4명 전원 absolute(절대 CV 0.42~0.70 대 압축률 0.75~1.01).

*9차 - 실제 저자 홀드아웃. 형식은 되고 내용은 안 된다.*
`experiments/expert_prompt_eval_se.py`. cooking 저자 4명, 학습 12개 /
홀드아웃 25개, 조건 4개. base(지시 없음) / stable(측정으로 고른 길이
앵커) / ratio_forced(압축률 강제 - 요약에서 이겼던 그 방법) /
paragraphs(stable + 문단 수 목표).

| 지표 (4명 평균) | base | stable | ratio_forced | paragraphs |
|---|---|---|---|---|
| 형식 거리 (길이·문장) | 0.233 | **0.158** | 0.814 | 0.256 |
| 문단 수 절대오차 | 1.65 | 1.90 | 3.90 | **0.88** |
| ROUGE-L | 0.154 | 0.158 | 0.145 | 0.156 |

**형식은 개선된다.** 형식 거리가 0.233 → 0.158 (32% 감소), 저자 4명 중
3명에서 좋아진다. **측정으로 고르는 것이 값을 한다** - 압축률을 강제하면
0.814 로 5배 나빠진다. 요약에서 최고였던 방법이 실제 Q&A 에서는 base 보다
훨씬 못하다.

**내용은 개선되지 않는다.** ROUGE-L 0.154 → 0.158 이고 부호검정은
한 명이 stable 쪽(18/25, p=0.043), 한 명이 base 쪽(7/25, p=0.043)으로
갈린다 - 노이즈다. Q&A 에서 답변 내용은 질문이 정하므로 형식을 맞춰도
같은 문장이 나오지 않는다. **형식은 이기고 내용은 못 이긴다를 그대로
보고한다.**

**문단 절이 필요하다는 것도 확인됐다.** 길이만 지시하면(stable) 문단
오차가 base 보다 나빠진다(1.65 → 1.90) - u17143·u19707 에서 문단이
1.0 으로 붕괴한다. 문장 절을 뺐을 때 모델이 문장을 잘게 쪼갠 것과 같은
현상이다. **길이를 조이면 조이지 않은 구조가 무너진다.** 문단 목표를
같이 주면 0.88 로 내려간다.

**대신 지시를 하나 더 붙이면 앞의 두 개가 덜 지켜진다.** paragraphs 의
형식 거리가 0.256 으로 stable(0.158)보다 나쁘고 base(0.233)보다도
나쁘다. 길이·문장·문단을 동시에 다 맞추지는 못한다 - 어느 쪽을 우선할지는
제품 결정이고, 지금 수치로는 둘 다 최적인 조건이 없다.

**MACSum 보다 개선폭이 작은 이유도 데이터에 있다.** 형식 거리가 여기서는
0.158 인데 MACSum 에서는 0.016~0.073 이었다. 실제 저자는 스스로에게 덜
일관적이다 - u4638 은 학습 12개 평균이 356단어인데 홀드아웃 25개는
263단어다(섞어서 분할한 뒤에도 35% 차이). 앵커는 학습에서 잰 356단어를
충실히 재현해 350단어를 냈다. **방법의 오차가 아니라 저자 본인의 변동을
물려받은 것**이고, 사람을 대상으로 하는 한 없앨 수 없는 몫이다.

**선택 규칙의 결함을 하나 잡아 고쳤다.** 처음엔 변동계수가 작은 쪽을
그냥 골랐는데 n=12 에서 불안정했다. u67 은 전체 96쌍으로 보면 절대 쪽이
맞는데(0.511 대 0.961) 이 분할에서는 뒤집혀 압축률을 골랐고, 목표
180단어에 331단어를 내며 형식 거리가 base 의 0.189 에서 **0.797 로
아무것도 안 한 것보다 나빠졌다.** 마진을 실측으로 걸었다
(`RATIO_MARGIN = 0.6` - 저자 11명 x 20개 분할 = 220 케이스에서 압축률CV/
절대CV 비율이 요약 쪽 최대 0.545, Q&A 쪽 최소 0.694 로 틈이 벌어진다.
그 사이 값을 쓰면 220개 전부 정답이고, 마진 없이 고르면 13개를 틀린다).
고친 뒤 u67 은 205단어 / 형식 거리 0.264 로 돌아왔다.

이 케이스는 **근소한 차이로 방법을 바꾸면 아무것도 안 한 것보다 나빠질
수 있다**는 사례로 남긴다. 측정해서 고르는 것 자체가 옳아도, 측정값의
표본 오차를 감안하지 않으면 측정이 해가 된다.

*10차 - 상충을 선별로 피해보기. 실패.* 문단 절을 항상 붙이면 길이·문장이
나빠지니, **모델 기본값과 실제로 다른 항목만** 붙이면 둘 다 얻을 수
있다고 봤다(`structure_anchor`). 이득이 저자와 모델 기본값의 거리에
비례한다는 것은 이미 확인된 관찰이고, 실제로 base 가 이미 저자의 문단
수에 가까운 저자도 있었다(u19707: 실제 3.3, base 3.6).

**안 됐다.** 형식 거리 평균 0.264 로 `paragraphs`(0.256)와 사실상 같다.
이유가 명확하다 - **모델 기본값이 실제 저자 4명 전원과 문단·글머리
기호에서 달라서 선별 게이트가 한 번도 작동하지 않았다.** 네 저자 모두
두 항목이 다 뽑혔으니 `paragraphs` + 글머리 기호와 같은 조건이 된 것이다.

여기서 결론을 내린다. **문제는 "무엇을 재는가"가 아니라 여러 수치 목표를
한 프롬프트에 쌓으면 서로 간섭한다는 것이다.** 같은 현상을 세 번 봤다 -
단어만 지시하면 문장이 쪼개지고, 길이만 지시하면 문단이 붕괴하고, 셋을
같이 주면 앞의 둘이 덜 지켜진다. 지표를 더 찾아 앵커에 더하는 길은
이 벽을 다시 만난다.

*11차 - 예시만 보여주면 되지 않나. 반은 맞다.* 수치를 재서 지시문에
박는 게 이 방법의 전부인데, 가장 먼저 나올 질문은 "그냥 이 사람 답변
몇 개를 보여주면 되지 않나"다. 그 비교가 없으면 측정 앵커의 값어치를
주장할 수 없다. 이 조건은 간섭 문제도 우회한다 - 예시는 절이 아니라
본보기라서 간섭할 절 자체가 없다. 저자의 실제 답변 3개를 예시로 넣었다
(`fewshot_prompt`. 과제 본문은 800자로 자르지만 **답변은 자르지 않는다** -
자르면 전달하려는 형식, 특히 길이가 왜곡된다).

| 조건 | 형식 거리 (4명 평균) | 프롬프트 길이 |
|---|---|---|
| base | 0.233 | 16단어 |
| fewshot (예시만) | 0.201 | 약 1,290단어 |
| stable (측정 앵커) | **0.158** | 29단어 |
| **fewshot + 앵커** | **0.121** | 약 1,300단어 |

**예시만으로는 측정 앵커를 못 이긴다.** 프롬프트를 45배 써도 0.201 대
0.158 이다. 예시는 문체를 보여주지만 "이 과제에 몇 단어를 쓸지"를
지정하지는 못한다 - u4638 에서 실제 263단어에 예시 조건은 183단어를
냈고(과소), 앵커는 350단어를 냈다(과대).

**그런데 둘을 합치면 앵커 단독보다 24% 낫다** (0.158 → 0.121, 저자 4명
중 3명에서 개선). **서로 대체가 아니라 보완이다.** 예시가 수치로 못
재는 것을 나르고, 앵커가 예시로 안 잡히는 분량을 못 박는다. 심사에서
나올 질문에 대한 답이 수치로 나왔고, 동시에 제품 설계도 정해진다 -
사용자 자료가 있으면 예시와 앵커를 **같이** 넣는다.

ROUGE-L 은 여전히 안 움직인다 (base 0.154 / fewshot 0.161 /
fewshot_stable 0.160). 저자 한 명에서만 부호검정이 유의하다(18/25,
p=0.043). **형식은 전이되고 내용은 전이되지 않는다**는 결론은 그대로다.

*12차 - 간섭을 GEPA 에게 넘기기.* 절을 사람이 쌓는 대신, 저자 프로필을
**평가 함수**로 만들어 GEPA 가 여러 목표를 동시에 만족하는 문구를 찾게
했다 (`agents/expert_onboarding.py` 의 `expert_form_metric`,
`agents/expert_gepa.py`). 이 프로젝트의 원래 논지와 같은 경로이고,
구조화된 피드백이 효과가 있다는 것도 이미 확인돼 있다(rich 0.98 대
terse 0.44). 피드백은 방향을 적는다 - "too many words: 340 against this
author's 210" 처럼, 어느 쪽으로 얼마나 어긋났는지가 있어야 최적화기가
고칠 수 있다.

규칙 10 을 지켜 `optimize/run_gepa.py` 는 고치지 않고 거기의
`MetricEvaluator` 만 가져다 썼다.

**예산 배분에서 한 번 막혔다.** 예산 12 로 돌렸더니 GEPA 가 시드를
그대로 돌려줬다 - 반복 0 의 기준 평가가 valset 크기만큼 호출을 쓰는데
valset 을 학습 전체(12개)로 뒀더니 탐색 여지가 0 이었다. valset 을 6 으로
줄이고 예산을 60 으로 올렸다.

**참조선을 같이 재야 읽을 수 있다.** 저자의 실제 답변을 자기 프로필로
채점하면 0.40~0.67 밖에 안 나온다 - 답변 하나는 자기 평균 주위에서
흔들리기 때문이다. 다만 이것을 상한이라 부르면 틀린다 - 평균에 딱 붙어
쓰는 생성기는 흔들리는 사람보다 높은 점수를 받고, 실제로 stable 조건이
0.653 으로 저자 본인 0.451 을 넘었다. "사람 수준이 여기쯤"이라는
참조선으로만 읽는다.

**순환 논증을 명시한다** (절대 규칙 7). GEPA 가 이 평가 함수로
최적화하므로 형식 점수는 **최적화한 지표**다. 한 번도 안 보여준 과제에서
재지만 함수는 같으므로 그렇게 이름을 붙여 적고, ROUGE-L 을 반드시
병기한다.

**예비 결과 (저자 1명, 홀드아웃 5개 - 본 실행은 미완료)**

| 조건 | 형식 점수 | ROUGE-L | 형식 거리 | 프롬프트 |
|---|---|---|---|---|
| 저자 본인 (참조선) | 0.451 | - | - | - |
| base | 0.251 | 0.110 | 0.455 | 16단어 |
| stable | 0.653 | 0.147 | 0.071 | 29단어 |
| fewshot | 0.440 | 0.139 | 0.317 | 1,290단어 |
| **gepa** | 0.631 | 0.137 | **0.027** | 463단어 |

방향은 좋다 - 형식 거리가 0.455 에서 0.027 로 내려가 사람이 만든 최고
조건(stable 0.071)보다 낫고, 프롬프트도 few-shot 의 1/3 이다. **다만
저자 1명 / 홀드아웃 5개이므로 결론으로 쓰지 않는다.**

**본 실행은 API 크레딧 소진으로 중단됐다** (저자 4명 x 홀드아웃 25개
예정, 3번째 저자의 홀드아웃 20/25 에서 429 insufficient_quota). 크레딧이
채워지면 재개한다 - 생성 결과는 캐시에 있어서 base·stable·fewshot 쪽은
비용이 거의 없고, GEPA 최적화만 다시 호출한다.

**중단이 설계 결함 하나를 드러냈다.** 결과를 마지막에 한 번만 저장하고
있어서, 이미 끝난 저자 2명의 최적화(호출 120회, 실제 비용)가 디스크에
하나도 안 남았다. 표준 출력 로그에서 프롬프트 하나만 건져냈다
(`experiments/results/expert_gepa_prompts_partial.json`). GEPA 최적화는
생성 캐시를 타지 않으므로 다시 돌리면 그 비용을 또 쓴다. 두 실행 스크립트
모두 **저자마다 즉시 저장**하고, 중단 시 사유와 지점을 파일에 남기도록
고쳤다.

**그리고 살펴야 할 오염이 하나 보인다.** GEPA 가 찾은 프롬프트에 학습
과제의 **내용**이 들어갔다 - "dumpling soit" 같은 특정 질문, suet(영국식
덤플링에 쓰는 신장 주변 지방)의 설명, 밀가루에 효모가 들어있는지에 대한
해설까지 프롬프트 본문에 박혀 있다. 이건 문체 프롬프트가 아니라 문체 +
도메인 지식 프롬프트다. 홀드아웃 이득의 일부가 형식이 아니라 그 내용에서
왔을 수 있으므로, 본 실행에서는 **내용 문장을 제거한 대조 조건**을 같이
돌려야 한다. 이 프로젝트가 "프롬프트로 되는 것은 방식이고 지식은 아니다"라고
적어둔 경계를 GEPA 가 스스로 넘어간 셈이라, 그 자체로도 적어둘 관찰이다.

*13차 - 네 도메인 16명으로 확정.* OpenAI 크레딧이 끊긴 동안 Stack
Exchange API(별개 쿼타, 무료)로 사이트를 둘 더 받아 진단만 돌렸다 -
diy(실용 수리)와 academia(학계). 생성 비용 0 이다.

| 특징 | MACSum | cooking | writing | diy | academia | 판정 |
|---|---|---|---|---|---|---|
| 압축률 | **165.51** | 1.72 | 0.38 | 1.70 | 0.77 | 실제 저자 **4/4 탈락** |
| 단어/답변 | 5.76 | 4.27 | 2.13 | 4.87 | 4.14 | **4/4 통과** |
| 문장/답변 | 3.07 | 4.19 | 2.23 | 6.78 | 2.24 | **4/4 통과** |
| 문장당 단어 | 1.02 | 3.27 | 1.14 | 4.10 | 9.05 | 3/4 통과 |
| 문단/답변 | 0.00 | 4.14 | 1.43 | 3.85 | 1.15 | 2/4 통과 |
| 글머리 기호 비율 | 0.00 | 0.66 | 0.97 | 1.51 | 1.77 | 0/4, 값은 상승 |
| 유보 표현/100단어 | 0.01 | 0.34 | 0.32 | 0.42 | 0.73 | 4/4 탈락 |
| 숫자/100단어 | 0.06 | 0.01 | 0.23 | 0.13 | 0.36 | 4/4 탈락 |
| 1인칭/100단어 | 0.23 | 0.33 | 0.14 | 0.35 | 0.71 | 4/4 탈락 |

**압축률 탈락은 확정이다** - 네 도메인 16명 전원에서 임계값 아래고,
MACSum 의 165.51 과는 두 자릿수 차이다. "압축률로 재라"는 결론은 요약
과제에만 해당한다.

**절대 단어 수와 문장 수는 네 도메인 모두에서 통과한다** - 저자 식별에
쓸 수 있는 핵심 두 항목이 확정됐다. 문장당 단어 수는 3/4(academia 에서
9.05 로 가장 강하다), 문단 수는 2/4 로 도메인에 따라 갈린다.

**글머리 기호는 임계값을 넘지는 못하지만 도메인을 따라 단조 증가한다**
(MACSum 0.00 → cooking 0.66 → writing 0.97 → diy 1.51 → academia 1.77).
목록을 쓸 일이 많은 분야일수록 저자 간 차이가 커진다는 뜻으로 읽히고,
표본을 늘리면 통과할 가능성이 남아 있다.

**유보 표현·숫자·1인칭 밀도는 16명에서도 전부 탈락했다.** 최댓값이
academia 의 유보 표현 0.73 과 1인칭 0.71 인데 그래도 임계값의 절반이
안 된다. **지표 기각을 확정**한다 - 코퍼스가 문제라는 변명이 더 이상
성립하지 않는다.

**모수화 선택이 완벽히 갈린다.** 학습 12개로 고르게 했을 때 MACSum
페르소나는 3/3 이 압축률, **실제 저자는 16/16 이 절대 단어 수**다.
마진(0.6)을 넣은 규칙이 도메인을 넘어 작동한다.

*14차 - GEPA 본 실행. 사람이 조립한 조건을 못 이겼고, 이유는 내 지표에
있었다.* 저자 4명 x 홀드아웃 25개, 예산 60회.

| 지표 (4명 평균) | base | stable | fewshot | **fewshot_stable** | gepa |
|---|---|---|---|---|---|
| 형식 점수 (**최적화한 지표**) | 0.376 | 0.622 | 0.523 | **0.663** | 0.649 |
| 형식 거리 (최적화 안 한 지표) | 0.233 | 0.158 | 0.201 | **0.121** | 0.235 |
| ROUGE-L (독립) | 0.154 | 0.158 | **0.161** | 0.160 | 0.150 |
| 프롬프트 (단어) | 16 | 29 | 537~1290 | 550~1303 | 330~489 |

(참조선: 저자 본인 답변의 형식 점수 평균 0.479)

**최적화한 지표에서는 GEPA 가 거의 동점이다**(0.649 대 0.663). 그런데
**최적화하지 않은 형식 거리에서는 0.235 로 base(0.233)와 같다.** 독립
지표인 ROUGE-L 은 0.150 으로 전 조건 중 최저다. 즉 GEPA 는 내가 준
지표를 올렸을 뿐 저자에게 가까워지지 않았다.

항목별로 뜯어보면 선명하다 (홀드아웃 실제 대비 평균 상대오차):

| 항목 | base | stable | fewshot | fewshot_stable | gepa |
|---|---|---|---|---|---|
| 단어 수 | 0.303 | 0.169 | 0.207 | **0.101** | 0.267 |
| 문장 수 | 0.246 | 0.207 | 0.243 | **0.157** | 0.283 |
| 문단 수 | 0.446 | 0.474 | **0.267** | 0.339 | 0.316 |
| 글머리 기호 | 2.095 | **0.696** | 1.555 | 0.750 | 1.458 |

**원인은 내가 만든 평가 함수의 오차 상한이다.** `PROFILE_ERROR_CAP = 1.0`
을 뒀는데, 목표가 0 에 가까운 항목에서는 어느 쪽으로 얼마나 벗어나도
오차가 상한에 붙어 **구분이 사라진다.** 저자의 글머리 기호 비율이 0.07
인데 GEPA 는 0.41 까지 남발했고(6배), fewshot_stable 은 0.00 을 냈다.
둘 다 상대오차 1.0 을 넘어 똑같이 상한 처리되므로 지표는 이 차이를
전혀 벌하지 못한다. **볼 수 없는 것은 최적화되지 않는다.**

상한을 넣은 이유 자체는 있었다 - 한 항목이 크게 어긋났을 때 점수가
그것만 따라가면 나머지 개선이 안 보인다. 그런데 목표가 0 근처인 항목에서는
그 상한이 항목 하나를 통째로 무력화한다. 넣은 이유와 부작용을 같이
적어둔다.

**절대 규칙 7 의 새 사례다.** "최적화에 쓴 지표만으로 보고하지 않는다"가
없었다면 이 결과를 "GEPA 가 사람 조립과 동등(0.649 대 0.663)"으로 쓰고
넘어갔을 것이다. 최적화하지 않은 지표를 나란히 놓은 덕분에 지표 게이밍이
잡혔다. specificity 축, 사용자 확신도와 같은 계열의 교훈이고, 이번에는
**내가 방금 만든 지표에서** 나왔다.

**지금 결론: 최고 조건은 여전히 `fewshot_stable` 이다** - 저자의 답변
3개 + 측정한 길이 앵커. 형식 거리 0.121 로 base 의 절반이고, 단어·문장
수 오차도 전 조건 최소다. GEPA 경로는 지표를 고쳐 다시 재야 하고, 그
전까지는 사람이 조립한 조합이 최선이다.

**혼입은 코드로 쟀고, 내가 앞서 한 말은 틀렸다.** GEPA 프롬프트에
도메인 내용이 들어간 것을 보고 "학습 자료를 베꼈다"고 적었는데, 축자
4-gram 겹침은 **0.000** 이었다(few-shot 은 0.959 로 나오므로 지표는
작동한다). GEPA 는 베낀 게 아니라 자기 말로 다시 썼다. 의미 수준을 잡는
지표를 따로 만들어(`topic_leakage` - 도메인 단어 목록을 손으로 만들지
않고, **무관한 사이트 코퍼스를 대조군으로** 써서 "이 저자 자료엔 있고
남의 도메인엔 없는 단어"를 주제 어휘로 정의한다) 다시 쟀다.

| 조건 | 축자 4-gram | 주제 어휘 |
|---|---|---|
| base | 0.000 | 0.000 |
| stable | 0.000 | 0.000 |
| fewshot | **0.959** | **0.142** |
| gepa | 0.000 | 0.031 |

오염은 실재하지만 작고(430단어 중 도메인 단어 8개), **방향이 반대다** -
실제 답변을 통째로 넣는 few-shot 이 GEPA 보다 4.6배 크다. 산출물에 주제가
묻어나는 것을 걱정한다면 문제는 GEPA 가 아니라 few-shot 쪽이다.

대조군 크기에서 한 번 틀렸다 - 40개로 쟀을 때 순수 형식 앵커가 0.105 로
나와서 일반 영어 단어를 주제 어휘로 오인했다. 세 사이트 전체(1,148개)로
키우니 0.000 이 됐다.

**중단 복구도 고쳤다.** 앞선 실행이 크레딧 소진으로 끊겼을 때 결과를
마지막에 한 번만 저장하고 있어서 끝난 저자 2명의 최적화(호출 120회,
실제 비용, 생성 캐시로 재현 불가)가 디스크에 하나도 안 남았다. 두 실행
스크립트 모두 저자마다 즉시 저장하고 중단 사유·지점을 파일에 남긴다.

*15차 - 지표를 고쳐 다시. 진단은 맞았고 결론은 그대로다.* 오차를 하드
상한 대신 항목 성격에 맞게 바꿨다 - 값 자체가 0~1 비율인 항목(글머리
기호)은 **절대 차이**로, 개수 항목은 상대오차를 `e/(1+e)` 로 눌러 담아
**끝에서 포화되지 않게** 했다. 한 항목이 점수를 독차지하지 않게 하려던
원래 목적은 유지되고, 0 근처에서 구분이 사라지는 부작용만 없앤다.

| 지표 (4명 평균) | base | stable | fewshot | **fewshot_stable** | gepa |
|---|---|---|---|---|---|
| 형식 점수 (최적화한 지표) | 0.670 | 0.848 | 0.770 | **0.869** | 0.857 |
| 형식 거리 (최적화 안 한 지표) | 0.233 | 0.158 | 0.201 | **0.121** | 0.191 |
| ROUGE-L (독립) | 0.154 | 0.158 | **0.161** | 0.160 | 0.153 |

(참조선 0.748. 형식 점수의 절대값은 오차 함수가 바뀌어 14차와 비교
불가다. 14차 결과는 `expert_gepa_cooking_cappederror.json` 에 남겼다.)

**진단이 맞았다.** GEPA 의 형식 거리가 0.235 → **0.191** 로 내려가
base(0.233)와 fewshot(0.201)을 넘어섰다. 지표의 구멍이 원인이었다는 것이
확인된다.

**항목별로 보면 무엇이 일어났는지 정확히 보인다.**

| 항목 | 지표에 있나 | base | stable | fewshot_stable | gepa(상한) | gepa(고친 후) |
|---|---|---|---|---|---|---|
| 단어 수 | 있음 | 0.303 | 0.169 | **0.101** | 0.267 | 0.190 |
| 문장 수 | 있음 | 0.246 | 0.207 | **0.157** | 0.283 | 0.218 |
| 문장당 단어 | **없음** | 0.151 | **0.098** | 0.104 | - | **0.164** |

**지표에 넣은 항목은 좋아졌고, 빼놓은 항목은 나빠졌다.** 문장당 단어
수는 `FORM_KEYS` 에는 있지만 `PROFILE_KEYS` 에는 없어서 GEPA 가 한 번도
듣지 못한 항목인데, 바로 거기서 전 조건 중 최악(0.164, base 0.151 보다도
나쁘다)이다. 같은 실패가 두 번 다른 모습으로 나온 것이다 - 상한이
글머리 기호를 안 보이게 만들었을 때는 6배 남발했고, 이번엔 문장당 단어
수가 아예 없어서 흘렀다. **최적화기는 평가 함수에 적힌 것만 지킨다.**

**그렇다고 그 항목을 지표에 넣지는 않는다.** 넣으면 `FORM_KEYS` 가
`PROFILE_KEYS` 에 완전히 포함돼서 형식 거리가 순수한 최적화 대상이 되고,
절대 규칙 7 이 요구하는 "최적화하지 않은 지표"가 사라진다. 지금은 세
항목 중 둘이 겹치고 하나가 겹치지 않는 **부분 독립** 상태이고, 그 하나가
이번 진단을 가능하게 했다. 지표를 평가에 맞춰 넓히는 것은 점수를 올리는
방법이지 결과를 믿게 만드는 방법이 아니다.

**결론은 바뀌지 않는다. 최고 조건은 `fewshot_stable` 이다** - 저자의
답변 3개 + 측정한 길이 앵커. 형식 거리 0.121 로 GEPA(0.191)보다 좋고,
비용도 비교가 안 된다.

| 조건 | 프롬프트 | 사전 비용 | 형식 거리 |
|---|---|---|---|
| stable | 29단어 | 없음 | 0.158 |
| fewshot_stable | 550~1,303단어 | 없음 | **0.121** |
| gepa | 289~486단어 | **저자당 최적화 60회** | 0.191 |

GEPA 쪽은 프롬프트가 짧다는 장점이 있지만(few-shot 의 1/3), 저자마다
최적화를 돌려야 하고 결과가 더 나쁘다. **지금 제품에 넣을 것은
`fewshot_stable` 이다.** GEPA 경로는 저자 한 명(u67, 형식 거리 0.363)이
평균을 끌어내리는 것도 걸린다 - 그 저자는 stable 에서도 유일하게 base 보다
나빴다(0.264 대 0.189). 잘 안 되는 저자가 누구인지는 따로 볼 문제다.

*16차 - "앵커를 언제 빼야 하나"를 풀려다 문제 정의가 틀렸음을 확인.*
`experiments/anchor_gating.py`. cooking u67 이 앵커 때문에 나빠졌고 그
저자만 모델 기본 출력이 이미 자기 길이였다(212단어 대 215단어). 그래서
**모델 기본값과의 격차가 작으면 앵커를 빼자**는 규칙을 세웠다. 격차는
학습 자료와 학습 과제의 기본 출력만으로 구하므로 홀드아웃을 안 보고
판정할 수 있어 실제로 쓸 수 있는 규칙이었다.

**저자 4명으로는 음성 사례가 하나뿐이라 임계값을 정하면 그 한 점에
맞추는 것**이므로 네 사이트 x 4명 = 16명으로 늘렸다.

| 예측 변수 | 이득과의 상관 (n=16) |
|---|---|
| 모델 기본값과의 격차 | **−0.031** |
| 저자의 학습 CV | +0.013 |
| 학습/홀드아웃 드리프트 | **−0.818** |

**가설 기각.** 격차는 이득을 전혀 예측하지 못한다(4명에서는 0.667 로
보였다). 해가 된 저자의 격차 0.092~0.591 과 도움이 된 저자의 0.052~0.659
가 완전히 겹치고, 임계값 스윕에서 **어떤 값도 "항상 붙임"(평균 형식 거리
0.1934)을 이기지 못한다.** 가장 나쁜 사례가 가설과 정반대다 - diy/u47125
는 격차가 0.591 로 큰데 이득 −0.604 다.

그리고 **가설의 출발점 자체가 표본의 산물이었다.** u67 의 악화는 홀드아웃
25개에서 −0.075 였는데 15개로 바꾸자 +0.000 이 됐다.

**설명하는 변수는 학습 표본이 그 저자를 얼마나 잘 대표하는가다**
(상관 −0.818). u47125 는 학습 12개 평균이 465단어인데 홀드아웃은
246단어였다(드리프트 0.890). 앵커는 그 틀린 목표를 충실히 재현했다.
다만 드리프트는 홀드아웃을 써야 계산되므로 **판정 규칙으로 쓸 수 없다** -
설명은 되고 예측은 안 된다.

**개별 저자 실패의 대부분은 평가 잡음이었다.** 드리프트가 표본 오차라는
해석을 직접 시험했다 - 음성 저자 3명의 홀드아웃을 15개에서 45개로 키웠다.

| 저자 | 홀드아웃 15 | 홀드아웃 45 |
|---|---|---|
| diy/u18078 | −0.020 | **+0.086** |
| academia/u53 | −0.026 | **+0.183** |
| diy/u47125 | −0.604 | −0.351 |
| diy/u35141 (대조) | +0.200 | +0.062 |
| academia/u40589 (대조) | +0.211 | +0.185 |

**셋 중 둘이 부호가 뒤집혔다.** 학습을 늘려도 드리프트가 거의 안 줄어든
이유도 여기 있다(8개 0.220 → 30개 0.178) - 드리프트는 잡음 있는 두 평균의
차이이고, 홀드아웃 15개 쪽 오차만으로 CV/√15 ≈ 0.13 이 깔린다.

**남은 진짜 케이스는 학습 예시를 늘리면 해결된다.** 홀드아웃을 고정하고
(`--holdout-offset`) 학습 크기만 바꿨다. base 가 양쪽에서 0.3410 으로
같은 것이 고정이 제대로 됐다는 확인이다.

| 저자 | | 학습 12 | 학습 30 |
|---|---|---|---|
| u47125 | 저자 추정 | 465단어 | **332단어** (실제 ~331) |
| | 형식 거리 | 0.485 | **0.176** |
| | 이득 | **−0.122** | **+0.187** |
| u4638 | 형식 거리 | 0.203 | **0.119** |
| | 이득 | +0.116 | **+0.200** |
| 평균 (stable) | | 0.3440 | **0.1475** |

유일한 진짜 실패가 양수로 뒤집히고, 이미 잘 되던 저자도 0.203 → 0.119 로
좋아진다. 진단 사슬이 끝까지 맞았다 - 격차는 예측 못 함 → 드리프트가
설명함 → 드리프트는 표본 오차 → 표본을 늘리면 해결.

**여기서 나오는 결론 세 가지.**

1. **게이팅은 풀 문제가 아니었다.** 앵커는 항상 붙이는 게 맞다.
   측정해서 고르는 것이 늘 옳은 것은 아니고, 무엇을 측정해도 예측력이
   없으면 고르지 않는 것이 최선이다.
2. **학습 예시는 12개가 아니라 30개를 모아야 한다.** 제품에 그대로
   반영할 수치다. 지금까지의 모든 저자별 수치는 12개 기준이므로 실제
   성능을 과소평가하고 있다.
3. **저자별 수치를 홀드아웃 15개로 읽으면 안 된다.** 이 프로젝트가
   보고해온 저자별 표에서 작은 차이는 잡음이다. 평균(base 0.2771 →
   stable 0.1934, 16명 중 12명 개선)은 신뢰할 수 있다.

3번은 절대 규칙 7·8 과 같은 계열의 교훈이라 규칙으로 올린다.

*17차 - 학습 30개 기준 최종 검증. 권고가 뒤집혔다.*
사이트 4곳 x 저자 2명 = 8명, 학습 30개, **홀드아웃 30개로 위치까지 고정**
(`--holdout-offset 30`). 규칙 11 을 방금 만들었으니 여기서부터 지킨다.

| 조건 | 형식 거리 | 개선폭 | 이긴 저자 | p값 | 프롬프트 |
|---|---|---|---|---|---|
| base | 0.308 | - | - | - | 16단어 |
| **stable** | **0.146** | +0.162 | **8/8** | **0.008** | **29단어** |
| fewshot | 0.222 | +0.085 | 7/8 | 0.070 | 1,010단어 |
| fewshot_stable | 0.149 | +0.159 | **8/8** | **0.008** | 1,022단어 |

길이 상대오차: base 0.342 / stable 0.148 / fewshot 0.210 /
**fewshot_stable 0.130**. ROUGE-L: base 0.135, 나머지 셋 모두 0.145~0.146.

**학습 30개에서는 예시가 아무것도 더해주지 않는다.** stable 0.146 대
fewshot_stable 0.149 이고 페어드로 stable 5승 3패(p=0.727) - 차이가 없다.
그런데 프롬프트는 29단어 대 1,022단어로 **35배**다.

**11차의 권고(`fewshot_stable`)를 철회한다.** 그때 예시가 24% 기여한 것은
학습이 12개여서 앵커의 목표가 부정확했기 때문이다. 예시가 그 오차를
메워주고 있었던 것이고, 학습을 30개로 늘려 목표가 정확해지면 메울 것이
없다. 같은 저자에서 방향이 보인다.

| 저자 | 학습12 stable | 학습12 +예시 | 학습30 stable | 학습30 +예시 |
|---|---|---|---|---|
| cooking/u4638 | 0.231 | **0.098** | 0.143 | **0.053** |
| cooking/u67 | 0.264 | 0.266 | **0.100** | 0.136 |

**예시와 수치는 서로 보완이 아니라 대체 관계였다** - 자료가 적을 때만
예시가 값을 한다. 11차에서 "보완"이라고 쓴 것은 학습 12개라는 조건
아래에서만 참이었고, 그 조건을 적지 않은 것이 잘못이었다.

**제품 결론: `stable` 단독을 쓴다.**

| | stable | fewshot_stable |
|---|---|---|
| 형식 거리 | 0.146 | 0.149 |
| 프롬프트 | 29단어 | 1,022단어 |
| 사용자 자료가 프롬프트에 들어가나 | **아니오** | 예 |

같은 성능에 프롬프트가 1/35 이고, **사용자의 글이 모델에 전송되지 않는다** -
앵커는 측정한 수치 29단어뿐이다. 개인 자료를 다루는 기능에서 이건
부수 효과가 아니라 설계상의 이점이다.

*18차 - 앱 연결 완료.* `expert_profile.py`(Streamlit 없는 계층) +
`app.py` 의 `_show_expert_flow()`. 입력 화면 맨 위에서 경로가 갈린다 -
"비교해서 만들기"(기존 흐름, 무수정)와 "내가 쓴 글에서 만들기"(신규).
규칙 10 대로 완전히 분리했고, 경로를 갔다 돌아와도 기존 흐름이 그대로
시작되는지 테스트로 고정했다.

**모델을 한 번도 호출하지 않는다.** 효과가 전부 코드로 잰 앵커에서
나오므로 자료를 보낼 이유가 없다. 그래서 **사용자의 글이 어디로도
전송되지 않고**, 프롬프트에 들어가는 것은 측정한 수치 29단어뿐이다.
`tests/test_expert_flow_app.py` 가 이 경로에서 generator·GEPA 함수를
호출하면 실패한다 - 나중에 "여기서 한 번만 호출하면 더 좋을 텐데" 하고
넣는 것을 막는 장치다.

**산출물이 정적 프롬프트 하나가 됐다.** 4차에서 "비율을 규칙으로 적으면
모델이 못 따르니 과제마다 계산해야 한다"고 적었는데, 그건 **압축률
모수화에만** 해당한다. 실제 저자 16명 전원이 절대 단어 수 모수화였고,
그때 목표는 새 과제와 무관하므로 앵커가 고정된다. 그대로 복사해 쓸 수
있다. 압축률이 뽑히는 사용자(요약 위주 자료)에게는 화면에서 "이 문구를
다른 과제에 그대로 쓰면 어긋난다"고 알린다 - 조용히 한 과제의 값을
박아주면 안 된다.

**화면에 재는 것과 지시하는 것을 구분해 보여준다.** 문단 수와 글머리
기호 비율은 프로필로 보여주지만 프롬프트에는 넣지 않는다. 넣으면 문단은
맞지만(오차 1.90 → 0.88) 길이·문장이 나빠지기 때문이고(형식 거리
0.158 → 0.256), 그 상충을 접기(expander)에 수치로 적어 사용자가 고를 수
있게 뒀다. 기본값은 저자 8명에게 검증된 조건(길이·문장만)이다.

**자료가 부족하면 계산해주지 않는다.** 5개 미만은 거부하고, 30개 미만은
계산하되 경고한다. 부족한데 조용히 보여주면 사용자가 그 수치를 믿는다 -
실측에서 12개 추정이 실제와 40% 어긋난 저자가 있었다.

**배포 쪽 결함 하나를 같이 고쳤다.** `scripts/bump_build_marker.py` 의
감시 대상에 `agents` 가 없었다. app.py 가 `agents/expert_onboarding.py` 를
import 하는데 그 파일만 고친 푸시는 프로세스를 재시작시키지 않아 옛
모듈이 배포에 남는다 - **앱을 두 번 내려앉힌 바로 그 경로다.** 추가했다.

**기존 앱 테스트의 순서 의존도 고쳤다.** 라디오를 `radio[0]` 으로 집고
있어서 위젯을 하나 추가하자 4개 파일 9개 테스트가 깨졌다. 두 라디오에
`key` 를 주고 테스트가 key 로 집게 했다.

**남은 것**: 실사용자 응답 수집(`feedback.py`)은 비교 경로에만 붙어
있다. 전문가 경로에도 붙여야 이 기능의 만족도를 알 수 있다. 자료가
12개 수준인 사용자에게 예시를 함께 넣는 경로도 아직 화면에 없다 -
그 구간에서는 예시가 값을 한다(0.121 대 0.158).

---

## 발표 서술 방향

핵심 문장: "선택 기반 개인화는 작동한다. 다만 모든 취향 축이 측정
가능한 것은 아니며, 어떤 축이 그러한지를 실험으로 구분했다." 성공
(개인화·수렴·이식성·피드백 효과)과 한계(specificity·에이전트의 축
발견)를 같은 비중으로 놓고, 한계에서 무엇을 알게 됐는지를 말하는
구성이다.

강조할 것:
- 실험 과정에서 발견하고 고친 함정들(페르소나 오라클 재설계로
  완전복원율 0~8% → 80%). 실제로 실험을 굴려본 팀만 쓸 수 있는 내용이다.
- 도메인·언어 양쪽에 대한 이식성 실증.
- 무엇을 가져다 쓰고 무엇을 직접 만들었는지의 명시적 구분.

피할 것:
- 산출물이 "범용적으로 더 좋은 프롬프트"라는 주장.
  목표는 개인 적합도이며, 타인에게 맞지 않는 것이 정상이다.
- 순환 논증으로 보이는 단일 지표 보고.
