# pytest 설정. 마커를 등록해두면 오타 난 마커가 경고로 잡히고,
# 무엇이 실제 API를 쓰는지 한눈에 구분된다.
[pytest]
testpaths = tests
markers =
    api: 실제 모델 API를 호출한다. OPENAI_API_KEY 가 없으면 자동으로 건너뛴다.
    macsum: data/macsum 데이터가 필요하다. 없으면 자동으로 건너뛴다.

# API를 쓰지 않는 테스트만 돌리려면:
#   python -m pytest -m "not api"
