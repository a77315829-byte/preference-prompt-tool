"""수행계획서 39면 pptx 생성.

    python build_plan.py <출력경로>

수치는 전부 저장소의 커밋된 결과에서 재계산한 값이다.
"""

from __future__ import annotations

import sys

from deckkit import (ACCENT, FS_SM, FS_XS, INK, INK2, INK3, MONO, OK, WARN,
                     Bars, Cols, Deck, Flow, Gantt, Note, Stats, Table, stack)

AC = {"color": ACCENT, "bold": True}
BD = {"bold": True, "color": INK}
MN = {"font": MONO, "color": INK}
MNB = {"font": MONO, "color": INK, "bold": True}
HI = {"font": MONO, "color": ACCENT, "bold": True}
OKT = {"color": OK, "bold": True, "font": MONO}
WNT = {"color": WARN, "bold": True, "font": MONO}
RDT = {"color": ACCENT, "bold": True, "font": MONO}

CH1 = "1. 추진 배경 및 목표"
CH2 = "2. 수행 범위"
CH3 = "3. 수행 방안 및 일정"
CH4 = "4. 추진 조직"
CH5 = "5. 프로젝트 관리"

d = Deck()

# ══ 01 표지 ═══════════════════════════════════════════════════════════
d.cover(
    "CAPSTONE DESIGN · 수행계획서",
    "선호 기반 프롬프트\n자동 생성 도구",
    '"원하는 것을 서술하세요"를 "이 둘 중 나은 쪽을 고르세요"로 바꾼다.\n'
    "8회의 비교 선택에서 암묵적 선호를 추정해 개인 전용 시스템 프롬프트를 생성한다.",
    [("문서 구분", "수행계획서\n(Project Plan)"), ("작성일", "2026. 09. 18."),
     ("수행 기간", "15주 / 3인"), ("현재 단계", "v2 완료 · 예선 제출 준비")],
)

# ══ 02 목차 ═══════════════════════════════════════════════════════════
s, top = d.page("AGENDA", "목차",
                [("성공과 한계를 ", {}), ("같은 비중", AC),
                 ("으로 놓는다. 유리한 지표만 골라 보고하지 않는다.", {})], "02")
stack(d, s, [Cols([
    [Note("layer", [("1. 추진 배경 및 목표", BD), ("     P.04–08", {"font": MONO, "color": INK3})],
          "개인화 방식의 한계 · 문제 정의 · 정량 목표 · 기술 계보 · 기대효과"),
     Note("layer", [("2. 수행 범위", BD), ("     P.10–19", {"font": MONO, "color": INK3})],
          "시스템 개요 · 기능/비기능 요건 · 아키텍처 · 핵심 알고리즘 3종 · "
          "전문가 경로 · 검증 범위"),
     Note("layer", [("3. 수행 방안 및 일정", BD), ("     P.21–30", {"font": MONO, "color": INK3})],
          "측정 우선 방법론 · 확보된 결과 5건 · 한계와 음성 결과 · 단계별 계획 · 15주 일정")],
    [Note("layer", [("4. 추진 조직", BD), ("     P.32–33", {"font": MONO, "color": INK3})],
          "3인 역할 분담 · 공수 산정 · 협업 규칙"),
     Note("layer", [("5. 프로젝트 관리", BD), ("     P.35–38", {"font": MONO, "color": INK3})],
          "위험 관리 · 품질 관리 · 산출물 · 데이터 출처 · 개인정보 보호"),
     Note("layer", [("부록", BD), ("     P.39–41", {"font": MONO, "color": INK3})],
          "검사 함수 대 사람 라벨 · 예상 질문과 근거"),
     Note("red", "이 문서의 원칙",
          "본 계획서에 적힌 모든 수치는 저장소에 커밋된 실험 결과에서 재계산한 값이다. "
          "기각된 가설과 되돌린 기능도 성과와 같은 분량으로 기재한다.")],
])], top, label="02")

# ══ 03 장 표지 1 ══════════════════════════════════════════════════════
d.chapter(CH1, "01", "추진 배경 및 목표",
          ["1-1 배경", "1-2 문제 정의", "1-3 목표 및 정량 지표",
           "1-4 기술 계보와 차별점", "1-5 기대효과"], "03")

# ══ 04 배경 ═══════════════════════════════════════════════════════════
s, top = d.page(CH1, '1-1. 배경 — 개인화는 "서술"에 의존한다',
                [("지금의 개인화 수단은 전부 ", {}),
                 ("사용자가 자기 취향을 글로 설명할 수 있다", AC),
                 ("고 가정한다.", {})], "04")
stack(d, s, [
    Cols([
        [Note("layer", "① 커스텀 인스트럭션",
              '사용자가 "간결하게", "전문적으로" 같은 문구를 직접 작성한다. '
              "추상적 형용사가 실제로 어떤 출력이 되는지는 사용자가 알 수 없다. "
              "본 실험의 비교군 B에 해당한다.")],
        [Note("layer", "② 프롬프트 생성 도구",
              '"어떤 프롬프트를 원하냐"를 다시 묻는다. 질문의 형태만 바뀌고 '
              "서술 부담은 그대로다.")],
        [Note("layer", "③ 파인튜닝 · 선호 정렬",
              "선호 데이터로 모델 자체를 바꾼다. 개인 단위로는 데이터량·비용·"
              "접근성이 모두 성립하지 않는다.")],
    ]),
    Note("note", "이 가정이 깨지는 지점",
         [("사람은 ", {}),
          ("두 결과물을 비교하면 즉시 고를 수 있지만, 무엇을 기준으로 골랐는지 "
           "서술하지 못한다.", BD),
          (" 실험에서 비교군 B(직접 작성한 지침)는 독립 지표 ROUGE-L ", {}),
          ("0.168", MN), ("에 머물렀고, 같은 페르소나에서 비교 선택으로 조립한 "
                          "프롬프트는 ", {}), ("0.207", HI),
          ("에 도달했다. 서술 능력이 아니라 ", {}), ("판별 능력", BD),
          ("을 입력으로 쓰면 더 나은 결과가 나온다는 뜻이다.", {})], size=FS_SM),
], top, label="04")

# ══ 05 문제 정의 ══════════════════════════════════════════════════════
s, top = d.page(CH1, "1-2. 문제 정의 — 무엇을 입력으로 받는가",
                [("입력을 ", {}), ("서술", AC), ("에서 ", {}), ("선택", AC),
                 ("으로 바꾸고, 그 선택을 기계가 읽을 수 있는 평가 함수로 번역한다.", {})],
                "05")
stack(d, s, [
    Flow([
        ("비교 선택", "같은 과제, 다른 설정으로 만든 결과물 A·B 중 하나를 고른다. 8회 반복."),
        ("선호 추정", "Bradley-Terry 기반으로 축별 선호값과 상대 확신도를 추정한다."),
        ("평가 함수 조립", "추정된 선호를 점수 + 자연어 피드백을 내는 함수로 변환한다. 본 과제의 핵심 기여."),
        ("프롬프트 최적화 (선택)", "GEPA가 그 함수를 목표로 개선한다. 대표 수치(3-3)는 이 단계 없이 시드 템플릿만으로 낸 값이다."),
        ("산출물", "그 사용자 전용 시스템 프롬프트 텍스트 한 덩어리."),
    ], core=2),
    Cols([
        [Note("ok", "이 과제가 하는 것", bullets=[
            [("상용 API를 ", {}), ("부품으로 호출", BD), ("해 조합한다", {})],
            "개인 적합도를 목표로 한다",
            "어떤 취향 축이 측정 가능한지를 실험으로 구분한다",
        ])],
        [Note("warn", "이 과제가 하지 않는 것", bullets=[
            [("모델을 학습하거나 파인튜닝하지 ", {}), ("않는다", BD)],
            '"범용적으로 좋은 프롬프트"를 목표로 하지 않는다',
            [("산출물이 ", {}), ("모든 사람에게 잘 맞으면 개인화 실패", BD), ("로 본다", {})],
        ])],
    ]),
], top, label="05")

# ══ 06 목표 및 정량 지표 ══════════════════════════════════════════════
s, top = d.page(CH1, "1-3. 목표 및 정량 지표",
                [('완성도의 기준은 "동작하는 도구"가 아니라 ', {}),
                 ("측정된 결과", AC), ("다.", {})], "06")
stack(d, s, [
    Table(["구분", "목표 지표", "목표", "현재 실측", "판정"],
          [["개인화 효과", "독립 지표(ROUGE-L)에서 직접 작성 지침(B) 초과", "> 0.168",
            (["0.207 (4/5)"], HI), (["경향"], WNT)],
           ["선호 복원", "숨긴 축별 선호의 완전복원율 (5문서×5시드)", "≥ 70%",
            (["80%"], HI), (["달성"], OKT)],
           ["수렴 효율", "2축 완전 도달까지의 비교 횟수", "≤ 10회",
            (["8~9회"], HI), (["달성"], OKT)],
           ["구조적 기여", "구조화 피드백의 최적화 효과 (동일 예산 40회, 최적화 지표)", "유의한 차이",
            (["0.98 vs 0.44"], HI), (["부분"], WNT)],
           ["확장성", "engine/ 무수정으로 동작하는 카테고리 수", "≥ 2",
            (["5"], HI), (["달성"], OKT)],
           ["언어 이식성", "한국어 도메인 완주 및 검사 함수 스팟 체크", "기능 검증",
            "94% / 40%", (["달성"], OKT)],
           ["형식 복제", "실제 저자 형식 거리 (저자 8명 × 홀드아웃 30)", "유의한 감소",
            (["0.308→0.146"], HI), (["달성"], OKT)],
           ["실사용자 검증", "공개 링크 방문자 만족 응답", "3~5인", "수집 중",
            (["진행"], WNT)],
           ["측정 불가 축 복원", "하이브리드 판정으로 specificity 축 복원", "≥ 0.70",
            "0.558", (["기각"], RDT)],
           ["사람 축 재발견", "온보딩 에이전트가 사람이 고른 축과 일치", "|r| ≥ 0.7",
            "0.453", (["기각"], RDT)]],
          [1.3, 3.6, 1.0, 1.15, 0.7],
          caption="목표 지표 및 달성 현황", aligns=["l", "l", "r", "r", "l"],
          size=FS_XS),
    Note("note", None,
         [("판정을 낮춰 적은 두 칸: ", BD),
          ("개인화 효과는 차이 0.039가 표준편차 안이라 페어드 4/5승만 말하고 유의성은 "
           "주장하지 않는다(3-3). 구조적 기여의 0.98은 최적화 목표 지표로 잰 값이라 "
           "규칙 7에 따라 독립 검증 전까지 부분 달성이다(3-2, 잔여 과제 2). ", {}),
          ("기각된 두 항목은 지우지 않고 왜 실패했는지를 결과로 보고한다.", BD)],
         size=FS_XS),
], top, label="06")

# ══ 07 기술 계보 ══════════════════════════════════════════════════════
s, top = d.page(CH1, "1-4. 기술 계보와 차별점",
                [("가져다 쓴 것과 직접 만든 것을 ", {}), ("명시적으로 구분", AC),
                 ("한다.", {})], "07")
stack(d, s, [Cols([
    [Note("note", "가져다 쓴 것 (기성 부품)", bullets=[
        [("GEPA", BD), (" — 반성적 프롬프트 진화 (Agrawal et al., ICLR 2026)", {})],
        [("DSPy", BD), (" — 자동 프롬프트 최적화 프레임워크", {})],
        [("LiteLLM", BD), (" — 다중 모델 API 통합 인터페이스", {})],
        [("MACSum", BD), (" — 혼합 속성 제어 요약 데이터셋 (Zhang et al., TACL 2023)", {})],
        [("rouge-score", BD), (" — 독립 채점용 ROUGE-L (LCS 기반)", {})],
        [("Kiwi", BD), (" — 한국어 형태소 분석", {})],
     ]),
     Note("note", None,
          [("기성 도구를 쓰는 것이 감점 요인이 아니라, ", {}),
           ("어디까지가 기성품인지 명시하지 않는 것", BD),
           ("이 감점 요인이다. 본 계획서는 모든 결과에 대해 어느 부품이 "
            "기여했는지를 함께 적는다.", {})])],
    [Note("red", "직접 만든 것 (라이브러리로 대체 불가)", bullets=[
        [("selector.py", MNB), (" — 다음에 보여줄 쌍 선택. 불확실도 기반 능동 질의", {})],
        [("estimator.py", MNB), (" — 선택 이력 → 축별 선호 + 확신도", {})],
        [("metric_builder.py", MNB), (" — 선호 → GEPA용 평가 함수 조립", BD)],
        [("domain_onboarding.py", MNB), (" — 축 제안 → 검사 코드 작성 → 판별력 실측 → 재시도", {})],
        [("expert_onboarding.py", MNB), (" — 저자 코퍼스에서 형식 앵커 추출", {})],
     ]),
     Note("ok", "차별점 — 구조화 피드백",
          [("GEPA는 점수만이 아니라 자연어 피드백도 받는다. 본 도구는 축을 명시적으로 "
            "갖고 있으므로 ", {}),
           ('"길이 위반: 6문장(목표 3문장 이하). 원문 수치 3개 중 1개 누락."', MN),
           (" 같은 피드백을 낼 수 있다. ", {}),
           ("범용 최적화 도구가 할 수 없는 부분이고, 효과를 어블레이션으로 "
            "실증했다(3-2).", BD)])],
])], top, label="07")

# ══ 08 기대효과 ═══════════════════════════════════════════════════════
s, top = d.page(CH1, "1-5. 기대효과",
                [("도구 하나가 아니라, ", {}),
                 ("어떤 취향이 측정 가능한지에 대한 지도", AC),
                 ("를 함께 남긴다.", {})], "08")
stack(d, s, [
    Stats([("8회", "사용자가 부담하는 전체 작업량.\n서술 없이 클릭만으로 완료"),
           ("5", "엔진 무수정으로 동작하는 카테고리\n(요약 영·한 / 코딩 / 리뷰 / 이메일)"),
           ("0회", "전문가 경로의 API 호출 수.\n사용자 글이 외부로 전송되지 않는다"),
           ("242", "API 없이 통과하는 회귀 테스트\n(착수 시점 16개)")]),
    Cols([
        [Note("layer", "정성적 효과 ① 진입 장벽",
              "프롬프트 작성법을 모르는 사용자가 자기 취향에 맞는 프롬프트를 얻는다. "
              "입력은 두 결과물 중 하나를 고르는 것뿐이다.")],
        [Note("layer", "정성적 효과 ② 방법론 자산",
              [("축 하나를 채택하기 전에 ", {}), ("판별력을 먼저 측정", BD),
               ("하는 절차를 확립했다. specificity 축을 5가지 방법으로 시도해 "
                "전부 실패한 기록이 그 절차의 근거다.", {})])],
        [Note("layer", "정성적 효과 ③ 재현 가능한 실패 기록",
              "폐기한 중간 결론 3건과 되돌린 기능 1건을 경과 그대로 남겼다. "
              "실제로 실험을 굴린 팀만 쓸 수 있는 내용이다.")],
    ]),
    Note("red", "가장 방어 가능한 단일 주장",
         [('"선택 기반 개인화는 작동한다. 다만 모든 취향 축이 측정 가능한 것은 '
           '아니며, 어떤 축이 그러한지를 실험으로 구분했다."', BD)], size=FS_SM),
], top, label="08")

# ══ 09 장 표지 2 ══════════════════════════════════════════════════════
d.chapter(CH2, "02", "수행 범위",
          ["2-1 시스템 개요", "2-2~3 기능 요구사항", "2-4 비기능 요구사항",
           "2-5 아키텍처", "2-6~8 핵심 알고리즘", "2-9 전문가 경로",
           "2-10 검증 범위"], "09")

# ══ 10 시스템 개요 ════════════════════════════════════════════════════
s, top = d.page(CH2, "2-1. 시스템 개요 — 두 개의 입력 경로",
                [("같은 산출물에 도달하는 ", {}), ("서로 다른 두 경로", AC),
                 ("를 제공한다.", {})], "10")
stack(d, s, [Cols([
    [Note("red", [("경로 A — 비교 선택 (주 경로)", BD),
                  ("        API 사용", {"font": MONO, "color": ACCENT})],
          [("입력: ", BD), ("과제 카테고리 선택 + 원문 입력 + 8회 비교 클릭\n", {}),
           ("처리: ", BD),
           ("라운드마다 축 조합이 다른 후보 2개를 동시 생성 → 선택 이력 누적 → "
            "축별 선호 추정 → 평가 함수 조립 → GEPA 최적화\n", {}),
           ("산출: ", BD),
           ("시스템 프롬프트 + 추정된 축별 선호 + 갈린 비교 수 게이지", {})]),
     Note("note", "왜 8회인가",
          [("수렴 곡선 실측에서 불확실도 기반 선택이 ", {}), ("8~9", MN),
           ("라운드에 2축 완전 도달했다. 그 이상 늘렸을 때의 실측은 없다 — 인접한 값"
            "(normal/long)을 더 가를 수 있는지는 미확인이다(3-4). 라운드 수는 ", {}),
           ("service.TOTAL_ROUNDS", MN),
           (" 한 곳에서 관리한다.", {})])],
    [Note("ok", [("경로 B — 전문가 자료 (보조 경로)", BD),
                 ("        API 0회", {"font": MONO, "color": OK})],
          [("입력: ", BD),
           ("자기가 쓴 답변 뭉치를 구분선으로 나눠 붙여넣기. 권장 30개 이상\n", {}),
           ("처리: ", BD),
           ("전부 파이썬 코드로 계측. 단어·문장·문단·글머리 기호 비율을 재고, "
            "절대 단어 수와 압축률 중 ", {}), ("변동계수가 작은 쪽", BD),
           ("을 길이 모수화로 선택\n", {}), ("산출: ", BD),
           ("29단어 형식 앵커가 붙은 프롬프트 + 측정된 형식 프로필", {})]),
     Note("note", "경로 B의 설계상 이점",
          [("효과가 전부 코드 계측에서 나오므로 ", {}),
           ("사용자의 글이 어디로도 전송되지 않는다.", BD),
           (" 같은 자료를 예시로 프롬프트에 넣어봤을 때 형식 거리는 ", {}),
           ("0.146 → 0.149", MN), ("로 차이가 없었다(페어드 ", {}),
           ("p=0.727", MN), ("). 보낼 이유가 없다.", {})])],
])], top, label="10")

# ══ 11 기능 요구사항 ① ═══════════════════════════════════════════════
s, top = d.page(CH2, "2-2. 기능 요구사항 ① 핵심 기능",
                [("별표(", {}), ("★", AC),
                 (") 항목은 라이브러리로 대체할 수 없는 부분이며, 공수의 대부분이 "
                  "여기에 들어간다.", {})], "11")
stack(d, s, [
    Table(["ID", "기능", "상세", "모듈", "상태"],
          [["FR-01", "도메인 정의 로드", "축 이름·값·문구·검사 함수 지정을 YAML에서 읽고 스키마 검증",
            "domain_loader", (["완료"], OKT)],
           ["FR-02", "후보 생성 및 캐싱", "캐시 키는 조립된 프롬프트 텍스트 자체 — 문구를 고치면 무효화된다",
            "generator", (["완료"], OKT)],
           ["FR-03", "후보 2개 동시 생성", "순서 보존 필수. 뒤바뀌면 사용자가 고른 것과 다른 축을 학습한다",
            "generator", (["완료"], OKT)],
           ["FR-04 ★", "쌍 선택 알고리즘", "무작위 / 축 순차 / 불확실도 3종. 확신한 축은 다시 묻지 않는다",
            "selector", (["완료"], OKT)],
           ["FR-05 ★", "선호 추정", "Bradley-Terry 선호값 + 정규화 엔트로피 확신도 (축 간 상대 비교용)",
            "estimator", (["완료"], OKT)],
           ["FR-06 ★", "평가 함수 조립", "선호 → (점수, 자연어 피드백) 함수. 축 가중치는 확신도에서 유도",
            "metric_builder", (["완료"], OKT)],
           ["FR-07", "GEPA 연동", "시드·trainset/valset·metric 전달. 호출 예산 상한 고정",
            "run_gepa", (["완료"], OKT)],
           ["FR-08", "데모 모드", "API 키 없이 규칙 기반 후보로 8회 완주. 축을 실제로 구현한다",
            "demos/", (["완료"], OKT)],
           ["FR-09 ★", "전문가 형식 앵커", "답변 계측 → 길이 모수화 자동 선택 → 29단어 앵커. API 0회",
            "expert_onboarding", (["완료"], OKT)],
           ["FR-10 ★", "도메인 온보딩 에이전트", "축 제안 → 코드 작성 → 샌드박스 판별력 측정 → 재시도(최대 2회)",
            "domain_onboarding", (["MVP"], WNT)],
           ["FR-11", "하이브리드 판정 라우팅", "코드 → 소형 → 대형. 확신도는 단일 토큰 logprob 소프트맥스",
            "checks/*_hybrid", (["기각"], RDT)]],
          [0.75, 1.75, 5.2, 1.75, 0.6],
          caption="핵심 기능 요구사항", size=FS_XS),
], top, label="11")

# ══ 12 기능 요구사항 ② ═══════════════════════════════════════════════
s, top = d.page(CH2, "2-3. 기능 요구사항 ② 운영 · 검증 기능",
                [("공개 링크로 심사받는 과제이므로, ", {}), ("링크가 죽지 않는 것", AC),
                 ("이 기능 요구사항이다.", {})], "12")
stack(d, s, [
    Table(["ID", "기능", "상세 및 설계 근거", "상태"],
          [["FR-12", "지출 상한 이중화",
            "세션당 3회 + 앱이 공유하는 하루 60회. 세션 상한만으로는 브라우저를 새로 열면 "
            "초기화돼 공개 링크에서 방어가 안 된다. 확인과 차감을 한 락 안에서 수행 — "
            "나누면 동시 방문자가 마지막 한 칸을 같이 가져간다", (["완료"], OKT)],
           ["FR-13", "배포 감시",
            "6시간마다 실제 브라우저로 열어 화면이 그려지는지 확인, 실패 시 메일 통보. "
            "마커는 DOM 속성이 아니라 화면에 보이는 문구를 쓴다 — 예전에 입력란의 maxlength를 "
            "마커로 썼다가 프레임워크가 DOM에 찍지 않아 낭패를 봤다", (["완료"], OKT)],
           ["FR-14", "배포 갱신 강제",
            "코드만 푸시하면 이미 import된 모듈이 갱신되지 않아 구버전이 남는다. 의존성 파일의 "
            "빌드 마커를 올려 프로세스 재시작을 강제하고 감시 대상 디렉터리를 명시", (["완료"], OKT)],
           ["FR-15", "실사용자 응답 수집",
            "결과 화면 끝의 예/아니오 한 줄 질문. 저장소 없이 표준 출력 한 줄 JSON(의존성 0개). "
            "원문과 생성 결과는 기록하지 않는다. api / demo / expert 모드를 합치지 않는다 — "
            "서로 다른 것을 평가하므로 합치면 아무것도 말하지 못한다", (["수집 중"], WNT)],
           ["FR-16", "결과 재현 검사",
            "문서의 수치를 커밋된 결과 파일에서 다시 계산해 일치를 확인. 리뷰 중 실제로 두 번 "
            "어긋났기 때문에(%p 혼용, 표본 5개 평균만으로 우위 주장) 코드로 막는다", (["완료"], OKT)],
           ["FR-17", "생성 코드 안전 실행",
            "AST 정적 검증(import 전면 금지, 던더 이름·속성 패턴 차단) + 프로세스 격리 + 타임아웃. "
            "개별 위험 이름을 나열하는 대신 구조적으로 막아 샌드박스 탈출 경로까지 차단", (["완료"], OKT)],
           ["FR-18", "결과 중간 저장",
            "장시간 유료 실행은 항목 단위로 즉시 저장하고 어디서 왜 멈췄는지를 함께 기록. 실제로 "
            "크레딧 소진으로 중단돼 두 항목의 유료 결과를 잃은 사고가 있었다", (["완료"], OKT)]],
          [0.75, 2.0, 8.3, 0.85],
          caption="운영 및 검증 기능 요구사항", size=FS_XS),
], top, label="12")

# ══ 13 비기능 요구사항 ════════════════════════════════════════════════
s, top = d.page(CH2, "2-4. 비기능 요구사항",
                [("비용과 응답 시간은 ", {}), ("측정해서 관리", AC),
                 ("한다. 추정치로 두지 않는다.", {})], "13")
stack(d, s, [Cols([
    [Table(["조건", "라운드당", "8회 완주"],
           [["배포, 순차 호출 (이전)", "20.3초", "약 160초"],
            ["배포, 동시 호출 (현재)", (["4.2초"], HI), (["약 34초"], HI)],
            ["로컬 통제, 순차", "7.8초", "약 62초"],
            ["로컬 통제, 동시", "2.9초", "약 23초"]],
           [2.4, 1.0, 1.1], caption="성능 — 응답 시간 실측",
           aligns=["l", "r", "r"]),
     Note("note", None,
          [("배포 수치의 개선폭이 더 큰 이유는 이전 측정에 콜드스타트가 섞여 "
            "있었기 때문이다. 동시 생성만의 순효과는 ", {}),
           ("같은 조건에서 잰 로컬 비교의 63%", BD),
           ("로 본다 (캐시를 타지 않도록 매번 새 원문, 3회 측정).", {})])],
    [Table(["항목", "기준"],
           [["비용", "동일 (원문, 축조합) 쌍은 두 번 호출하지 않는다. 후보 생성은 소형 모델, GEPA 성찰만 강한 모델"],
            ["재현성", "비교 실험에는 temperature=0과 고정 시드. 고정 전에는 같은 페르소나 2회 실행이 277단어와 104단어로 갈려 노이즈를 결과로 읽었다"],
            ["가용성", "공개 링크 상시 접속. 12시간 무트래픽 절전은 감수하고, 아예 안 뜨는 상태를 빨리 아는 것을 목표로 한다"],
            ["보안", "API 키는 환경 파일에서만 읽고 저장소에 커밋하지 않는다"],
            ["개인정보", "사용자 원문·생성 결과·자기 자료를 저장하지 않는다. 전문가 경로는 외부 전송 자체가 없다"],
            ["이식성", "서버형 DB를 쓰지 않는다. 상태는 JSON 파일 또는 세션 내에만 둔다"],
            ["접근성", "420px 폭에서 8회 완주 가능. 무료 티어 메모리 한도 안에서 동작"]],
           [1.0, 4.6], caption="비기능 요구사항", size=FS_XS)],
], weights=[1, 1.15])], top, label="13")

# ══ 14 아키텍처 ═══════════════════════════════════════════════════════
s, top = d.page(CH2, "2-5. 아키텍처 — 도메인을 모르는 엔진",
                [("엔진 코드에 도메인 특정 단어가 등장하지 않는다.", AC),
                 (" 이 제약이 확장성 주장의 실체다.", {})], "14")
stack(d, s, [Cols([
    [Note("layer", [("표현 계층", BD), ("        app.py", {"font": MONO, "color": INK3})],
          "화면 조립만 담당. 3단계(카테고리 선택 → 비교 → 결과)"),
     Note("layer", [("순수 서비스 계층", BD),
                    ("        service.py · theme.py · expert_profile.py",
                     {"font": MONO, "color": INK3})],
          [("UI 프레임워크를 import하지 않는다. 세션 상태는 선택 이력만 보관하고 "
            "나머지는 이력에서 재생성한다. ", {}),
           ("UI 없이 그대로 단위 테스트할 수 있다.", BD)]),
     Note("red", [("엔진 계층 (도메인 무지)", BD),
                  ("        팀의 핵심 기여", {"font": MONO, "color": ACCENT})],
          [('"축이 N개 있고 각 축에 값이 M개 있다"까지만 안다. 축 이름, 축 값, '
            "프롬프트 문구, 검사 함수 지정은 전부 도메인 정의 파일에서 읽는다. ", {}),
           ("이 규칙을 어기면 도메인 추가 시 엔진을 다시 짜야 한다.", BD)]),
     Note("layer", [("도메인 계층 (교체 가능)", BD),
                    ("        domains/ · checks/ · demos/",
                     {"font": MONO, "color": INK3})],
          "과제 카테고리 5종. 새 카테고리는 이 계층에만 파일을 추가한다"),
     Note("layer", [("에이전트 계층 (분리)", BD),
                    ("        agents/", {"font": MONO, "color": INK3})],
          "축 발견 · 전문가 형식 추출. 기존 파이프라인과 완전히 분리되어 실패해도 "
          "확보된 결과를 훼손하지 않는다")],
    [Note("red", "검증 방법",
          [("완전히 다른 도메인(이메일 초안, 축 3개: 길이·격식·구조)을 정의 파일만 "
            "추가해 붙였다. ", {}), ("엔진 코드는 한 줄도 고치지 않았다.", BD)]),
     Note("note", "남아 있던 구멍도 메웠다",
          [("전에는 새 도메인을 앱에 노출하려면 데모 생성기를 손으로 써야 했다. "
            "지금은 여러 도메인이 공유하는 축(길이·감정·격식·구조)을 실제로 구현한 ", {}),
           ("공용 데모 생성기", BD),
           ("가 있고, 전용 모듈이 없으면 라우터가 여기로 내려온다.", {})]),
     Note("warn", "축을 흉내만 내면 안 된다",
          "데모 후보가 아무렇게나 다른 텍스트를 내놓으면 사용자의 선택이 축과 "
          "무관해지고 추정된 선호가 의미를 잃는다. 데모 생성기도 축을 실제로 구현한다.")],
], weights=[1.55, 1])], top, label="14")

# ══ 15 알고리즘 ① ════════════════════════════════════════════════════
s, top = d.page(CH2, "2-6. 핵심 알고리즘 ① 쌍 선택",
                [("같은 질문을 반복하지 않는 것이 목표다. ", {}),
                 ("이미 확신한 축은 묻지 않는다.", AC)], "15")
stack(d, s, [Cols([
    [Note("layer", "무작위", "축 조합을 임의로 뽑는다. 중복 질문이 다발하고 수렴이 느리다"),
     Note("layer", "축 순차 질의",
          "축을 하나씩 순서대로 가른다. 질문 수는 적으나 축 간 상호작용을 포착하지 못한다"),
     Note("red", "불확실도 기반 (채택)",
          "현재 추정에서 가장 불확실한 축을 가르는 쌍을 우선 제시한다"),
     Note("note", None,
          "세 방식을 모두 구현해 비교 결과를 발표의 핵심 그래프로 쓴다. 비교 없이 "
          '하나만 만들면 "왜 그 방식인가"에 답할 근거가 없다.')],
    [Table(["방식", "도달 축수", "완전복원", "비고"],
           [["무작위", "1.3 / 2", "32%", "10라운드까지 정체. 15라운드에도 유지 못 함(평균 1.8/2)"],
            ["축 순차 질의", "1.8 / 2", "80%", "복원 후 되돌아가지 않는다"],
            ["불확실도 기반", (["2.0 / 2"], HI), (["80%"], HI),
             (["8~9라운드에 완전 도달"], BD)]],
           [1.3, 1.0, 0.95, 4.0],
           caption="수렴 실측 — 5문서 × 5시드 평균, 2축 기준",
           aligns=["l", "r", "r", "l"], size=FS_XS),
     Note("warn", "정직하게 적어야 하는 부분",
          [("불확실도 방식이 순차보다 ", {}), ("빠르지는 않다.", BD),
           (" 실측 곡선에서 둘 다 6라운드에 복원하고 완전복원율도 같은 80%다. "
            '그래서 회귀 테스트에 "불확실도가 더 빠르다"를 박지 않았고, 대신 ', {}),
           ("둘 다 복원 후 되돌아가지 않는 것", BD), ("과 ", {}),
           ("무작위는 유지하지 못하는 것", BD),
           ("만 고정했다. 불확실도 방식의 이점은 속도가 아니라 최종 도달 "
            "축수(2.0 대 1.8)다.", {})])],
], weights=[1, 1.5])], top, label="15")

# ══ 16 알고리즘 ② ════════════════════════════════════════════════════
s, top = d.page(CH2, "2-7. 핵심 알고리즘 ② 선호 추정과 확신도",
                [("내부 계산에 쓰이던 수치가 ", {}),
                 ("사용자에게 보여줄 만큼 의미 있는지는 따로 재봐야 한다.", AC)],
                "16")
stack(d, s, [Cols([
    [Note("note", "추정 방식",
          [("선택 이력을 Bradley-Terry 쌍대 비교로 보고 축별 선호값을 추정한다. "
            "확신도는 축별 선호 분포의 정규화 엔트로피에서 유도하고, ", {}),
           ("평가 함수의 축 가중치", BD),
           ("로 쓴다 — 확신이 큰 축에 큰 가중치를 준다.", {})]),
     Table(["카테고리 (축 값 수)", "8회", "12회", "16회", "24회", "복원"],
           [["요약 한국어 (3값)", "0.06", "0.06", "0.06", "0.06", (["9/9"], HI)],
            ["코딩 (2값)", "0.18", "0.28", "0.37", "0.49", (["8/8"], HI)]],
           [2.2, 0.7, 0.7, 0.7, 0.7, 0.8],
           caption="확신도 보정 실측 — 완벽히 일관된 가상 사용자",
           aligns=["l", "r", "r", "r", "r", "r"], size=FS_XS)],
    [Note("red", "시도했으나 되돌린 기능 — 확신도 표시",
          [("결과 화면에 확신도를 띄우려 했으나 ", {}), ("철회했다.", BD),
           (" 3값 축에서는 비교를 24회까지 늘려도 ", {}), ("0.06", MN),
           ("에 머무는데 선호는 ", {}), ("9/9", MN),
           (' 전부 정확히 복원한다. 맞힌 사용자에게 "확신도 6%"라고 적으면 '
            "오히려 오인시킨다.", {})]),
     Note("warn", "대안도 실패했다",
          [('"이 축이 갈린 비교 중 추정 결과와 같은 쪽을 고른 비율"도 쟀는데, '
            "3값 축에서는 선호값이 아닌 두 값끼리 붙는 비교가 섞여 ", {}),
           ("일관된 사용자 52% 대 무작위 클릭 55%", BD),
           ("로 구분되지 않았다 (2값인 코딩은 100% 대 83%로 갈린다).", {})]),
     Note("ok", "대신 화면에 무엇을 넣었나",
          [("보정되지 않은 확신도 대신 ", {}), ('"갈린 비교 수"', BD),
           ("를 축별 게이지로 보여준다. 이 수치는 정의가 명확하고 해석에 보정이 "
            "필요하지 않다 — 그 축을 실제로 몇 번 물었는지다. 보정된 확신도는 "
            "사후분포나 홀드아웃 일관성 검사가 필요한 별도 과제로 남긴다.", {})])],
])], top, label="16")

# ══ 17 알고리즘 ③ ════════════════════════════════════════════════════
s, top = d.page(CH2, "2-8. 핵심 알고리즘 ③ 평가 함수 조립 → GEPA",
                [("GEPA에 넘기는 세 인자 중 ", {}),
                 ("평가 함수가 본 과제의 기여", AC),
                 ("이고, 나머지 둘은 데이터다.", {})], "17")
stack(d, s, [Cols([
    [Table(["GEPA 인자", "본 도구의 대응", "출처"],
           [["seed_candidate", "추정된 축 선호로 조립한 초기 시스템 프롬프트", "estimator"],
            ["trainset / valset", "과제 원문 수십 개", "데이터셋"],
            [(["metric"], RDT), (["축별 위반 내역을 점수 + 자연어 피드백으로 반환"], BD),
             (["metric_builder ★"], HI)],
            ["max_metric_calls", "비용 상한. 초기에는 낮게 시작", "설정"]],
           [1.5, 3.4, 1.4], caption="GEPA 인자 대응", size=FS_XS),
     Note("note", "반환 형태",
          [('return 0.6, "길이 위반: 6문장(목표 3문장 이하). 원문 수치 3개 중 1개 누락."',
            MN),
           ("\n점수만 주면 GEPA는 무엇을 고쳐야 하는지 모른다. 축을 명시적으로 "
            "갖고 있으므로 이걸 줄 수 있다.", {})])],
    [Note("warn", "설계 과정에서 걸린 함정 — 시드가 이미 최적",
          [("1차 어블레이션에서 시드를 추정 선호로 조립했더니 ", {}),
           ("검증 점수가 처음부터 1.0으로 포화", BD),
           ("돼 두 조건 모두 개선할 여지가 없었다. 무의미한 비교였다. 시드를 "
            "일부러 못 맞는 것으로 바꿔 재실행했다.", {})]),
     Note("red", "같은 함정이 제품에도 있다 — 정직하게 기재",
          [("대표 수치(3-3의 D)는 GEPA 없이 시드 템플릿만으로 낸 값이다. ", BD),
           ("배포 앱의 최적화 버튼도 같은 이유로 대부분 아무것도 바꾸지 않는다. "
            "시드가 추정된 선호로 조립되고 평가 함수가 그 ", {}), ("같은 선호", BD),
           ("를 검사하므로, 로그에 ", {}),
           ("Base program full valset score: 1.0", MN), (" 이후 ", {}),
           ("All subsample scores perfect. Skipping.", MN), ("이 찍힌다.\n", {}),
           ("시드를 중립으로 바꾸는 것은 ", {}),
           ("홀드아웃 검증이 필요한 별도 과제", BD),
           ("이므로 임의로 손대지 않았고, 대신 화면이 ", {}),
           ('"시드가 이미 통과해 바꿀 것이 없었다"고 말하도록', BD),
           (" 고쳤다. 조용히 아무 일도 하지 않는 버튼을 두는 것이 더 나쁘다.", {})])],
])], top, label="17")

# ══ 18 전문가 경로 ════════════════════════════════════════════════════
s, top = d.page(CH2, "2-9. 전문가 프롬프트 경로 — 계측만으로 만든다",
                [("길이 기준을 상수로 박지 않고, ", {}),
                 ("어느 모수화가 이 저자에게 안정적인지를 학습 예시에서 재서 고른다.",
                  AC)], "18")
stack(d, s, [Cols([
    [Table(["특징", "요약", "Q&A cooking", "Q&A writing"],
           [["압축률", (["165.51"], HI), "1.72", "0.38"],
            ["단어 / 답변", "5.76", "4.27", "2.13"],
            ["문장 / 답변", "3.07", "4.19", "2.23"],
            ["문장당 단어", "1.02", "3.27", "1.14"],
            ["문단 / 답변", "0.00", "4.14", "1.43"],
            ["글머리 기호 비율", "0.00", "0.66", "0.97"],
            ["유보 표현 밀도", "0.01", "0.34", "0.32"]],
           [1.7, 1.0, 1.3, 1.3],
           caption="판별력비 — 저자 간 차이가 저자 안 흔들림보다 큰가 (임계값 2.0)",
           aligns=["l", "r", "r", "r"], size=FS_XS),
     Note("red", None,
          [("압축률 앵커는 요약 과제의 산물이었다.", BD),
           (" 요약은 원문이 길면 요약도 길어 상관이 ", {}), ("0.91~0.996", MN),
           ("이지만, 실제 Q&A에서는 ", {}), ("0.04~0.25", MN),
           ("다 — 답변 길이는 ", {}), ("질문의 난이도", BD),
           ("가 정하고 질문의 길이가 정하지 않는다. 두 사이트에서 재현됐다.", {})])],
    [Note("ok", "대응 — 모수화를 측정으로 고른다",
          [("학습 예시에서 절대 단어 수와 압축률의 변동계수를 각각 재서 작은 쪽을 "
            "쓴다. 상수를 박지 않는다. 여유 계수 ", {}), ("0.6", MN),
           ("을 두면 저자 11명 × 20개 분할 = ", {}),
           ("220개 분할을 전부 올바르게 판정", BD),
           ("한다(계수 없이는 13개를 놓친다). 요약 페르소나 3명은 전원 압축률, "
            "실제 저자 16명은 전원 절대 단어 수가 선택됐다.", {})]),
     Note("note", "앵커의 두 절은 각각 다른 일을 한다",
          [("문장당 단어 수는 판별력 ", {}), ("1.02", MN),
           ("로 탈락했지만, 앵커에서 그 절을 빼면 모델이 문장을 잘게 쪼개 문장당 "
            "단어가 ", {}), ("13~17", MN),
           ("로 내려간다 — 저자(18~23)도 모델 기본값(22.2)도 아닌 값이다. "
            "형식 거리가 3~11배 나빠진다.\n", {}),
           ('"그 특징이 저자를 구분하지 못한다"와 "그 특징을 적는 것이 도움이 '
            '안 된다"는 다른 말이다.', BD),
           (" 문장 절은 저자 정보를 나르는 게 아니라 ", {}),
           ("모델의 기본 습관을 묶는 제약", BD), ("으로 일한다.", {})])],
])], top, label="18")

# ══ 19 검증 범위 ══════════════════════════════════════════════════════
s, top = d.page(CH2, "2-10. 실험 및 검증 범위",
                [("데이터셋마다 ", {}),
                 ("무엇을 검증할 수 있고 무엇은 할 수 없는지", AC),
                 ("를 먼저 확정한다.", {})], "19")
stack(d, s, [
    Table(["데이터", "규모", "역할", "검증 가능", "검증 불가 — 그 이유"],
          [["MACSum macdoc", "문서 94개\n페르소나 3종",
            "주 실험. 속성 조합 하나 = 페르소나 하나, 사람 작성 요약이 답안지",
            "복원율 · 수렴 곡선 · 비교군 A/B/D · 독립 채점",
            (["길이 외 문체. 요약 2,954개 전체에서 글머리 기호가 0이고 문단 수가 정확히 1이다"], BD)],
           ["MACSum macdial", "요약 2,954개", "위 판정의 확인용 반대쪽 분할",
            "길이 외 문체 검증 불가의 확정", "동일 — 글머리 기호 0, 문단 1"],
           ["Stack Exchange\n(공개 API · CC BY-SA)", "저자 16명 / 4사이트\n저자당 88~100쌍",
            "전문가 경로 검증. 제품이 노리는 상황 그 자체 — 자기 분야에 답을 많이 써둔 실제 사람",
            "형식 거리 · 판별력 진단 · 모수화 선택", "내용 정확도. 답변의 사실성은 채점하지 않는다"],
           ["Yelp Review Full", "3,000건 캐싱\n(전체 65만)",
            "세 번째 카테고리(리뷰). 감정 축 사전을 실측으로 검증한 뒤 도메인을 만들었다",
            "감정 축의 단조성\n(−0.0022 / 0.019 / 0.0392)",
            "개인 저자 단위 문체. 리뷰는 한 사람이 여러 개를 쓰지 않는다"],
           ["한국어 요약", "스팟 체크 20건\n+ 72케이스",
            "언어 이식성 검증 및 데모",
            "파이프라인 완주 · 스팟 체크 · 백엔드 일치율 97%",
            (["정량 복원율. 사람 주석 답안지가 없어 오라클을 세울 수 없다"], BD)],
           ["specificity 라벨", "5,379건\n(normal 4,670 / high 709)",
            "하이브리드 판정 계층의 진짜 정확도 측정. 프록시가 아니다",
            "5가지 판정 방법의 균형정확도", "—"]],
          [1.55, 1.5, 3.1, 2.2, 2.6],
          caption="검증 자산 — 출처 / 규모 / 검증 가능 범위", size=FS_XS),
], top, label="19")

# ══ 20 장 표지 3 ══════════════════════════════════════════════════════
d.chapter(CH3, "03", "수행 방안 및 일정",
          ["3-1 측정 우선 방법론", "3-2~3-6 확보된 결과 5건",
           "3-7 한계와 음성 결과", "3-8 단계별 수행 계획",
           "3-9 추진 일정", "3-10 마일스톤"], "20")

# ══ 21 방법론 ═════════════════════════════════════════════════════════
s, top = d.page(CH3, "3-1. 방법론 — 측정 우선 개발",
                [("축을 채택하기 전에 ", {}), ("판별력을 먼저 측정한다.", AC),
                 (" 11개 절대 규칙으로 문서화해 팀 전체가 공유한다.", {})], "21")
stack(d, s, [Cols([
    [Note("red", "절대 규칙 (전문)", bullets=[
        [("1. 엔진 코드에 ", {}), ("도메인 특정 단어가 등장해서는 안 된다", BD)],
        "2. API 호출 결과는 반드시 캐싱한다",
        "3. 새 라이브러리를 추가하기 전에 먼저 묻는다",
        "4. API 키를 코드에 하드코딩하지 않는다",
        "5. 한 번에 파일 하나 또는 함수 하나 단위로 작업한다",
        [("6. 평가 지표는 가능한 한 ", {}), ("코드로 직접 잰다.", BD),
         (" 모델 판정은 최후 수단", {})],
        [("7. ", {}), ("최적화에 사용한 지표만으로 최종 성능을 보고하지 않는다.", BD),
         (" 독립 지표를 병기한다", {})],
        "8. 한국어에서는 정량 복원율을 산출하지 않는다 — 답안지가 없다",
        "9. 생성된 코드는 검증 후에만 실행한다",
        "10. 신규 모듈은 기존 파이프라인과 분리한다",
        [("11. ", {}), ("개별 케이스의 작은 차이를 결론으로 쓰지 않는다", BD)],
     ], size=FS_XS)],
    [Note("note", "규칙 6·7이 생긴 경위",
          [("비교군 D의 프롬프트는 검사 함수로 조립한 평가 함수에 대해 최적화되고, "
            "그 결과를 다시 같은 검사 함수로 채점한다. ", {}),
           ("자기가 최적화한 함수로 자기를 채점하는 구조", BD),
           ("이므로 순환 논증으로 지적당할 수 있다. 그래서 최적화에 전혀 쓰이지 않은 "
            "독립 지표를 추가하고, ", {}), ("점수가 낮아져도 그대로 보고한다.", BD),
           (" 0.996보다 0.85가 더 믿을 만한 결과다.", {})], size=FS_XS),
     Note("warn", "규칙 11이 생긴 경위 — 가장 최근",
          [('저자 한 명의 결과를 보고 "모델이 큰 길이 목표에 체계적으로 미달한다"는 '
            "결론을 냈다가 ", {}), ("폐기했다.", BD),
           (" 요청 단어 수를 20~320으로 바꿔 전달 함수를 직접 재보니 모델은 요청값을 "
            "거의 정확히 따랐다(비율 ", {}), ("0.99~1.06", MN),
           ("). 실제 원인은 실험 설계였다 — 학습과 홀드아웃 부분집합의 평균 길이가 "
            "체계적으로 달랐다(98.5 대 128.4단어). ", {}),
           ("거의 완벽한 준수를 미달로 읽은 것이다.", BD)], size=FS_XS),
     Note("ok", "측정 우선의 실제 효과",
          [("감정 축을 만들 때 사전 없이 바로 구현하지 않고, 3,000건으로 별점 파생 "
            "라벨과의 단조성을 먼저 확인했다(−0.0022 / 0.019 / 0.0392). 임계값도 "
            "중앙값 사이 중점을 실측 분포에서 뽑았다. ", {}),
           ("이 절차를 밟은 축은 이후 문제를 일으키지 않았다.", BD)], size=FS_XS)],
], weights=[1, 1.25])], top, label="21")

# ══ 22 결과 ① 어블레이션 ═════════════════════════════════════════════
s, top = d.page(CH3, "3-2. 확보된 결과 ① 구조화 피드백 어블레이션",
                [("인과관계가 가장 깨끗한 결과. ", {}),
                 ("점수 계산 함수는 완전히 동일하게 두고 피드백 텍스트만 바꿨다.",
                  AC)], "22")
stack(d, s, [Cols([
    [Note("note", "실험 설계",
          [("같은 호출 예산 ", {}), ("40", MN),
           ("회 안에서 도달하는 검증 점수를 비교한다. 바꾼 변수는 단 하나 — "
            "GEPA에게 보이는 피드백 텍스트다.", {})],
          bullets=[[("rich", BD), (" — 축별 위반 내역", {})],
                   [("terse", BD), (" — 점수만 (Score: 0.44)", {})]]),
     Bars([("rich", "0.98", 0.98, ACCENT), ("terse", "0.44", 0.44, INK3),
           ("rich 8회 시점", "0.96", 0.96, ACCENT),
           ("rich 시작점", "0.36", 0.36, INK3)],
          note="같은 예산 40회에서 도달한 검증 점수. 0~1 척도.")],
    [Note("ok", "결과",
          [("rich 0.98 대 terse 0.44.", BD), (" rich는 ", {}), ("8", MN),
           ("회 만에 0.36에서 0.96으로 뛰고 그 언저리를 유지하는데, terse는 "
            "40회 내내 0.44 근처에서 벗어나지 못한다.", {})]),
     Note("red", "이 수치는 최적화 목표 지표로 잰 값이다 (규칙 7)",
          [("검증 점수 0.98은 GEPA가 최적화한 바로 그 평가 함수로 잰 값이다. "
            "독립 지표(ROUGE-L)로 다시 재는 것이 잔여 과제 2번이고, 그 전까지 "
            "목표표(1-3)에는 ", {}), ("부분 달성", BD), ("으로 적는다.", {})],
          size=FS_XS),
     Note("red", "왜 이 결과가 중요한가",
          [('"우리는 축을 명시적으로 갖고 있어서 구조화된 피드백을 줄 수 있고, '
            '범용 최적화 도구는 못 하는 부분"이라는 본 과제의 기여 주장에 대한 ', {}),
           ("유일한 직접 증거", BD),
           ("다. 다른 변수 없이 피드백 형식 하나만 바꿔서 나온 차이라 인과관계를 "
            "주장할 수 있다.", {})]),
     Note("warn", "이 실험을 한 번 다시 설계해야 했다",
          [("1차 시도에서는 시드를 추정 선호로 조립했는데, 그러면 시드가 이미 평가 "
            "함수에 최적화돼 검증 점수가 처음부터 1.0으로 포화된다. ", {}),
           ("두 조건 모두 개선할 여지가 없어 무의미한 비교였다.", BD),
           (" 시드를 일부러 못 맞는 것으로 바꿔 재실행했다.", {})])],
])], top, label="22")

# ══ 23 결과 ② 비교군 ═════════════════════════════════════════════════
s, top = d.page(CH3, "3-3. 확보된 결과 ② 개인화 효과 — 비교군 A · B · D",
                [("두 지표를 ", {}), ("나란히", AC),
                 (" 보고한다. 절대값이 크게 낮아지는 것도 감추지 않는다.", {})], "23")
stack(d, s, [Cols([
    [Table(["조건", "checks\n(최적화 목표와 동일 계열)", "ROUGE-L\n(독립)"],
           [["A — 프롬프트 없이 기본 호출", "0.600 ± 0.224", "0.101 ± 0.055"],
            ["B — 사용자가 직접 쓴 지침", "0.668 ± 0.240", "0.168 ± 0.060"],
            [(["D — 본 도구 (8회 비교 → 자동 조립)"], BD),
             (["0.996 ± 0.010"], HI), (["0.207 ± 0.057"], HI)]],
           [2.5, 1.6, 1.4], caption="비교군별 결과 — 5개 문서 평균",
           aligns=["l", "r", "r"], size=FS_XS),
     Bars([("A · checks", ".600", 0.600, INK3), ("B · checks", ".668", 0.668, INK3),
           ("D · checks", ".996", 0.996, ACCENT),
           ("A · ROUGE-L", ".101", 0.404, INK3),
           ("B · ROUGE-L", ".168", 0.672, INK3),
           ("D · ROUGE-L", ".207", 0.828, OK)],
          note="checks는 0~1 척도, ROUGE-L은 0~0.25 척도로 그렸다. "
               "두 지표의 절대값은 직접 비교할 수 없다."),
     Note("note", "B의 지침 원문 · D의 구성",
          [('"Summarize the following article in a clear and concise way that '
            'captures the key points."', MN),
           (" — 5개 문서 동일, 페르소나를 모르는 상태로 작성. ", {}),
           ("D는 GEPA를 쓰지 않는다", BD),
           (" — 8회 루프 → 시드 템플릿 조립 → 생성. 검사 함수와 사람 라벨의 "
            "일치율은 부록 A.", {})], size=FS_XS)],
    [Note("note", "독립 지표를 어떻게 세웠나",
          [("최적화 루프에서 완전히 분리된 ROUGE-L을 쓴다 — ", {}),
           ("알고리즘이 다르고", BD),
           ("(검사 함수는 bigram Dice, 이쪽은 LCS 기반), ", {}),
           ("비교 대상도 다르다", BD),
           ("(원문이 아니라 사람이 작성한 정답 요약).", {})], size=FS_XS),
     Note("warn", "주장의 강도를 정확히 적는다",
          [("ROUGE-L에서 D(0.207)와 B(0.168)의 차이 ", {}), ("0.039", MN),
           ("는 ", {}), ("표준편차보다 작다.", BD),
           (" 평균만으로 우위라 단정할 근거가 약하다. 문서 5개 각각에서 페어드로 "
            "비교하면 ", {}), ("D가 5개 중 4개에서 B보다 높다", BD),
           ("(나머지 1개는 −0.0012로 사실상 동률). 표본이 5개뿐이라 통계적 "
            "유의성은 주장하지 않는다.", {})], size=FS_XS),
     Note("red", "지표를 병기하면 절대값이 무너진다 — 그래도 병기한다",
          [("D 기준 ", {}), ("0.996 → 0.207", MN),
           (". B 대비 상대 차이도 checks 기준 +32.8%p에서 ROUGE-L 기준 ", {}),
           ("+3.9%p", BD), ("로 줄어든다. ", {}),
           ("이게 0.996만 내세우는 것보다 방어 가능한 주장이다.", BD)], size=FS_XS)],
], weights=[1.25, 1])], top, label="23")

# ══ 24 결과 ③ 복원율 ═════════════════════════════════════════════════
s, top = d.page(CH3, "3-4. 확보된 결과 ③ 복원율 — 그리고 오라클을 다시 만든 일",
                [("가장 큰 개선은 알고리즘이 아니라 ", {}),
                 ("평가 방식의 오류를 찾아 고친 것", AC), ("에서 나왔다.", {})],
                "24")
stack(d, s, [Cols([
    [Note("red", "페르소나 오라클 재설계 — 0~8% → 80%",
          [('처음에는 "그 속성 조합의 사람 요약 = 답안지"로 삼아 단어 겹침으로 '
            "페르소나의 선택을 대행했다. 그런데 ", {}),
           ("원문을 그대로 발췌하는 후보가 고유명사를 가져와 정답 요약과 우연히 "
            "어휘가 더 겹쳐", BD),
           (" 엉뚱한 축으로 수렴했다 — 내용 겹침과 문체 겹침을 구분하지 못한 것이다.\n"
            "정답 요약 텍스트 대신 ", {}),
           ("검증해둔 검사 함수를 페르소나의 실제 축값에 대고 두 후보에 돌려", BD),
           (" 점수가 높은 쪽을 고르는 방식으로 바꿨다. 5문서 평균 완전복원율이 ", {}),
           ("0~8%에서 80%로", BD), (" 올랐다.", {})], size=FS_XS),
     Note("warn", "같은 시기에 발견한 언어 혼재 버그",
          [("과제 서술이 한국어라 출력 언어가 축 조합에 따라 들쭉날쭉했고, 영어 "
            "데이터 기준의 겹침·키워드·선택 대행이 ", {}),
           ("전부 언어 불일치로 왜곡된 값", BD),
           ("을 내고 있었다. 출력 언어를 명시 고정해 고쳤고, 같은 맥락에서 캐시 키도 "
            "축 조합이 아니라 ", {}), ("조립된 프롬프트 텍스트 자체", BD),
           ("로 바꿨다 — 안 그러면 문구를 고쳐도 캐시가 무효화되지 않는다.", {})],
          size=FS_XS)],
    [Table(["실험", "설계", "완전복원", "축 단위"],
           [["알고리즘 비교", "5문서 × 3알고리즘 × 5시드, 750행 커밋",
             (["80%"], HI), "2.0 / 2"],
            ["폐쇄 루프 회귀 테스트", "3문서 × 페르소나 3종 = 9케이스, 8라운드 실제 호출",
             "5/9 (56%)", "12/18 (67%)"]],
           [1.8, 3.1, 1.1, 1.0],
           caption="복원율 — 두 실험은 서로 다른 것을 잰다",
           aligns=["l", "l", "r", "r"], size=FS_XS),
     Note("note", "두 수치가 다른 이유를 명시한다",
          [("축별로 갈린다.", BD), (" ", {}), ("length=short", MN),
           (" 선호는 문서 3개에서 3번 다 정확히 복원되는데, ", {}),
           ("length=long", MN), (" 선호는 3번 중 2번 ", {}), ("normal", MN),
           ("로 수렴한다. ", {}),
           ("8라운드 안에서는 인접한 값을 가르기 어렵다", BD),
           ("는 뜻이다. 그래서 회귀 테스트는 안정적으로 복원되는 케이스의 회귀와 "
            "어려운 케이스의 완주만 잡는다.", {})], size=FS_XS),
     Note("ok", "이 수치를 정정한 경위",
          [('원래 문서에는 "정확히 복원하는 걸 확인했다"고 적어뒀는데, 근거 파일이 '
            "검증문 없는 출력 스크립트라 ", {}),
           ("아무도 회귀를 못 보고 있었다.", BD),
           (" 테스트로 바꾸면서 9케이스를 실제로 돌려 재고 수치를 내렸다. 문서의 "
            "수치와 커밋된 결과가 어긋나면 실패하는 검사도 함께 붙였다.", {})],
          size=FS_XS)],
], weights=[1, 1.15])], top, label="24")

# ══ 25 결과 ④ 확장성 ═════════════════════════════════════════════════
s, top = d.page(CH3, "3-5. 확보된 결과 ④ 확장성 — 도메인과 언어",
                [("엔진 코드를 ", {}), ("한 줄도 고치지 않고", AC),
                 (" 카테고리 5종을 붙였다. 정량 검증 1종, 부분 검증 3종, 동작 확인만 1종.", {})], "25")
stack(d, s, [Cols([
    [Table(["카테고리", "축", "검증 수준"],
           [["문서 요약 (영어)", "길이 · 추출성 · 주제",
             (["정량 — 복원율 80%, ROUGE-L, 비교군"], BD)],
            ["문서 요약 (한국어)", "동일", "스팟 체크 20건 + 백엔드 일치율 97%. 정량 복원율 없음(답안지 없음)"],
            ["코딩", "2값 축", "일관된 가상 사용자 완전복원 8/8"],
            ["고객 리뷰", "길이 · 감정 · 주제", "감정 축 3,000건 단조성 확인 + 실제 API 6회 루프 선호 복원"],
            ["이메일 초안", "길이 · 격식 · 구조", "확장성 테스트(동작)만. 실측 데이터 없음"]],
           [1.6, 1.7, 3.6], caption="과제 카테고리 5종 — 동작은 전부, 검증 수준은 다르다",
           size=FS_XS),
     Note("ok", "확장성 주장의 구멍도 메웠다",
          [("전에는 엔진은 안 고쳐도 ", {}),
           ("데모 생성기를 손으로 써야 한다", BD),
           ("는 구멍이 남아 있었다. 공용 데모 생성기가 여러 도메인이 공유하는 축을 "
            "실제로 구현하면서, 리뷰·이메일은 정의와 검사 함수만으로 다섯 번째 "
            "카테고리까지 동작한다. 다섯 카테고리 전부의 8회 완주를 검사하는 "
            "테스트가 있다.", {})], size=FS_XS)],
    [Note("note", "한국어에서 실제로 고쳐야 했던 것",
          [('한국어는 조사가 붙어 "정부는/정부가/정부의"가 전부 다른 토큰이 된다. '
            "표면형 비교는 겹침을 과소평가하므로 ", {}),
           ("형태소 분석기로 어간만 추출", BD),
           ("해 비교한다. 스팟 체크에서 발췌형 조합은 겹침 ", {}), ("94%", MN),
           (", 서술형 조합은 ", {}), ("40%", MN), ("로 명확히 갈렸다.", {})],
          size=FS_XS),
     Note("warn", "배포 제약 때문에 두 번째 백엔드가 필요했다",
          [("형태소 분석기 모델이 ", {}), ("88MB", MN),
           ("라 무료 티어에서 빌드 실패나 메모리 초과 위험이 있었다 — ", {}),
           ("제출 링크가 죽으면 실격", BD),
           ("이라 감수할 수 없는 위험으로 판단했다. 지연 로드로 바꾸고 사전이 "
            "필요 없는 ", {}), ("문자 4-gram 겹침", BD),
           (" 대체 경로를 뒀다.\n처음엔 어절 끝 조사를 규칙으로 떼려 했으나 "
            '실패했다 — "전문가"의 "가"를 주격 조사로 오인해 중간 대역 겹침이 ', {}),
           ("0.59 → 0.47", MN), ("로 내려가 임계값을 잘못 넘었다.", {})],
          size=FS_XS),
     Note("note", "n을 실측으로 골랐다",
          [("원문 3종 × 발췌 문장 수 4단계 × 어절 누락률 6단계 = ", {}),
           ("72", MN), ("케이스의 임계값 판정 일치율: n=2 ", {}), ("57%", MN),
           (" · n=3 ", {}), ("67%", MN), (" · ", {}), ("n=4 97%", BD),
           (" · n=5 ", {}), ("82%", MN), (" · n=6 ", {}), ("74%", MN),
           (". n=4는 r=0.977, 회귀 기울기 1.07 / 절편 −0.07로 거의 항등이라 "
            "보정 상수 없이 같은 임계값을 공유한다. ", {}),
           ("보고 수치는 전부 형태소 기준으로 유지", BD),
           ("하고, 환경이 조용히 대체 경로로 내려앉지 않도록 검사를 붙였다.", {})],
          size=FS_XS)],
], weights=[1, 1.15])], top, label="25")

# ══ 26 결과 ⑤ 전문가 경로 ════════════════════════════════════════════
s, top = d.page(CH3, "3-6. 확보된 결과 ⑤ 전문가 경로 — 실제 저자 8명",
                [("29단어 앵커 하나가 ", {}),
                 ("1,000단어가 넘는 예시 나열보다 낫다.", AC)], "26")
stack(d, s, [Cols([
    [Table(["사이트", "저자", "학습\n단어", "base", "앵커", "예시", "예시\n+앵커"],
           [["academia", "75368", "164", "0.171", (["0.079"], HI), "0.056", "0.066"],
            ["academia", "40589", "287", "0.416", (["0.176"], HI), "0.249", "0.229"],
            ["cooking", "4638", "320", "0.295", (["0.143"], HI), "0.102", "0.053"],
            ["cooking", "67", "194", "0.216", (["0.100"], HI), "0.196", "0.136"],
            ["diy", "47125", "332", "0.314", (["0.194"], HI), "0.192", "0.046"],
            ["diy", "18078", "158", "0.514", (["0.276"], HI), "0.554", "0.342"],
            ["writing", "26047", "305", "0.413", (["0.179"], HI), "0.372", "0.222"],
            ["writing", "553", "217", "0.122", (["0.019"], HI), "0.057", "0.096"],
            [(["평균"], BD), "8명", "247", (["0.308"], BD), (["0.146"], HI),
             "0.222", "0.149"],
            ["프롬프트 길이", "—", "—", "16단어", (["29단어"], HI), "983~1784", "996~1784"]],
           [1.3, 0.85, 0.8, 0.9, 0.9, 1.0, 1.15],
           caption="형식 거리 — 낮을수록 그 저자에 가깝다 / 학습 30개 · 홀드아웃 30개",
           aligns=["l", "r", "r", "r", "r", "r", "r"], size=FS_XS)],
    [Stats([("8/8", "앵커가 base보다 개선된 저자 수\n부호검정 p = 0.0078"),
            ("0.727", "앵커 단독 대 예시+앵커\n페어드 p값 — 차이 없음")], cols=2,
           size=20),
     Note("ok", "결론 — 앵커만 넣는다",
          [("예시 나열(0.222)은 앵커(0.146)를 ", {}),
           ("한 번도 이기지 못했고", BD),
           (", 둘을 합쳐도(0.149) 앵커 단독과 통계적으로 구분되지 않는다. "
            "프롬프트는 ", {}), ("1,300", MN), ("단어에서 ", {}), ("29", MN),
           ("단어로 줄고, 사용자 자료를 모델에 보낼 필요도 사라진다.", {})],
          size=FS_XS),
     Note("warn", "학습 자료는 30개가 필요하다",
          [("12개로 만든 앵커는 한 저자에서 목표를 ", {}),
           ("465단어로 추정했고 실제는 331단어", BD), ("였다. 30개로 늘리자 그 저자가 ", {}),
           ("−0.122 → +0.187", MN),
           ("로 뒤집혔다. 화면에서 자료가 부족하면 그 사실을 사용자에게 알린다.", {})],
          size=FS_XS),
     Note("note", "독립 지표도 병기한다 (규칙 7)",
          [("ROUGE-L은 ", {}), ("0.135 → 0.145", MN),
           ("로 거의 움직이지 않는다. ", {}),
           ("이 방법이 주장하는 것은 형식 복제이고 내용 근접이 아니다.", BD),
           (" 그대로 적는다.", {})], size=FS_XS)],
], weights=[1.35, 1])], top, label="26")

# ══ 27 한계와 음성 결과 ══════════════════════════════════════════════
s, top = d.page(CH3, "3-7. 한계와 음성 결과 — 성과와 같은 비중",
                [("실패한 시도에서 얻은 것을 적는다. ", {}),
                 ("다섯 가지 방법이 전부 실패한 것 자체가 결과다.", AC)], "27")
stack(d, s, [Cols([
    [Table(["방법", "요약문만", "원문도 제공"],
           [["정규식 휴리스틱", "normal 0.217 / high 0.228", "—"],
            ["개체명 인식", "실질 차이 없음", "—"],
            ["학습형 분류기", "0.500 (우연)", "—"],
            ["소형 모델 판정", "0.525", "0.558"],
            ["대형 모델 판정", (["0.464 (우연보다 낮음)"], BD), "0.514"]],
           [1.6, 2.2, 1.2],
           caption="측정 불가 축(specificity) — 5가지 방법 전부 실패 / 균형정확도",
           aligns=["l", "r", "r"], size=FS_XS),
     Note("warn", "이 실패에서 나온 독자적인 결론",
          [("확신도 임계값 ", {}), ("0.85", MN),
           ("에서 최고점(0.558)을 찍고, 임계값을 1.0으로 올려 전부 대형 모델에 "
            "올리면 ", {}), ("정확도는 0.514로 떨어지면서 비용은 5배", BD),
           ("가 된다.\n판별력이 이미 확인된 길이 축으로 대조 실험을 돌려 확인했다 — "
            "분류기 ", {}), ("0.736", MN), (", 소형 ", {}), ("0.428", MN),
           (", 대형 ", {}), ("0.272", MN), ("로 ", {}),
           ("모델에 맡길수록 단조롭게 나빠진다.", BD),
           (' 원인도 찾았다: 데이터셋이 "짧다"고 라벨링한 2문장짜리를 대형 모델은 '
            '확신도 1.000으로 "길다"고 판정한다 — ', {}),
           ("기준점 없이 물으면 모델이 데이터셋 고유의 라벨링 관례 대신 자기 내부 "
            "기준을 쓴다.", BD)], size=FS_XS)],
    [Table(["사람 축", "concise", "formal", "complex", "entity"],
           [["길이 (문장 수)", "0.121", "−0.453", "0.062", "−0.088"],
            ["추출성 (겹침)", "0.012", "0.113", "−0.112", "0.261"]],
           [1.7, 1.0, 1.0, 1.05, 0.95],
           caption="온보딩 에이전트 — 사람 축과의 상관 (임계값 |r| ≥ 0.7)",
           aligns=["l", "r", "r", "r", "r"], size=FS_XS),
     Note("red", '"다른 이름의 같은 축"이 아니라 정말 다른 축이다',
          [("8개 조합 전부 임계값에 크게 못 미친다(최댓값 0.453). 길이와 가장 "
            "가까운 것도 직관적으로 예상했던 conciseness가 아니라 formality였고 "
            "그마저 약한 ", {}), ("음의", BD), (" 상관이다. ", {}),
           ("이름이 다른 것이 아니라 다른 것을 찾은 것으로 정량 확인됐다.", BD)],
          size=FS_XS),
     Note("ok", "그래도 핵심 루프는 작동한다 — 이쪽이 독자적 성과",
          [("6개 문서로 축 4개를 제안했고 ", {}),
           ("전부 판별력 기준을 통과", BD),
           ("했다. 판별 불가능한 축을 제안하지도, 채택하지도 않았다. 라벨 없이도 "
            "실제로 판별력 있는 새 축을 ", {}),
           ("안전하게 찾아내고 나쁜 축은 스스로 걸러낸다.", BD)], size=FS_XS),
     Note("note", "기타 음성 결과", bullets=[
        [("유보 표현 · 숫자 · 1인칭 밀도 — 실제 사람 16명 사이에서도 신호 없음. ", {}),
         ("지표 자체를 기각", BD)],
        "비율을 규칙으로 적기 — 모델이 스스로 적용하지 못한다. 길이 오차 3단어 → 27~57단어",
        "전문가 경로에 GEPA — 손조립(0.121)에 지고(0.191) 저자당 60회 호출을 쓴다",
        [("실패 예측(게이팅) — 모델 기본값과의 격차 r=−0.031. ", {}),
         ("예측 불가", BD)],
     ], size=FS_XS)],
])], top, label="27")

# ══ 28 단계별 수행 계획 ══════════════════════════════════════════════
s, top = d.page(CH3, "3-8. 단계별 수행 계획 — 남은 과제",
                [("남은 항목은 전부 ", {}), ("확보된 결과의 방어", AC),
                 ("에 걸린 것이다. 새 기능보다 먼저 한다.", {})], "28")
stack(d, s, [
    Table(["순위", "과제", "왜 필요한가", "완료 판정 기준", "공수"],
          [["1", "실사용자 응답 집계",
            "모든 정량 결과가 페르소나 기반이고 '실제 사람도 좋아했다'는 근거가 없다",
            "api 모드 응답 3건 이상 확보 후 모드별 분리 보고. demo·expert와 합치지 않는다",
            "1주"],
           ["2", "시드 중립 GEPA 검증",
            "현재 최적화 버튼은 시드가 이미 평가 함수를 통과해 대부분 아무것도 바꾸지 않는다",
            "중립 시드 결과를 최적화에 쓰지 않은 지표로 채점해 base 대비 개선 확인. 어블레이션의 0.98은 순환이라 근거로 쓰지 않는다",
            "1.5주"],
           ["3", "자료가 적은 사용자용 경로",
            "학습 12개 구간에서는 예시 방식이 0.121로 앵커(0.158)보다 낫다",
            "자료 개수에 따라 경로를 자동 선택하고 두 구간의 형식 거리를 각각 보고",
            "1주"],
           ["4", "배포 버전 검증",
            "현재 감시는 화면이 그려지는지만 본다. 구버전이 떠 있어도 통과한다",
            "감시 스크립트가 배포된 버전 문자열을 확인하고 불일치 시 실패", "0.5주"],
           ["5", "보정된 확신도",
            "현재 확신도는 보여줄 만큼 보정되지 않아 화면에서 철회한 상태다",
            "사후분포 또는 홀드아웃 일관성 검사로, 완전복원한 사용자에게 높은 값이 나오는지 확인",
            "2주"],
           ["6", "결과 정리 및 발표", "성공과 한계를 같은 비중으로 놓는 구성",
            "발표 자료 + 최종 보고서 + 저장소 문서 갱신", "1.5주"]],
          [0.55, 2.0, 3.2, 4.8, 0.7],
          caption="잔여 과제 — 우선순위 / 완료 판정 기준", aligns=["r", "l", "l", "l", "r"],
          size=FS_XS),
    Cols([
        [Note("warn", "범위 밖으로 남기는 것 — 명시한다", bullets=[
            "에이전트 정교화(축 값 개수 자동 결정, 중복 제거, 반복 자기개선) — 3~4주 분량, 시간 제약상 제외",
            "최적화기 교체 검증 (GEPA → MIPROv2)",
            "한국어 전문가 경로 — 문자 단위 계측 기준을 다시 세워야 한다",
        ], size=FS_XS)],
        [Note("red", "우선순위 원칙",
              [("새 기능보다 기존 결과의 방어가 먼저다.", BD),
               (" 1·2번이 끝나기 전에는 3~5번에 착수하지 않는다. 확보한 결과가 "
                "순환 논증으로 무너지면 나머지가 전부 의미를 잃는다.", {})],
              size=FS_XS)],
    ]),
], top, label="28")

# ══ 29 추진 일정 ══════════════════════════════════════════════════════
s, top = d.page(CH3, "3-9. 추진 일정 — 15주",
                [("8주차에 ", {}), ("중간 점검으로 범위 축소선을 판단", AC),
                 ("하고, UI는 반드시 마지막에 만든다.", {})], "29")
stack(d, s, [
    Gantt([f"W{i}" for i in range(1, 16)],
          [("환경 구성 · 데이터 로드 · 튜토리얼", [(1, 2)], INK3, True),
           ("도메인 정의 · 생성기 · 캐싱", [(2, 3)], INK3, True),
           ("★ 쌍 선택 · 선호 추정 (3종)", [(4, 5)], INK3, True),
           ("★ 평가 함수 조립 · 검사 함수", [(6, 6)], INK3, True),
           ("GEPA 연동 · 엔드투엔드 관통", [(7, 8)], INK3, True),
           ("주 실험 · 수렴 곡선 · 비교군", [(9, 10)], INK3, True),
           ("독립 채점자 · 한국어 이식 · UI", [(10, 11)], INK3, True),
           ("하이브리드 판정 · 온보딩 에이전트", [(11, 12)], INK3, True),
           ("전문가 경로 · 배포 운영 · UI 개편", [(12, 13)], ACCENT, False),
           ("실사용자 집계 · 시드 중립 검증", [(13, 14)], OK, False),
           ("결과 정리 · 발표 준비", [(14, 15)], OK, False)],
          [(INK3, "완료 (v1 · v2)"), (ACCENT, "진행 중"), (OK, "계획"),
           (None, "W8 — 중간 점검 및 범위 축소선 판단"),
           (None, "★ 라이브러리로 대체 불가")]),
    Note("note", None,
         [("UI는 반드시 마지막에 만든다.", BD),
          (" 먼저 만들면 시간을 전부 잡아먹는다. 실제로 화면 개편은 실험이 전부 "
           "끝난 뒤에 착수했고, 그 과정에서 발견한 결함 두 건은 ", {}),
          ("코드를 읽어서가 아니라 화면을 실제로 그려봐서", BD),
          (" 잡혔다(마크다운 이중 이스케이프, 카드 높이 불일치).", {})], size=FS_SM),
], top, label="29")

# ══ 30 마일스톤 ═══════════════════════════════════════════════════════
s, top = d.page(CH3, "3-10. 마일스톤 및 범위 축소선",
                [("진척이 부족하면 ", {}), ("축소선을 강제 적용", AC),
                 ("한다. 판단 시점을 미리 정해둔다.", {})], "30")
stack(d, s, [Cols([
    [Table(["시점", "내용", "산출물"],
           [["W8", "중간 점검 — 범위 축소선 판단", "엔드투엔드 1회 관통 확인"],
            ["9/20", (["예선 제출"], BD),
             "공개 링크 · 저장소 · 본 계획서. 실사용자 응답은 '수집 중'으로 기재"],
            ["10/7", (["본선 발표"], BD), "실사용자 응답 실제 수치 · 발표 자료"],
            ["W15", "최종 정리", "최종 보고서 · 저장소 문서 갱신"]],
           [0.85, 2.3, 3.2], caption="마일스톤", aligns=["r", "l", "l"],
           size=FS_XS),
     Note("red", "일정상의 설계 의도",
          [("실사용자 응답 수집은 예선 시점에 표본이 없다. 그래서 예선에는 ", {}),
           ("'수집 중'으로만 쓰고", BD),
           (" 본선 발표에 실제 수치를 넣는 것이 이 설계의 의도다. 없는 표본을 "
            "추정치로 채우지 않는다.", {})], size=FS_XS)],
    [Note("ok", "최소 달성선 (반드시) — 전부 확보", bullets=[
        "도메인 1종, 축 3개",
        "선택 루프 동작, 평가 함수 자동 생성",
        "페르소나 기반 복원율 · 수렴 곡선 측정",
        "비교군 A · B 대비 결과",
     ], size=FS_XS),
     Note("note", "여유 시 항목 — 달성 현황", bullets=[
        "알고리즘 3종 비교 — 완료",
        "2번째 도메인 이식 — 완료 (5종)",
        "공개 배포 — 완료",
        "실사용자 검증 — 진행",
        "비교군 C (상용 프롬프트 생성 도구) — 미착수",
        "최적화기 교체 검증 — 범위 밖",
     ], size=FS_XS),
     Note("warn", "축소 시 우선 버리는 것",
          [("배포보다 실험 결과가 우선이다.", BD),
           (" 실험이 끝나지 않았으면 배포를 버린다. 다만 이미 배포가 안정화된 현 "
            "시점에서는 그 반대 위험이 더 크므로, 잔여 기간에는 ", {}),
           ("배포 링크의 가용성을 실험 재실행보다 우선", BD),
           ("한다 — 링크가 죽으면 심사 자체가 성립하지 않는다.", {})], size=FS_XS)],
], weights=[1.15, 1])], top, label="30")

# ══ 31 장 표지 4 ══════════════════════════════════════════════════════
d.chapter(CH4, "04", "추진 조직",
          ["4-1 조직 및 역할", "4-2 공수 산정", "4-3 협업 규칙"], "31")

# ══ 32 조직 및 역할 ══════════════════════════════════════════════════
s, top = d.page(CH4, "4-1. 조직 및 역할",
                [("3인 · 타 프로젝트 병행 · 1인당 주 10시간. ", {}),
                 ("실제 개발 가능 기간을 6~8주로 간주", AC),
                 ("하고 범위를 잡았다.", {})], "32")
stack(d, s, [
    Cols([
        [Note("red", [("엔진 / 알고리즘", BD),
                      ("     PL 겸임", {"font": MONO, "color": ACCENT})],
              [("담당: ", BD), ("쌍 선택 · 선호 추정 · 평가 함수 조립 · GEPA 연동\n", {}),
               ("산출: ", BD), ("엔진 5개 모듈, 핵심 3모듈의 회귀 테스트\n", {}),
               ("판단 권한: ", BD),
               ("도메인 무지 규칙 위반 여부의 최종 판정. 엔진에 도메인 단어가 "
                "들어가는 변경은 이 역할이 거부할 수 있다", {})], size=FS_XS)],
        [Note("layer", "실험 / 평가",
              [("담당: ", BD),
               ("페르소나 오라클 · 독립 채점자 · 판별력 진단 · 통계 처리\n", {}),
               ("산출: ", BD), ("결과 파일 39건, 수렴 곡선, 부호검정 p값\n", {}),
               ("판단 권한: ", BD),
               ("축 채택 여부. 판별력 측정을 통과하지 않은 축은 이 역할이 기각한다", {})],
              size=FS_XS)],
        [Note("layer", "도메인 / 화면 / 운영",
              [("담당: ", BD),
               ("도메인 정의·검사 함수 5종, 화면 3단계, 배포 감시, 지출 상한\n", {}),
               ("산출: ", BD), ("카테고리 5종, 순수 서비스 계층, 감시 워크플로\n", {}),
               ("판단 권한: ", BD),
               ("배포 위험 판정. 링크 가용성을 해칠 변경은 보류시킬 수 있다", {})],
              size=FS_XS)],
    ]),
    Cols([
        [Note("note", "4-3. 협업 규칙 — 실패에서 나온 것", bullets=[
            [("한 번에 파일 하나 또는 함수 하나", BD),
             (" 단위로 작업한다. 전체를 한꺼번에 생성하지 않는다", {})],
            "새 라이브러리 추가는 제안 → 합의를 거친다. 배포 환경의 메모리 한도가 걸린 사안이다",
            [("문서의 수치와 커밋된 결과가 어긋나면 ", {}), ("검사가 실패", BD),
             ("한다. 사람 검토에 의존하지 않는다", {})],
            "검증문 없는 출력 스크립트를 결과의 근거로 쓰지 않는다 — 아무도 회귀를 못 본다",
        ], size=FS_XS)],
        [Note("warn", "병행 제약이 실제로 만든 결정",
              [("주 10시간 제약 때문에 ", {}), ("먼저 만들지 않은 것들", BD),
               ("이 있다. 서버형 DB를 쓰지 않고, 프런트엔드를 분리하지 않고, UI를 "
                "마지막으로 미룬 것은 모두 이 제약에서 나온 결정이다. ", {}),
               ("범위를 줄인 대신 남긴 것에 측정을 붙였다.", BD)], size=FS_XS)],
    ]),
], top, label="32")

# ══ 33 공수 산정 ══════════════════════════════════════════════════════
s, top = d.page(CH4, "4-2. 공수 산정",
                [("가용 공수 ", {}), ("450시간", AC),
                 (". 실험·검증에 가장 많이 배분한다 — 완성도의 기준이 측정된 "
                  "결과이기 때문이다.", {})], "33")
stack(d, s, [Cols([
    [Table(["영역", "투입(h)", "비중", "근거"],
           [["★ 엔진 핵심 3모듈", "80", "17.8%", "라이브러리로 대체 불가. 회귀 테스트 포함"],
            ["실험 설계 · 실행 · 집계", "110", "24.4%", "결과 파일 39건. 폐기된 시도 3건 재실행 포함"],
            ["도메인 · 검사 함수 5종", "60", "13.3%", "축 채택 전 판별력 측정 시간 포함"],
            ["전문가 경로", "55", "12.2%", "저자 16명 코퍼스 + 8차에 걸친 방법 수정"],
            ["에이전트 · 하이브리드 판정", "50", "11.1%", "샌드박스 방어 2계층. 부수 목표는 기각"],
            ["화면 · 배포 · 운영", "55", "12.2%", "지출 상한 이중화, 배포 감시, 화면 개편"],
            ["문서 · 발표", "40", "8.9%", "프로젝트 문서 1,765행 + 저장소 안내 552행"],
            [(["합계"], BD), (["450"], BD), (["100%"], BD), "—"]],
           [2.4, 0.85, 0.8, 3.4], caption="영역별 공수 배분 — 3인 × 15주 × 10시간",
           aligns=["l", "r", "r", "l"], size=FS_XS)],
    [Stats([("24.4%", "실험에 들어간 비중.\n단일 영역 최대"),
            ("3건", "폐기한 중간 결론.\n재실행 공수를 산정에 포함했다")], cols=1,
           size=20),
     Note("red", "산정에서 실제로 틀린 부분 — 기재한다",
          [("전문가 경로는 원래 계획에 없던 항목으로, ", {}),
           ("8차에 걸쳐 방법을 고치면서 55시간을 썼다.", BD),
           (" 그중 상당 부분이 잘못된 중간 결론을 검증하고 되돌리는 데 들어갔다. "
            "탐색적 실험의 공수는 사전 산정이 어렵다는 것을 기록해둔다.", {})],
          size=FS_XS),
     Note("note", "절감 요인",
          "캐싱 규칙(규칙 2) 덕분에 실험을 수백 회 재실행하면서도 API 비용이 "
          "폭발하지 않았다. 같은 조립 프롬프트는 두 번 호출되지 않는다.", size=FS_XS)],
], weights=[1.6, 1])], top, label="33")

# ══ 34 장 표지 5 ══════════════════════════════════════════════════════
d.chapter(CH5, "05", "프로젝트 관리",
          ["5-1 위험 관리", "5-2 품질 관리", "5-3 산출물 목록",
           "5-4 데이터 출처 및 개인정보"], "34")

# ══ 35 위험 관리 ══════════════════════════════════════════════════════
s, top = d.page(CH5, "5-1. 위험 관리 — 실제로 일어난 사고 기준",
                [("예상되는 위험을 나열하지 않는다. ", {}),
                 ("이미 발생한 사고와 그때 넣은 대응", AC), ("을 적는다.", {})],
                "35")
stack(d, s, [
    Table(["위험", "실제로 일어난 것", "영향", "대응 (구현 완료)", "잔여 위험"],
          [[(["제출 링크 다운"], BD),
            "코드만 푸시해 import된 모듈이 갱신되지 않아 배포 앱이 2회 내려앉았다",
            (["심사 불가"], RDT),
            "빌드 마커로 프로세스 재시작 강제 + 6시간 주기 배포 감시. 감시 대상에 agents 누락도 발견해 추가 — 앱을 내려앉힌 바로 그 경로다",
            "감시가 버전은 확인하지 않는다. 잔여 과제 4번"],
           [(["API 크레딧 소진"], BD),
            "유료 실행이 중단되며 끝난 저자 2명의 최적화 120회 결과가 디스크에 하나도 남지 않았다",
            (["유료 결과 유실"], RDT),
            "저자마다 즉시 저장 + 중단 사유·지점 기록. 세션 3회 / 하루 60회 상한 이중화",
            "앱 재시작 시 카운터가 0이 된다. 결제 쪽 월 상한을 대체하지 않는다"],
           [(["측정 불가 축에 시간 소진"], BD),
            "specificity 축을 5가지 방법으로 측정 시도 후 전부 실패", "상당한 공수",
            "축 채택 전 판별력 사전 측정을 절차로 고정(규칙 6). 이후 감정 축은 3,000건으로 단조성을 먼저 확인하고 만들었다",
            "코드로 못 재는 축은 여전히 다룰 수 없다"],
           [(["순환 논증 / 지표 게이밍"], BD),
            "최적화기가 내가 방금 만든 평가 함수의 구멍을 파고들었다 — 오차가 상한에 붙어 구분이 사라지자 글머리 기호를 6배 남발",
            "결과 신뢰도",
            "최적화하지 않은 지표를 반드시 병기(규칙 7). 지표를 평가에 맞춰 넓히지 않고 부분 독립 상태를 유지한다",
            "독립 지표가 하나뿐이다"],
           [(["표본 과소로 인한 오결론"], BD),
            "저자 1명 관찰로 세운 게이팅 가설이 16명에서 기각(r=−0.031). 홀드아웃 15개에서 '해로웠다'던 3명 중 2명이 45개에서 부호가 뒤집혔다",
            "중간 결론 3건 폐기",
            "절대 규칙 11 신설. 홀드아웃 30개·위치 고정, 부호검정 p값 병기",
            "실사용자 표본은 여전히 0에 가깝다"],
           [(["조용한 실패"], BD),
            "응답 수집이 UI는 정상인데 로그에 한 줄도 남지 않았다(출력 버퍼). 한글이 인코딩 때문에 깨진 것도 같은 계열",
            "수집했다고 착각",
            "줄을 ASCII 전용으로 만들어 환경 의존을 없애고, 사람이 읽을 때 집계 스크립트가 되돌린다",
            "—"],
           [(["배포 환경 자원 한도"], BD),
            "형태소 분석기 모델 88MB가 무료 티어 빌드/메모리를 위협",
            (["실격 위험"], RDT),
            "지연 로드 + 사전 없는 문자 4-gram 대체 경로. n은 72케이스 실측으로 선택(일치율 97%)",
            "대체 경로로 조용히 내려앉지 않는지 검사로 감시 중"],
           [(["화면에서만 드러나는 결함"], BD),
            "마크다운 이중 이스케이프로 코드가 글자로 표시. 카드 높이가 달라져 길이 축 판단 자체가 오염",
            "실험 타당성",
            "본문을 직접 HTML로 넣지 않고 프레임워크가 그리게 하고 높이를 고정한다. 렌더 결과를 실제로 본다를 절차에 넣었다",
            "—"]],
          [1.7, 3.4, 1.1, 3.4, 2.4],
          caption="위험 관리 — 발생 사실 / 영향 / 구현된 대응", size=FS_XS),
], top, label="35")

# ══ 36 품질 관리 ══════════════════════════════════════════════════════
s, top = d.page(CH5, "5-2. 품질 관리 — 사람 검토에 의존하지 않는다",
                [("문서의 수치와 커밋된 결과가 어긋나면 ", {}), ("검사가 실패", AC),
                 ("한다. 실제로 두 번 어긋났기 때문이다.", {})], "36")
stack(d, s, [Cols([
    [Stats([("16 → 242", "오프라인 통과 테스트\n(API 포함 258)"),
            ("39", "커밋된 결과 파일\n(JSON · CSV)"),
            ("11", "문서화된 절대 규칙")], cols=3, size=20),
     Note("red", "착수 시점의 실상 — 초록불이 거짓이었다",
          [("테스트가 전부 통과하는데도 ", {}), ("16개 파일 중 4개만 수집", BD),
           ("되고 있었고, 9개 파일은 검증문이 0개인 출력 스크립트였다. 핵심 모듈로 "
            "표시한 셋 중 하나는 테스트 파일 자체가 없었다. ", {}),
           ('"초록색이니 괜찮다"가 가장 위험한 상태였다.', BD)], size=FS_XS),
     Note("note", "표시자를 등록해 조용한 통과를 막았다",
          [("API 키나 데이터가 없으면 ", {}), ("이유를 표시하며 건너뛴다.", BD),
           (" 조용히 통과시키지 않는다. 키 없이 돌리는 경로를 별도로 두어 누구나 "
            "즉시 회귀를 확인할 수 있다.", {})], size=FS_XS)],
    [Note("ok", "테스트 값을 가정하지 않고 실측해서 넣었다",
          [("불확실도 방식이 순차보다 빠르지 않고 둘 다 6라운드에 복원하는 것이 "
            "실제 곡선이라, ", {}),
           ('"불확실도가 더 빠르다"를 테스트로 박지 않았다.', BD),
           (" 대신 둘 다 복원 후 되돌아가지 않는 것과, 무작위는 15라운드에도 "
            "유지하지 못하는 것(평균 1.8/2)을 고정했다. ", {}),
           ("바라는 값이 아니라 관측된 값을 고정한다.", BD)], size=FS_XS),
     Note("warn", "좋은 테스트의 증거",
          [("응답 기록에 필드를 추가했을 때 기존 개인정보 테스트가 ", {}),
           ("즉시 깨졌다", BD),
           (" — 키 화이트리스트를 정확히 검사하고 있었기 때문이다. 자유 텍스트를 "
            '막는 것도 규약이 아니라 코드로 강제했다(숫자만 허용). "원문은 '
            '기록하지 않는다"가 ', {}), ("약속이 아니라 성질", BD),
           ("이 됐다.", {})], size=FS_XS),
     Note("note", "품질 게이트", bullets=[
        [("생성 코드는 ", {}), ("정적 검증 + 프로세스 격리", BD),
         (" 2계층을 통과해야 실행된다. 우회 6가지 케이스로 실제 검증", {})],
        "비교 실험은 temperature=0과 고정 시드 없이는 결과로 인정하지 않는다",
        "축은 판별력 측정을 통과해야 채택된다",
        "결론은 홀드아웃 30개 이상 · 부호검정 p값 병기 후에만 쓴다",
     ], size=FS_XS)],
])], top, label="36")

# ══ 37 산출물 ═════════════════════════════════════════════════════════
s, top = d.page(CH5, "5-3. 산출물 목록",
                [("산출물의 절반은 코드가 아니라 ", {}),
                 ("측정 결과와 그 해석 기록", AC), ("이다.", {})], "37")
stack(d, s, [
    Table(["구분", "산출물", "규모", "제출 형태"],
          [["실행 코드", "도메인 무지 엔진 (5개 모듈)", "★ 핵심 기여", "저장소"],
           ["", "도메인 정의 · 검사 함수 · 데모 생성기", "카테고리 5종", "저장소"],
           ["", "에이전트 패키지 (축 발견 · 형식 추출 · 샌드박스)", "기존과 분리", "저장소"],
           ["동작하는 도구", "공개 웹 앱 (비교 경로 · 전문가 경로 · 데모 모드)", "3단계 화면",
            (["공개 링크"], HI)],
           ["", "배포 감시 워크플로 · 지출 상한", "6시간 주기", "저장소"],
           ["측정 결과", "실험 결과 파일 (수렴 · 비교군 · 판별력 · 라우팅)", "39건", "저장소 커밋"],
           ["", "그래프 (수렴 · 어블레이션 · 비교군 · 라우팅)", "6점", "발표 자료"],
           ["", "화면 캡처", "8점", "저장소 · 발표"],
           ["문서", "프로젝트 문서 — 절대 규칙 11개, 실패 경과 전문", "1,765행", "저장소"],
           ["", "저장소 안내 — 성공과 한계를 같은 비중으로 기술", "552행", "저장소"],
           ["", (["본 수행계획서"], BD), "39면", (["제출"], HI)],
           ["발표", "발표 자료 · 최종 보고서", "본선", "제출"]],
          [1.6, 5.2, 1.6, 1.7], caption="산출물 — 구분 / 내용 / 규모 / 제출 형태",
          aligns=["l", "l", "r", "l"], size=FS_XS),
    Note("note", "문서를 산출물로 세는 이유",
         [("폐기한 중간 결론 3건과 되돌린 기능 1건의 경과가 ", {}),
          ("그 문서에만 남아 있다.", BD),
          (" 같은 함정을 다시 밟지 않기 위한 기록이고, 결과 수치의 출처를 추적할 수 "
           "있게 하는 유일한 경로다. 본 계획서의 모든 수치도 그 기록과 커밋된 결과 "
           "파일에서 재계산했다.", {})], size=FS_SM),
], top, label="37")

# ══ 38 데이터 출처 ════════════════════════════════════════════════════
s, top = d.page(CH5, "5-4. 데이터 출처 및 개인정보",
                [("데이터는 ", {}), ("게이팅 없이 접근 가능한 것", AC),
                 ("만 쓴다. 신청이 필요한 데이터셋은 기각했다.", {})], "38")
stack(d, s, [Cols([
    [Table(["데이터", "접근", "이용 조건"],
           [["MACSum", "공개 저장소", "학술 이용. 원문은 뉴스·회의록에서 수집"],
            ["Stack Exchange", "공개 API\n키 불필요 · 하루 300회",
             "CC BY-SA. 표준 라이브러리만 사용 — 새 의존성 0개"],
            ["Yelp Review Full", "공개 데이터\n게이팅 없음", "3,000건만 캐싱해 사용"]],
           [1.5, 1.7, 2.8], caption="데이터 출처 및 이용 조건", size=FS_XS),
     Note("warn", "기각한 후보와 그 이유", bullets=[
        [("격식체 변환 벤치마크", BD), (" — 원저작권 신청이 필요해 당일 구현 불가", {})],
        [("인구통계 병렬 데이터", BD),
         (" — 축 자체가 성별·나이·정치성향이라 개인화 축으로 부적절", {})],
        [("회의록 요약 분할", BD), (' — 접근은 가능하나 여전히 "요약" 계열', {})],
     ], tail=[("두 번째 항목은 기술적 이유가 아니라 ", {}),
              ("윤리적 판단으로 기각", BD), ("했다.", {})], size=FS_XS)],
    [Note("red", "개인정보 보호 — 코드로 강제한다", bullets=[
        [("사용자가 입력한 ", {}), ("원문과 생성 결과를 기록하지 않는다.", BD),
         (" 개인 정보가 섞일 수 있고, 사용자가 공유할 의도로 넣은 값이 아니다", {})],
        [("응답 기록의 부가 필드는 ", {}), ("숫자만 받는다.", BD),
         (" 자유 텍스트를 허용하면 언젠가 누가 그 칸으로 원문을 흘린다", {})],
        [("전문가 경로는 ", {}), ("모델을 한 번도 호출하지 않는다.", BD),
         (" 이 경로에서 생성 함수를 호출하면 테스트가 실패한다 — 나중에 "
          '"한 번만 호출하면 더 좋을 텐데" 하고 넣는 것을 막는 장치다', {})],
        "API 키는 환경 파일에서만 읽고 커밋하지 않는다",
     ], size=FS_XS),
     Note("ok", "공개 배포에서의 키 노출 대응",
          [("공개 링크에서는 방문자가 팀의 키를 소비한다. 세션 3회 / 하루 60회 "
            "상한을 ", {}), ("확인과 차감을 한 락 안에서", BD),
           (" 수행해 동시 방문자가 마지막 한 칸을 같이 가져가는 것을 막는다. "
            "데모 모드는 키 없이 8회 완주가 가능하므로, 비용 없이 기능을 확인할 수 "
            "있는 경로가 항상 열려 있다.", {})], size=FS_XS)],
])], top, label="38")

# ══ 39 부록 A 검사 함수 대 사람 라벨 ══════════════════════════════════
s, top = d.page("부록", "부록 A. 검사 함수 대 사람 라벨 — 순환 논증 방어를 위해 잰 것",
                [("측정값은 라벨과 같은 방향이지만 ", {}),
                 ("라벨을 재현하지는 못한다.", AC),
                 (" 페르소나 실험은 YAML이 정의한 값 아래에서 성립한다.", {})], "39")
stack(d, s, [Cols([
    [Table(["축", "값", "N", "라벨 검사 통과율", "원시값 평균"],
           [["length", "short", "1,305", "81.1%", "1.74 문장"],
            ["", "normal", "2,785", (["20.4%"], RDT), "2.23 문장"],
            ["", "long", "1,289", "32.2%", "3.96 문장"],
            ["extractiveness", "normal", "4,675", "66.9%", "0.436"],
            ["", "high", "371", "39.1%", "0.553"],
            ["", "fully", "333", "78.4%", "0.836"],
            [(["(specificity)"], {"color": INK3}), "normal / high", "5,379", "검사 함수 없음",
             "0.217 / 0.228"]],
           [1.5, 1.0, 0.8, 1.4, 1.2],
           caption="MACSum macdoc 사람 작성 요약 5,379개 · API 호출 0회",
           aligns=["l", "l", "r", "r", "r"], size=FS_XS),
     Stats([("0.446", "length 균형정확도 (최적 절단점으로도 0.489)"),
            ("0.598", "extractiveness 균형정확도 (최적 절단점으로도 0.610)")], cols=2,
           size=20)],
    [Note("ok", "① 측정값은 라벨과 단조 관계다",
          [("문장 수 1.74 < 2.23 < 3.96, 겹침 0.44 < 0.55 < 0.84 — 평균 순서가 라벨 "
            "순서와 일치한다. specificity의 0.217 대 0.228(순서조차 없음)과 대조된다. "
            "검사 함수가 재는 것은 ", {}), ("사람 라벨과 같은 방향의 성질", BD),
           ("이다.", {})], size=FS_XS),
     Note("red", "② 그러나 임계값이 아니라 라벨 자체가 절대값이 아니다",
          [("MACSum \"long\" 요약의 10분위가 ", {}), ("1문장", BD),
           (", 중앙값 3문장이다. 길이 라벨은 문서 안에서의 상대 개념으로 보이고, "
            "문장 수를 세는 함수로는 원리적으로 재현할 수 없다. 임계값을 사람 분포에 "
            "맞춰 다시 골라도 0.49가 상한인 이유다.", {})], size=FS_XS),
     Note("note", "결과 해석에 미치는 영향",
          bullets=[
            [("복원율 80%는 ", {}), ("유지", BD),
             (" — 오라클도 후보 생성도 YAML 정의를 쓰므로 루프는 자기 일관적. 검증된 것은 "
              "\"YAML이 정의한 값 아래에서 선택·추정 기계가 숨긴 선호를 복원하는가\"", {})],
            [("ROUGE-L 절대값이 낮은 이유 하나가 설명됨 — D는 long=5문장+로 생성되고 "
              "사람 long은 중앙값 3문장", {})],
            [("D > B (ROUGE-L 4/5)는 그 ", {}), ("불리한 조건에서 나온 결과", BD),
             ("라 약해지지 않는다", {})],
            [("YAML은 지금 고치지 않는다 — 결과 전부가 무효가 되고, 고쳐도 상한이 0.49", {})],
          ], size=FS_XS)],
], weights=[1, 1.1])], top, label="39")

# ══ 40 부록 B 예상 질문 ① ════════════════════════════════════════════
s, top = d.page("부록", "부록 B. 예상 질문과 근거 ① — 반드시 나오는 것",
                [("데이터가 없는 질문은 ", {}), ("먼저 인정", AC),
                 ("한다. 인정하면 추궁이 오지 않는다.", {})], "40")
stack(d, s, [
    Table(["질문", "답", "근거"],
          [[(["GPT에 '내 프롬프트 만들어줘' 하면 되지 않나?"], BD),
            "그것도 \"어떤 프롬프트를 원하냐\"를 다시 묻는다 — 서술 부담이 그대로다. 상용 도구와의 직접 비교(비교군 C)는 미착수이고 데이터가 없다. 이 점을 먼저 말한다.",
            "1-1 ②, 3-10"],
           [(["B(직접 쓴 지침)를 일부러 약하게 쓴 것 아닌가?"], BD),
            "원문 그대로: \"Summarize the following article in a clear and concise way that captures the key points.\" 5개 문서 동일, 페르소나를 모르는 상태로 작성. 이것이 바로 \"자기 축을 모르는 사용자\"의 지침이다.",
            "3-3"],
           [(["0.996은 자기 채점 아닌가?"], BD),
            "맞다. 그래서 최적화에 쓰지 않은 ROUGE-L을 병기하고(0.207 대 0.168, 페어드 4/5) 유의성은 주장하지 않는다. 검사 함수 자체는 사람 라벨을 재현하지 못하며(균형정확도 0.45/0.60), 페르소나 실험은 YAML 정의 아래에서 성립한다고 먼저 말한다.",
            "3-3, 부록 A"],
           [(["축값을 알았으면 템플릿에 넣으면 되지, GEPA는 왜?"], BD),
            "대표 수치가 바로 그 템플릿이다 — D는 GEPA를 쓰지 않는다. GEPA의 효과는 시드가 나쁠 때 0.36→0.96으로 올리는 어블레이션에서만 보였고, 시드가 이미 좋으면 아무것도 하지 않는다. 제품에서 GEPA는 성능 장치가 아니라 평가 함수가 개선 가능한 형태임을 검증한 장치다.",
            "2-8, 3-2"],
           [(["실사용자가 0명인데 사람에게 통하는지 어떻게 아나?"], BD),
            "모른다. 모든 정량 결과는 페르소나 기반이다. 수집 목적은 통계적 주장이 아니라 페르소나에서 안 보이는 실패 유형을 찾는 것이고, 본선에 실제 수치를 넣는다.",
            "3-8 ①, 3-10"],
           [(["5개 카테고리 중 검증된 건 몇 개인가?"], BD),
            "동작 5종. 정량 검증 1종(영어 요약), 부분 검증 3종(한국어 스팟 체크·코딩 가상 사용자·리뷰 감정 축 단조성), 동작 확인만 1종(이메일).",
            "3-5"]],
          [2.6, 7.5, 1.1], caption="반드시 나오는 질문", size=FS_XS),
], top, label="40")

# ══ 41 부록 C 예상 질문 ② ════════════════════════════════════════════
s, top = d.page("부록", "부록 C. 예상 질문과 근거 ② — 날카로운 것",
                [("본문이 스스로 열어둔 구멍이다. ", {}),
                 ("방어하려 들지 말고 넘긴다.", AC)], "41")
stack(d, s, [
    Table(["질문", "답", "근거"],
          [[(["long 선호 사용자에게는 실패하는 것 아닌가?"], BD),
            "3번 중 2번 normal로 수렴한다. 8라운드 안에서 인접 값을 가르기 어렵고, 라운드를 늘렸을 때는 미측정이다. 그리고 MACSum의 long 자체가 절대 문장 수가 아니라(중앙값 3문장) 그 축의 정의가 사람 관례와 어긋나 있다.",
            "3-4, 부록 A"],
           [(["가장 강한 결과가 보조 경로인데 왜 보조인가?"], BD),
            "대상 사용자가 다르다. 전문가 경로(0.308→0.146, 8/8, p=0.008)는 자기 글 30개 이상이 있는 사람만 쓸 수 있고, 주 경로는 아무나 8번 클릭하면 된다.",
            "2-1, 3-6"],
           [(["specificity가 '측정 불가'라는 건 5개 시도로 일반화한 것 아닌가?"], BD),
            "맞다. 주장을 \"표면 신호와 제로샷 모델 판정으로는 측정 불가\"로 좁힌다. 5가지 방법은 전부 그 범주다.",
            "3-7"],
           [(["평가 함수 구멍을 최적화기가 파고든 사고 — 지금도 그 상태 아닌가?"], BD),
            "보장하지 못한다. 그래서 최적화하지 않은 지표를 병기하고, 지표를 평가에 맞춰 넓히지 않는다. 독립 지표가 하나뿐인 것은 잔여 위험으로 적어뒀다.",
            "5-1"],
           [(["최적화 버튼이 아무것도 안 하면 GEPA를 빼도 되지 않나?"], BD),
            "제품 성능 근거로는 뺄 수 있다. 남기는 이유는 시드를 중립으로 두면 개선 여지가 생기고(어블레이션), 그때 독립 지표로 재는 것이 잔여 과제 2번이기 때문이다.",
            "2-8, 3-8 ②"],
           [(["심사 당일 여러 명이 동시에 쓰면 상한에 걸리지 않나?"], BD),
            "하루 상한 60회는 8라운드 세션 60번(약 960 호출)이다. 10명이 3번씩 써도 30회. 데모 모드는 상한 없이 8회 완주가 된다.",
            "2-3 FR-12"],
           [(["15주 계획서인데 대부분 완료인데, 계획서인가 결과보고서인가?"], BD),
            "v2 기준 현황을 병기한 계획서다. 잔여 과제 6건과 완료 판정 기준이 3-8에 있고, 남은 항목은 전부 확보된 결과의 방어에 걸린 것이다.",
            "3-8, 3-9"]],
          [2.6, 7.5, 1.1], caption="날카로운 질문", size=FS_XS),
], top, label="41")

# ══ 39 마무리 ═════════════════════════════════════════════════════════
d.cover(
    "핵심 주장",
    "선택 기반 개인화는\n작동한다.",
    "다만 모든 취향 축이 측정 가능한 것은 아니며,\n어떤 축이 그러한지를 실험으로 구분했다.",
    [("강조할 것", "고치고 나서야 알게 된 함정\n복원율 0~8% → 80%"),
     ("피할 것", '"범용적으로 더 좋은 프롬프트"라는 주장'),
     ("보고 원칙", "순환 논증으로 보이는\n단일 지표 보고 금지")],
)

out = sys.argv[1] if len(sys.argv) > 1 else "plan.pptx"
d.prs.save(out)
print(f"저장: {out}  슬라이드 {len(d.prs.slides)}장")
if d.warnings:
    print(f"\n넘침 경고 {len(d.warnings)}건:")
    for w in d.warnings:
        print("  -", w)
else:
    print("넘침 없음")
